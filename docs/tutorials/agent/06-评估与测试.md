# Agent 06 - 评估与测试

> 没有评估，就没有改进。Agent 工程的最大坑：你以为改好了，其实改坏了。
> 本节是阶段 4 的收尾，建立评估体系后才能进入阶段 5。

---

## 1. 为什么 Agent 测试难

### 1.1 与传统测试的差异

| 维度 | 传统测试 | Agent 测试 |
|------|---------|-----------|
| 输入 | 确定 | 自然语言（多变性） |
| 输出 | 确定 | 自然语言（多义性） |
| 行为 | 单一路径 | 多种合理路径 |
| 可重复 | ✅ | ❌（LLM 有概率） |
| 失败原因 | 代码 bug | prompt / Tool / 模型 / 参数 |

### 1.2 Agent 测试的三大类

```
1. 单元测试：单个 Tool 的输入输出
2. 集成测试：Tool + 业务 Service 协作
3. 端到端测试：用户问题 → Agent 回答
```

阶段 4 重点在 **端到端测试**。

---

## 2. 评估集设计

### 2.1 评估集结构

```json
{
  "id": "w1",
  "input": "明天北京天气",
  "expected_tools": ["getWeather"],
  "expected_keywords": ["温度", "度", "晴雨雪"],
  "expected_tool_calls": {
    "getWeather": {"city": "北京", "date": "*"}
  },
  "category": "weather",
  "difficulty": "easy"
}
```

### 2.2 字段说明

| 字段 | 作用 |
|------|------|
| `input` | 用户问题 |
| `expected_tools` | 应该调用的 Tool 列表 |
| `expected_keywords` | 回答中应该包含的关键词 |
| `expected_tool_calls` | Tool 应该用什么参数（支持通配符） |
| `category` | 分类（用于按类别统计） |
| `difficulty` | easy / medium / hard |

### 2.3 难度分级

| 难度 | 特征 | 示例 |
|------|------|------|
| easy | 单 Tool，参数明确 | "查北京天气" |
| medium | 单 Tool，参数需推理 | "我明天出差去上海，要带伞吗" |
| hard | 多 Tool 串联 | "查张三工位" |
| expert | 多 Tool + 上下文 | "我和张三的待办合在一起" |

---

## 3. 评估维度

### 3.1 Tool 选择准确率

LLM 是否选对了 Tool？

```
是否调用了 expected_tools？
是否没调用多余的 Tool？
```

### 3.2 参数填充准确率

参数是否填对？

```
getWeather(city="北京", date="2026-07-13")
                ↑ 对         ↑ 对（"明天"被正确转换）
```

### 3.3 回答质量

回答是否包含关键信息？

```
expected_keywords = ["温度", "度"]
result = "明天北京 25 度，晴"
匹配："度" ✅，但缺"温度" → 部分通过
```

### 3.4 鲁棒性

同一问题问 10 次，准确率多少？

```
"明天北京天气" 问 10 次：
  9 次 getWeather 调对了 → 准确率 90%
  1 次 LLM 编了答案 → 准确率 10% 那次
```

---

## 4. 评估框架实现

### 4.1 简单实现（自建）

```java
public class AgentEvaluator {

    private final ChatClient client;
    private final ToolCallRecorder recorder;

    public EvalResult evaluate(TestCase testCase) {
        recorder.start();
        String response = client.prompt()
                .user(testCase.input())
                .call()
                .content();
        List<ToolCall> calls = recorder.stop();

        return EvalResult.builder()
                .caseId(testCase.id())
                .input(testCase.input())
                .response(response)
                .toolCalls(calls)
                .toolSelectionScore(scoreToolSelection(calls, testCase.expectedTools()))
                .paramScore(scoreParams(calls, testCase.expectedToolCalls()))
                .answerScore(scoreAnswer(response, testCase.expectedKeywords()))
                .passed(isPassed(testCase, calls, response))
                .build();
    }

    private boolean isPassed(TestCase tc, List<ToolCall> calls, String resp) {
        // 至少调对了 Tool
        boolean toolOk = tc.expectedTools().stream()
                .allMatch(t -> calls.stream().anyMatch(c -> c.name().equals(t)));
        // 至少一个关键词命中
        boolean kwOk = tc.expectedKeywords().stream()
                .anyMatch(resp::contains);
        return toolOk && kwOk;
    }
}
```

### 4.2 ToolCallRecorder（关键工具）

需要记录 Agent 调了哪些 Tool、参数是什么。

#### LangChain4j 实现

用 `ToolExecutionRequest` 拦截：
```java
// 自定义 ToolListener 或 ToolExecutionLogger
```

#### Spring AI 实现

写一个 Advisor（Spring AI 1.0.0 API）：
```java
public class ToolCallRecorderAdvisor implements CallAdvisor {
    private final ThreadLocal<List<ToolCall>> recorder = new ThreadLocal<>();

    @Override
    public String getName() { return "ToolCallRecorderAdvisor"; }

    @Override
    public int getOrder() { return 50; }

    @Override
    public ChatClientResponse adviseCall(ChatClientRequest req, CallAdvisorChain chain) {
        // 拦截 Tool 调用，记录到 ThreadLocal
        // 1.0.0 入参是 ChatClientRequest，链推进用 chain.nextCall(req)
    }
}
```

### 4.3 批量评估

```java
public EvaluationReport evaluateAll(Path jsonlFile) {
    List<TestCase> cases = loadCases(jsonlFile);
    List<EvalResult> results = cases.stream()
            .map(this::evaluate)
            .toList();

    return EvaluationReport.builder()
            .totalCases(results.size())
            .passed((int) results.stream().filter(EvalResult::passed).count())
            .passRate(results.stream().filter(EvalResult::passed).count()
                    * 100.0 / results.size())
            .byCategory(statsBy(results, EvalResult::category))
            .byDifficulty(statsBy(results, EvalResult::difficulty))
            .failedCases(results.stream()
                    .filter(r -> !r.passed())
                    .toList())
            .build();
}
```

---

## 5. 自动化评估指标

### 5.1 简单指标（关键词匹配）

```java
double answerScore = expectedKeywords.stream()
        .filter(resp::contains)
        .count() * 1.0 / expectedKeywords.size();
```

### 5.2 LLM-as-Judge（更准）

让另一个 LLM 给回答打分：

```java
@SystemMessage("""
    你是评估员，给 AI 助手的回答打分。
    
    评估维度：
    1. 是否调用了正确的工具
    2. 回答是否准确
    3. 回答是否完整
    
    返回 JSON：{"score": 0-100, "reason": "..."}
    """)
EvaluationResult judge(String userQuery, String aiResponse, String expectedTool);
```

### 5.3 RAGAS（针对 RAG Agent）

详见 `reference/生产化与运营/11-LLMOps.md`。

---

## 6. 回归测试

### 6.1 改 prompt 前先跑评估

```bash
mvn test -Dtest=AgentEvaluationTest
# 当前准确率：85%
```

### 6.2 改 prompt 后再跑

```bash
mvn test -Dtest=AgentEvaluationTest
# 新准确率：92% → 提升
```

### 6.3 集成进 CI/CD

```yaml
# .gitlab-ci.yml
test:
  script:
    - mvn test -Dtest=AgentEvaluationTest
  only:
    - main
```

准确率下降超过 5% → 拦截合并。

---

## 7. 鲁棒性测试

### 7.1 重复 N 次

```java
@Test
void robustness_weatherQuery() {
    int trials = 10;
    int passed = 0;
    for (int i = 0; i < trials; i++) {
        String r = client.prompt().user("明天北京天气").call().content();
        if (r.contains("温度") || r.contains("度")) passed++;
    }
    assertTrue(passed >= trials * 0.9);  // 至少 90% 通过
}
```

### 7.2 同义不同说法

```java
@ParameterizedTest
@ValueSource(strings = {
    "明天北京天气",
    "明天北京天气怎么样",
    "明天北京什么天气",
    "明天去北京要穿什么",
    "明天北京会下雨吗"
})
void weatherVariations(String q) {
    String r = client.prompt().user(q).call().content();
    assertNotNull(r);
}
```

---

## 8. 失败用例分析

### 8.1 收集失败案例

```java
results.stream()
        .filter(r -> !r.passed())
        .forEach(r -> log.error("FAIL {} input={} tools={} resp={}",
            r.caseId(), r.input(), r.toolCalls(), r.response()));
```

### 8.2 失败分类

| 失败类型 | 表现 | 解决方向 |
|---------|------|---------|
| Tool 选错 | 该调 A 调了 B | 改 Tool 描述 |
| 参数错 | city 填了"上海"（应为"北京"） | 改参数描述 |
| 没调 Tool | LLM 直接编造 | 强化 system prompt |
| 多调 Tool | 调了无关 Tool | 描述写"不适用场景" |
| 回答不完整 | 缺关键词 | system prompt 要求"包含 X" |

### 8.3 迭代优化

每改一处 prompt / Tool 描述，**跑评估集验证**。

---

## 9. 用真实数据评估

### 9.1 生产日志

把生产的用户 query 脱敏后，作为评估集。

### 9.2 用户反馈

加 "👍 / 👎" 按钮，记录用户反馈。

### 9.3 A/B 测试

```
对照组（旧 prompt）：1000 个请求，准确率 85%
实验组（新 prompt）：1000 个请求，准确率 88%
```

---

## 10. 完整评估流程

```
1. 准备评估集（20-100 条）
   ↓
2. 跑评估
   ↓
3. 分析失败案例
   ↓
4. 改 prompt / Tool 描述
   ↓
5. 回归测试
   ↓
6. 重复 3-5 直到准确率达标（如 80%+）
   ↓
7. 接入 CI/CD 持续监控
```

---

## 11. 现成工具推荐

### 11.1 Promptfoo（推荐）

`promptfoo.dev`：开源 prompt 评估工具。

- YAML 定义评估规则
- 支持 LLM-as-Judge
- CLI 工具，CI 友好

```yaml
# promptfooconfig.yaml
prompts:
  - file://prompts/weather.txt
providers:
  - deepseek:deepseek-chat
tests:
  - vars:
      query: "明天北京天气"
    assert:
      - contains: "温度"
```

### 11.2 RAGAS

RAG 专用，参考 `reference/生产化与运营/11-LLMOps.md`。

### 11.3 LangSmith / Langfuse

生产可观测，参考 `reference/生产化与运营/11-LLMOps.md`。

---

## 12. 阶段 4 完成检查清单

完成本节后，你应该能：

- [ ] 设计一份完整的评估集（20+ 条）
- [ ] 实现 `AgentEvaluator`（哪怕简单版）
- [ ] 跑出当前 Agent 的准确率
- [ ] 改 prompt 后用评估集验证
- [ ] 找出至少 3 类失败模式
- [ ] 决定下一步优化方向

---

## 13. 阶段 4 收尾

写一篇阶段总结 `notes/W6-Evaluation.md`：

1. **你的 Agent 准确率多少**？哪些类型最容易失败？
2. **改 prompt 前后的准确率变化**
3. **评估体系让你少走了哪些弯路**
4. **进阶段 5 的目标是什么**？

---

## 14. 阶段 4 整体回顾

完成阶段 4，你已经是合格的 Java Agent 工程师了。

**你掌握的能力**：
- ✅ Tool 设计原则（单一职责、描述清晰、参数规范）
- ✅ Agent 防失控（迭代上限、超时、循环检测、成本控制）
- ✅ 多 Tool 编排（路由、状态机、并行）
- ✅ 完整项目（个人助理 / 智能运维）
- ✅ 评估体系（评估集、回归测试、LLM-as-Judge）

**接下来**：
- 阶段 5（生产化）—— 流式、Redis、限流、监控
- 阶段 6（综合项目）—— 把所有能力整合到一个大型项目

**强烈推荐先做阶段 5**。你的项目目前是"能跑"，阶段 5 把它推向"能上线"。
