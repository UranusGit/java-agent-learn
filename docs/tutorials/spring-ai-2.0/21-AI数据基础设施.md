# L3 AI 数据基础设施（Spring AI 2.0）

> 本文回答：**AI 应用的"数据"和传统应用有什么不同？需要哪几套数据管道？向量库怎么选？**
>
> 数据是 AI 应用的"原料"。原料不行，模型再强也白搭。
>
> 前置：[`./13-RAG工程化实战.md`](./13-RAG工程化实战.md)
> 预计：1.5 天

---

## 0. 认知地图

```
传统应用数据：CRUD + 主从复制 + 缓存
    +
AI 应用新增：
    ├── 文档数据（PDF / HTML / Markdown） → 文档管道
    ├── 向量数据（embedding） → 向量库 + ANN 索引
    ├── 对话数据（session / memory） → 会话管道
    └── 反馈数据（👍/👎 / 重写） → 评估管道
```

---

## 1. 三套数据管道

### 1.1 文档管道（Document Pipeline）

```
源数据（Wiki / PDF / DB）
    ↓ Extract（抽取）
原始文本 / 表格 / 图片
    ↓ Transform（清洗、富化、分块）
Document 对象
    ↓ Load（写入）
向量库 + 关系库
```

**特点**：

- 批处理为主（每天 / 每小时跑一次）。
- 数据量大（GB-TB 级）。
- 重视一致性（增量更新、版本控制）。

### 1.2 实时事件管道（Event Pipeline）

```
用户操作 / Agent 调用 / 工具执行
    ↓ Kafka / RabbitMQ
事件流
    ↓ Stream Processor
聚合 / 派生视图 / 告警
```

**特点**：

- 流式、低延迟（秒级）。
- 事件源（Event Sourcing）。
- 派生视图（CQRS）。

### 1.3 反馈数据管道（Feedback Pipeline）

```
用户行为（点击 / 停留 / 重试）
    +
显式反馈（👍/👎 / 重写）
    ↓
反馈事件
    ↓
质量分析 + 训练数据集
```

**特点**：

- 来源分散（前端埋点 + 后端日志 + DB）。
- 用于评估和持续优化。
- 转化为新版本 Prompt / RAG / 模型微调。

---

## 2. 文档管道深入

### 2.1 文档接入的多种来源

| 来源 | 接入方式 | 挑战 |
|------|---------|------|
| Confluence / Notion | REST API 同步 | 增量、版本、权限 |
| GitHub / GitLab | Webhook + clone | 大文件、二进制 |
| PDF / Word | 上传 | 表格、图片、OCR |
| 数据库（业务表） | JDBC 同步 | 结构化数据如何向量化 |
| 网页（爬虫） | Scheduled crawl | 反爬、JS 渲染 |
| 邮件 | IMAP 拉取 | 附件、隐私 |

### 2.2 增量索引

新文档来了，怎么知道哪些 chunk 要重建？

**方案 1：基于 hash**

```java
public void ingest(Document doc) {
    String newHash = sha256(doc.getText());
    String existingHash = docRepo.findHash(doc.getId());
    
    if (newHash.equals(existingHash)) {
        return;  // 没变
    }
    
    // 删除旧 chunk
    vectorStore.delete(List.of(doc.getId()));
    
    // 重新分块 + 入库
    List<Document> chunks = splitter.split(doc);
    vectorStore.add(chunks);
    
    docRepo.updateHash(doc.getId(), newHash);
}
```

**方案 2：基于时间戳**

```sql
SELECT * FROM docs 
WHERE updated_at > :lastSyncTime;
```

简单但不可靠（同内容改了又改回，仍会重算）。

**方案 3：基于版本**

每个文档维护 version 字段，索引时记录 version，跳过更早版本。

### 2.3 多格式文档处理

PDF 表格抽取（参考代码）：

```java
// org.demo02.data.pdf.PdfTableExtractor
// 本代码仅作学习材料参考

@Component
public class PdfTableExtractor {

    public List<Document> extract(Reader reader) {
        PdfDocument pdf = new PdfDocument(reader);
        List<Document> docs = new ArrayList<>();
        
        for (int i = 1; i <= pdf.getNumberOfPages(); i++) {
            // 提取页面文本
            String text = PdfTextExtractor.getText(pdf.getPage(i));
            docs.add(new Document(text, Map.of("page", i, "type", "text")));
            
            // 提取表格（用 Tabula 或类似库）
            List<Table> tables = extractTables(pdf.getPage(i));
            for (int j = 0; j < tables.size(); j++) {
                String tableMarkdown = tableToMarkdown(tables.get(j));
                docs.add(new Document(tableMarkdown,
                        Map.of("page", i, "type", "table", "table_index", j)));
            }
        }
        return docs;
    }
}
```

### 2.4 元数据规范

强制每个文档带这些元数据：

```java
Map<String, Object> standardMetadata = Map.of(
        "source", "confluence",           // 来源
        "source_url", "https://...",       // 原文链接
        "department", "hr",                // 部门
        "tenant_id", "tenant-001",         // 租户
        "doc_id", "doc-abc",               // 原文 ID
        "version", "v3",                   // 版本
        "ingested_at", Instant.now().toString(),  // 入库时间
        "language", "zh",                  // 语言
        "permissions", List.of("hr-read")  // 访问权限
);
```

---

## 3. 向量库选型矩阵

### 3.1 主流向量库对比

| 向量库 | 类型 | 优势 | 劣势 | 适合 |
|-------|------|------|------|------|
| **pgvector** | PostgreSQL 扩展 | 一套 DB 搞定关系 + 向量；RLS；事务 | 性能不如专用 | 中小规模（< 10M 向量）；强一致性需求 |
| **Milvus** | 专用 | 高性能、可水平扩展 | 部署复杂 | 大规模（亿级）；纯向量场景 |
| **Qdrant** | 专用 | Rust 实现，性能强；Rust 生态 | 生态小 | 中大规模；性能优先 |
| **Weaviate** | 专用 | 内置 hybrid search；GraphQL | 学习曲线 | 混合检索需求 |
| **Elasticsearch** | 搜索引擎 | 已有 ES 团队直接复用 | 向量性能中等 | 已用 ES 的团队 |
| **Chroma** | 嵌入式 | 简单；开发期方便 | 不适合生产 | 原型开发 |
| **Redis Stack** | 内存 | 极快 | 内存贵 | 缓存场景 |

### 3.2 决策树

```
你的向量规模 < 100 万？
├── 是 → pgvector（一个 DB 搞定，简单）
└── 否 →
    你的向量规模 < 1000 万？
    ├── 是 →
    │   有 PostgreSQL 运维经验？
    │   ├── 是 → pgvector（依然够用）
    │   └── 否 → Qdrant（独立但简单）
    └── 否 →
        需要 hybrid search（向量 + BM25）？
        ├── 是 → Weaviate / Elasticsearch
        └── 否 → Milvus（大规模纯向量）
```

### 3.3 Spring AI 接入

Spring AI 提供统一的 `VectorStore` 接口：

```java
// pgvector
@Bean
public VectorStore vectorStore(JdbcTemplate jdbc, EmbeddingModel embedding) {
    return PgVectorStore.builder(jdbc, embedding)
            .dimensions(1024)
            .distanceType(PgDistanceType.COSINE_DISTANCE)
            .indexType(IndexType.HNSW)
            .build();
}

// Milvus
@Bean
public VectorStore vectorStore(MilvusClient client, EmbeddingModel embedding) {
    return MilvusVectorStore.builder(client, embedding)
            .collectionName("documents")
            .databaseName("default")
            .initializeSchema(true)
            .build();
}
```

业务代码无需关心底层：

```java
List<Document> hits = vectorStore.similaritySearch(
        SearchRequest.builder().query("...").topK(5).build());
```

### 3.4 索引类型选择

| 索引 | 召回率 | 延迟 | 适合 |
|------|--------|------|------|
| **暴力搜索（无索引）** | 100% | 慢 | 小数据（< 10万） |
| **HNSW** | 高（>95%） | 快 | 通用首选 |
| **IVF** | 中 | 中 | 大规模、可接受稍低召回 |
| **PQ（乘积量化）** | 中低 | 极快、省内存 | 超大规模、内存受限 |

**推荐**：HNSW（99% 场景适用），大规模再上 IVF + PQ。

---

## 4. 会话数据管理

### 4.1 Memory 的存储分层

```
Hot Memory（Redis）：当前活跃会话，TTL 1 小时
    ↓ 不活跃
Warm Memory（PostgreSQL）：30 天内的会话，可查
    ↓ 过期
Cold Memory（对象存储）：归档，长期保留
```

### 4.2 Spring AI ChatMemory 持久化

```java
// JDBC 持久化
@Bean
public ChatMemory chatMemory(JdbcTemplate jdbc) {
    return JdbcChatMemoryRepository.builder()
            .jdbcTemplate(jdbc)
            .dialect(PostgresDialect.create())
            .build();
}
```

```sql
CREATE TABLE spring_ai_chat_memory (
    conversation_id VARCHAR(36) NOT NULL,
    messages JSONB NOT NULL,
    updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
    PRIMARY KEY (conversation_id)
);

CREATE INDEX idx_memory_updated ON spring_ai_chat_memory(updated_at);
```

### 4.3 会话归档

```java
@Scheduled(cron = "0 0 3 * * *")  // 每天凌晨 3 点
public void archiveOldSessions() {
    LocalDate threshold = LocalDate.now().minusDays(30);
    List<String> oldSessions = jdbc.queryForList(
            "SELECT conversation_id FROM spring_ai_chat_memory WHERE updated_at < ?",
            String.class, threshold.atStartOfDay());
    
    for (String sessionId : oldSessions) {
        // 归档到对象存储
        String content = jdbc.queryForObject(
                "SELECT messages::text FROM spring_ai_chat_memory WHERE conversation_id = ?",
                String.class, sessionId);
        objectStorage.put("archive/" + sessionId + ".json", content);
        
        // 从主库删除
        jdbc.update("DELETE FROM spring_ai_chat_memory WHERE conversation_id = ?", sessionId);
    }
}
```

---

## 5. 反馈数据管道

### 5.1 显式反馈

```java
@RestController
@RequestMapping("/feedback")
public class FeedbackController {

    @PostMapping
    public void record(@RequestBody FeedbackRequest req) {
        FeedbackEvent event = new FeedbackEvent(
                UUID.randomUUID().toString(),
                req.sessionId(),
                req.messageId(),
                req.userId(),
                req.type(),        // LIKE / DISLIKE / REPORT
                req.comment(),
                Instant.now()
        );
        feedbackRepo.save(event);
        
        // 推送到 Kafka 给评估管道
        kafka.send("feedback-events", event);
    }
}

public enum FeedbackType { LIKE, DISLIKE, REPORT, REWRITE }
```

### 5.2 隐式反馈

```java
public class ImplicitFeedbackTracker {

    public void recordMessageShown(String messageId, String userId) {
        // 用户看到了消息
    }

    public void recordCopyAction(String messageId) {
        // 用户复制了消息（强烈正面信号）
    }

    public void recordRegenerate(String sessionId) {
        // 用户点了"重新生成"（负面信号）
    }

    public void recordDwellTime(String messageId, long millis) {
        // 停留时间（< 1s 可能是不相关，> 30s 可能是认真阅读）
    }
}
```

### 5.3 反馈转化为训练数据

收集足够的反馈后，可以：

1. **构造评估集**：DISLIKE 的 case 加入评估集做回归。
2. **Few-shot 优化**：LIKE 的 case 加入 prompt 的 few-shot。
3. **模型微调**：（高级）收集大量 pair（差答案 → 好答案）做 RLHF。

---

## 6. 数据质量

### 6.1 数据质量维度

| 维度 | 检查 |
|------|------|
| 完整性 | 必填字段非空 |
| 一致性 | 多源数据匹配（如 HR 系统和工号系统） |
| 时效性 | 数据及时更新（每小时 / 每天） |
| 准确性 | 数据真实反映现实 |
| 唯一性 | 没有重复 |
| 合规性 | PII 脱敏、数据保留期 |

### 6.2 数据质量监控

```java
@Component
public class DataQualityMonitor {

    public DataQualityReport check() {
        DataQualityReport report = new DataQualityReport();
        
        // 完整性：必填字段空值率
        report.setMissingMetadataRate(
                checkRate("SELECT COUNT(*) FROM document_chunks WHERE metadata->>'source' IS NULL"));
        
        // 唯一性：重复 chunk 比例
        report.setDuplicateRate(
                checkRate("SELECT COUNT(*) - COUNT(DISTINCT content_hash) FROM document_chunks"));
        
        // 时效性：超过 7 天未更新的文档比例
        report.setStaleRate(
                checkRate("SELECT COUNT(*) FROM docs WHERE updated_at < NOW() - INTERVAL '7 days'"));
        
        // 告警
        if (report.getMissingMetadataRate() > 0.05) {
            alertService.notify("Missing metadata rate too high: " + report.getMissingMetadataRate());
        }
        return report;
    }
}
```

### 6.3 数据血缘（Data Lineage）

记录每个 chunk 从哪来、被谁用过：

```sql
CREATE TABLE data_lineage (
    chunk_id VARCHAR(64),
    source_type VARCHAR(32),     -- 'pdf' / 'wiki' / 'db'
    source_id VARCHAR(64),
    source_url TEXT,
    extracted_at TIMESTAMP,
    ingested_at TIMESTAMP,
    transformed_by VARCHAR(64),  -- 处理管道版本
    PRIMARY KEY (chunk_id)
);
```

出问题时能追溯到具体源头。

---

## 7. 数据安全与合规

### 7.1 PII 识别与脱敏

入库前自动识别 PII：

```java
@Component
public class PiiFilter {

    private static final Pattern ID_CARD = Pattern.compile("\\d{17}[\\dXx]");
    private static final Pattern PHONE = Pattern.compile("1[3-9]\\d{9}");
    private static final Pattern EMAIL = Pattern.compile("[\\w.-]+@[\\w.-]+\\.\\w+");

    public String mask(String text) {
        text = ID_CARD.matcher(text).replaceAll(match -> 
                maskKeep(match.group(), 4, 4));   // 保留前 4 后 4
        text = PHONE.matcher(text).replaceAll(match -> 
                maskKeep(match.group(), 3, 4));
        text = EMAIL.matcher(text).replaceAll(match -> {
            String email = match.group();
            int at = email.indexOf('@');
            return maskKeep(email.substring(0, at), 1, 1) + email.substring(at);
        });
        return text;
    }

    private String maskKeep(String s, int prefix, int suffix) {
        if (s.length() <= prefix + suffix) return "***";
        return s.substring(0, prefix) + "*".repeat(s.length() - prefix - suffix) + s.substring(s.length() - suffix);
    }
}
```

或者用 LLM 做 PII 识别（更准但贵）：

```java
public String llmMask(String text) {
    return judgeClient.prompt()
            .system("""
                把文本中的 PII（身份证、电话、邮箱、姓名、地址）替换为 [REDACTED]。
                保留其他内容不变。直接输出脱敏后的文本，不要解释。
                """)
            .user(text)
            .call().content();
}
```

### 7.2 数据保留期

```sql
-- 自动清理过期数据
CREATE OR REPLACE FUNCTION cleanup_old_data() RETURNS void AS $$
BEGIN
    DELETE FROM chat_messages WHERE created_at < NOW() - INTERVAL '90 days';
    DELETE FROM audit_log WHERE created_at < NOW() - INTERVAL '365 days';
    DELETE FROM feedback WHERE created_at < NOW() - INTERVAL '180 days';
END;
$$ LANGUAGE plpgsql;

-- 每天凌晨跑
SELECT pg_cron.schedule('0 3 * * *', 'SELECT cleanup_old_data()');
```

### 7.3 数据删除权（GDPR）

用户要求删除数据时：

```java
@Service
public class UserDataDeletionService {

    @Transactional
    public void deleteAll(String userId) {
        // 1. 对话历史
        jdbc.update("DELETE FROM spring_ai_chat_memory WHERE conversation_id IN " +
                "(SELECT id FROM conversations WHERE user_id = ?)", userId);
        
        // 2. 反馈
        jdbc.update("DELETE FROM feedback WHERE user_id = ?", userId);
        
        // 3. 审计日志（注意：合规可能要求保留）
        // jdbc.update("DELETE FROM audit_log WHERE user_id = ?", userId);
        
        // 4. 向量库（用户上传的文档）
        jdbc.update("DELETE FROM document_chunks WHERE metadata->>'uploaded_by' = ?", userId);
        
        // 5. 对象存储
        objectStorage.deleteAll("user-uploads/" + userId + "/");
        
        // 6. 备份（告知合规部门，备份中的副本将在 X 天后随过期删除）
    }
}
```

---

## 8. 实战避坑

### 8.1 "向量维度不匹配"

**症状**：换 embedding 模型后，向量库报维度错。

**原因**：bge-large-zh 是 1024 维，text-embedding-3-large 是 3072 维，不能混用。

**解决**：

- 一开始就用一致维度。
- 换模型时全量重建索引（reindex）。
- 多模型并存时建多个 collection。

### 8.2 "文档更新但向量没更新"

**症状**：用户在 Wiki 改了内容，RAG 还是查到旧的。

**原因**：增量索引没做好。

**解决**：见 §2.2，hash 或版本号校验。

### 8.3 "向量库数据膨胀"

**症状**：1 亿条 chunk，查询延迟从 50ms 涨到 2s。

**解决**：

- 上 HNSW 索引。
- 分片（按部门 / 租户拆 collection）。
- 删除过期 / 无效 chunk。

### 8.4 "Memory 内存爆炸"

**症状**：长会话累积，prompt 超出 context。

**解决**：

- 用 `MessageWindowChatMemory` 限制最大消息数。
- 超出窗口时调 LLM 生成摘要，保留摘要 + 最近 N 条。

### 8.5 "Feedback 数据被埋点淹没"

**症状**：埋点太多，反馈信号被噪声淹没。

**解决**：

- 区分关键事件（COPY / DISLIKE）和弱信号（停留时间）。
- 用 LLM-as-Judge 给每个 case 算质量分，弱信号只作辅助。

---

## 9. 实战任务

1. 给你的 RAG 系统加增量索引：文档 hash 变化才重建。
2. 实现 PDF 文档的表格抽取（用 Tabula 或类似库）。
3. 给 ChatMemory 接 JDBC 持久化，并实现 30 天后会话归档。
4. 实现 `FeedbackController`，记录 LIKE/DISLIKE 并推送到 Kafka。
5. 实现 `PiiFilter`，入库前自动脱敏身份证、电话、邮箱。
6. 实现数据质量监控看板：缺失元数据率、重复率、过期率。
7. （进阶）实现数据删除权（GDPR）：用户要求删除时清掉所有相关数据。
8. （选做）评估 pgvector vs Milvus：用同样 100 万文档，看查询延迟对比。

---

## 10. 理解检查

1. AI 应用需要哪三套数据管道？各自特点？
2. pgvector vs Milvus 各自适合什么场景？
3. HNSW / IVF / PQ 索引各自特点？默认选哪个？
4. 文档增量索引怎么做？hash / 时间戳 / 版本号各自的优劣？
5. 显式反馈 vs 隐式反馈，哪个更可靠？怎么结合？
6. PII 脱敏的两种方式（规则 vs LLM）各自的优劣？

---

## 11. 进 L3 下一篇之前的能力确认

完成本篇你应该能：

- [ ] 设计完整的文档管道（接入 / 增量 / 元数据规范）
- [ ] 选对向量库和索引类型
- [ ] 实现 ChatMemory 持久化 + 归档
- [ ] 收集显式和隐式反馈数据
- [ ] 自动识别 PII 并脱敏
- [ ] 监控数据质量指标

---

## 12. 相关文档

- [`./13-RAG工程化实战.md`](./13-RAG工程化实战.md) —— RAG 数据基础
- [`./19-AI原生系统设计.md`](./19-AI原生系统设计.md) —— 事件溯源架构
- [`./22-Agent可观测性完整栈.md`](./22-Agent可观测性完整栈.md) —— 数据可观测
- [pgvector 文档](https://github.com/pgvector/pgvector)
- [Milvus 文档](https://milvus.io/docs)

---

回到 [`./00-目录索引.md`](./00-目录索引.md)。
