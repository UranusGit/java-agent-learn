# 14 可观测性与成本治理

> 本文合并自原 07「可观测成本可靠性」+ 原 22「Agent 可观测性完整栈」+ 原 18「多模型路由与成本治理」的评估部分。
>
> 一篇搞定 LLM 应用的 Trace / Metrics / Logs + Drift 检测 + Prompt 血缘 + 成本治理。
>
> 前置：[`./03-Advisor链全解.md`](./03-Advisor链全解.md)
> 预计：2 天

---

## 0. 认知地图

```
传统 APM：Trace + Metrics + Logs（够用）
    +
Agent 新增：
    ├── Prompt 血缘（哪个 prompt 版本，渲染后是什么）
    ├── 模型血缘（用了哪个模型，参数是什么）
    ├── 工具调用血缘（调了什么工具，参数 + 结果）
    ├── RAG 检索血缘（查到了什么 chunk，相似度多少）
    ├── 质量指标（faithfulness、用户反馈率）
    └── Drift 检测（指标随时间漂移）
```

**核心心法**：Agent 应用比传统应用难调试 10 倍 —— 一次请求触发 N 次 LLM + M 次工具 + RAG + Memory，每一步都可能是凶手。

---

## 1. 传统 APM 不够用的地方

### 1.1 一次请求的实际工作量

```
用户问："我上个月订单为什么没发货"
    ↓
1. Router Agent 判断 → 调用 LLM 1（GPT-4o-mini）
2. 命中 Order Agent → 调用 LLM 2（Claude Sonnet）
3. RAG 检索：向量查询 → 命中 5 个 chunk
4. LLM 2 决定调工具：getOrderHistory
5. 工具执行：查 DB → 返回订单列表
6. LLM 2 决定调工具：getShippingStatus(orderId=...)
7. 工具执行：调物流 API → 返回状态
8. LLM 2 综合生成最终回复
```

**9 步、3 次 LLM 调用、2 次工具调用、1 次向量检索**。

### 1.2 LLM Observability 的新维度

| 维度 | 含义 |
|------|------|
| **Prompt 血缘** | 哪个 prompt 模板版本？渲染后实际是什么？ |
| **Model 血缘** | 哪个 provider / model / temperature？ |
| **Token 统计** | input / output / cached tokens / cost |
| **Tool trace** | 工具名、参数、返回、耗时、错误 |
| **Retrieval trace** | query、命中的 chunk、相似度分数 |
| **质量指标** | faithfulness、relevance、用户反馈 |
| **Drift** | 指标随时间的漂移 |

---

## 2. Spring AI 的内置可观测性

### 2.1 加依赖

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-actuator-boot-starter</artifactId>
</dependency>
<dependency>
    <groupId>io.micrometer</groupId>
    <artifactId>micrometer-tracing-bridge-otel</artifactId>
</dependency>
<dependency>
    <groupId>io.opentelemetry</groupId>
    <artifactId>opentelemetry-exporter-otlp</artifactId>
</dependency>
```

### 2.2 配置

```yaml
spring:
  ai:
    chat:
      client:
        observations:
          include-prompt: true      # 默认 false，开启后能看到完整 prompt
          include-completion: true
management:
  tracing:
    sampling:
      probability: 1.0   # 100% 采样（生产环境 5-10%）
  otlp:
    tracing:
      endpoint: http://otel-collector:4317
```

### 2.3 自动的 Span

Spring AI 自动给这些操作打 span：

- `ChatClient.prompt`
- `ChatModel.call` / `ChatModel.stream`
- `VectorStore.similaritySearch`
- `ChatMemory.add` / `ChatMemory.get`

每个 span 带 attributes：

```
gen_ai.operation.name = "chat"
gen_ai.system = "openai"
gen_ai.request.model = "gpt-4o"
gen_ai.response.model = "gpt-4o-2024-08-06"
gen_ai.usage.prompt_tokens = 234
gen_ai.usage.completion_tokens = 56
gen_ai.usage.total_tokens = 290
gen_ai.prompt = "..."
gen_ai.completion = "..."
```

### 2.4 自定义 Span

```java
@WithSpan("agent.tool.execute")
public ToolResult executeTool(
        @SpanAttribute("tool.name") String name,
        @SpanAttribute("tool.arguments") String args) {
    // ...
}
```

---

## 3. Prompt 血缘

### 3.1 问题

业务方报告："昨天 14:00 之后的回答质量明显下降"。

可能是：Prompt 改了？模型切换了？RAG 检索质量下降？Memory 出问题？

### 3.2 解决：每个 LLM 调用记录血缘

```java
// org.demo02.observability.PromptLineageAdvisor
// 本代码仅作学习材料参考

public class PromptLineageAdvisor implements BaseAdvisor {

    private final LineageRepository lineageRepo;

    @Override
    public ChatClientRequest before(ChatClientRequest req, AdvisorChain chain) {
        LineageRecord record = new LineageRecord(
                UUID.randomUUID().toString(),
                (String) req.context().get("sessionId"),
                Instant.now(),
                promptVersionProvider.current(),
                req.prompt().getSystemMessage().getText(),
                req.prompt().getUserMessage().getText(),
                req.toolNames(),
                null, null, null
        );
        req.context().put("__lineage_id__", record.id());
        lineageRepo.save(record);
        return req;
    }

    @Override
    public ChatClientResponse after(ChatClientResponse resp, AdvisorChain chain) {
        String id = (String) resp.context().get("__lineage_id__");
        ChatResponse chatResp = resp.chatResponse();
        lineageRepo.update(id,
                chatResp.getMetadata().getModel(),
                chatResp.getMetadata().getUsage().getTotalTokens(),
                chatResp.getText());
        return resp;
    }

    @Override public String getName() { return "PromptLineageAdvisor"; }
    @Override public int getOrder() { return Integer.MIN_VALUE + 60; }
}
```

### 3.3 血缘记录示例

```json
{
  "id": "lin-abc123",
  "session_id": "sess-xyz",
  "timestamp": "2026-07-17T10:30:15Z",
  "prompt_version": "v2.1.0",
  "prompt_template_hash": "sha256:...",
  "system_prompt": "你是客服...",
  "user_prompt": "我的订单为什么没发货",
  "tools_available": ["getOrderHistory", "getShippingStatus"],
  "model_used": "claude-sonnet-4-5",
  "total_tokens": 890,
  "response": "您的订单 #12345 当前状态是...",
  "duration_ms": 2300
}
```

---

## 4. 工具与检索血缘

### 4.1 工具调用 AOP

```java
@Aspect
@Component
public class ToolCallTracer {

    @Around("@annotation(tool)")
    public Object trace(ProceedingJoinPoint pjp, Tool tool) throws Throwable {
        Span span = tracer.nextSpan().name("tool." + pjp.getSignature().getName()).start();
        String toolName = pjp.getSignature().getName();
        String args = serialize(pjp.getArgs());
        long start = System.currentTimeMillis();

        try {
            Object result = pjp.proceed();
            long duration = System.currentTimeMillis() - start;

            ToolCallRecord record = new ToolCallRecord(
                    tracer.currentSpan().context().traceId(),
                    toolName, tool.description(),
                    args, serialize(result), duration, "success", null);
            repo.save(record);
            return result;
        } catch (Throwable e) {
            long duration = System.currentTimeMillis() - start;
            repo.save(new ToolCallRecord(..., duration, "error", e.getMessage()));
            span.error(e);
            throw e;
        } finally {
            span.end();
        }
    }
}
```

### 4.2 RAG 检索血缘

```java
public class RetrievalTracingRetriever implements DocumentRetriever {

    private final DocumentRetriever delegate;
    private final RetrievalTraceRepository traceRepo;

    @Override
    public List<Document> retrieve(Query query) {
        List<Document> docs = delegate.retrieve(query);

        RetrievalTrace trace = new RetrievalTrace(
                UUID.randomUUID().toString(),
                sessionId(),
                query.text(),
                docs.stream().map(d -> Map.entry(d.getId(), Map.of(
                        "content_preview", truncate(d.getText(), 100),
                        "score", d.getMetadata().get("score"),
                        "source", d.getMetadata().get("source")
                ))).collect(Collectors.toList()),
                Instant.now()
        );
        traceRepo.save(trace);

        return docs;
    }
}
```

### 4.3 检索质量监控

- **空检索率**：query 没命中任何 chunk 的比例（> 5% 说明知识库有缺口）。
- **低分检索率**：top1 相似度 < 0.5 的比例。
- **chunk 点击率**：哪些 chunk 经常被 LLM 用，哪些从不被引用。

---

## 5. Drift Detection（漂移检测）

### 5.1 什么是 Drift

**指标随时间慢慢恶化但没人发现**：

- faithfulness 从 0.85 慢慢降到 0.78。
- 用户 👎 率从 5% 涨到 8%。
- 平均 token 数从 800 涨到 1200。

这种慢漂移单日看不出，长期累积就是质量崩塌。

### 5.2 检测方法

```java
// org.demo02.observability.DriftDetector
// 本代码仅作学习材料参考

@Component
public class DriftDetector {

    public DriftReport detect() {
        DriftReport report = new DriftReport();
        // 比较：过去 7 天 vs 之前 30 天
        checkMetricDrift(report, "faithfulness", 7, 30, -0.03);
        checkMetricDrift(report, "thumb_up_rate", 7, 30, -0.02);
        checkMetricDrift(report, "p95_latency_ms", 7, 30, 500.0);
        checkMetricDrift(report, "avg_tokens", 7, 30, 100.0);
        return report;
    }

    private void checkMetricDrift(DriftReport report, String metric,
                                    int recentDays, int baselineDays, double threshold) {
        double recent = avg(metric, recentDays);
        double baseline = avg(metric, baselineDays);
        double delta = recent - baseline;
        if (Math.abs(delta) > Math.abs(threshold)) {
            report.addAlert(metric, baseline, recent, delta, threshold);
        }
    }
}
```

### 5.3 Drift 的常见原因

- Prompt 被改坏（无回归测试）。
- 新文档入库污染了知识库。
- 模型供应商悄悄改了模型（确实会发生）。
- 用户 query 分布变化。

---

## 6. 成本治理

### 6.1 成本差异数量级

| 模型 | 输入 ($/1M token) | 输出 ($/1M token) |
|------|-------------------|-------------------|
| Claude Opus 4 | 15 | 75 |
| GPT-4 Turbo | 10 | 30 |
| Claude Sonnet | 3 | 15 |
| DeepSeek-V3 | 0.27 | 1.1 |
| GPT-4o mini | 0.15 | 0.6 |

**结论**：同一问题，不同模型成本可以差 100 倍。

### 6.2 实时算钱

```java
@Component
public class PricingCalculator {

    private static final Map<String, Pricing> PRICING = Map.of(
            "gpt-4o-mini", new Pricing(0.15, 0.60),
            "claude-sonnet-4-5", new Pricing(3.0, 15.0),
            "claude-opus-4", new Pricing(15.0, 75.0),
            "deepseek-v3", new Pricing(0.27, 1.10)
    );

    public BigDecimal compute(String model, int inputTokens, int outputTokens) {
        Pricing p = PRICING.getOrDefault(model, new Pricing(1.0, 2.0));
        return BigDecimal.valueOf(inputTokens)
                .multiply(BigDecimal.valueOf(p.inputPerMillion()))
                .divide(BigDecimal.valueOf(1_000_000), 6, RoundingMode.HALF_UP)
                .add(BigDecimal.valueOf(outputTokens)
                        .multiply(BigDecimal.valueOf(p.outputPerMillion()))
                        .divide(BigDecimal.valueOf(1_000_000), 6, RoundingMode.HALF_UP));
    }

    record Pricing(double inputPerMillion, double outputPerMillion) {}
}
```

### 6.3 配额三层

```java
@Component
public class QuotaService {

    @Transactional
    public boolean tryConsume(String tenant, String user, int estimatedTokens) {
        LocalDate today = LocalDate.now();
        int updated = jdbc.update("""
            UPDATE tenant_quota 
            SET tokens_used = tokens_used + ? 
            WHERE tenant_id = ? AND day = ? AND tokens_used + ? <= tokens_limit
            """, estimatedTokens, tenant, today, estimatedTokens);
        return updated > 0;
    }
}
```

---

## 7. Prompt Cache：省钱的核武器

### 7.1 原理

很多 query 的前缀相同（system prompt + few-shot）。**重复部分缓存**，下次只算新部分。

| Provider | Cache 能力 | 缓存命中价格 |
|---------|-----------|------------|
| Anthropic | Prompt Caching（自动） | 输入价 10% |
| OpenAI | Automatic Prompt Caching | 输入价 50% |
| DeepSeek | Context Caching | 输入价 1% |

**收益**：长期运行的 Agent，system prompt 几千 token，缓存命中可省 50~90%。

### 7.2 提高命中率

```java
ChatClient.builder(anthropicModel)
        .defaultSystem("""
            你是公司助手。公司信息：
            ... (5000 token)
            
            操作规范：
            ... (3000 token)
            """)   // 这部分被缓存
        .build();
```

**关键**：静态内容（system + few-shot）和动态内容（用户输入）严格分离。

---

## 8. 自建 vs 用现成平台

### 8.1 主流 LLM Observability 平台

| 平台 | 类型 | 特点 |
|------|------|------|
| **Langfuse**（开源自托管） | 开源 | 业界标杆 |
| **Phoenix**（Arize） | 开源 | trace + eval 一体 |
| **LangSmith** | 商业 SaaS | LangChain 生态深度 |
| **Datadog LLM** | 商业 | 已用 Datadog 直接加 |

**推荐**：起步阶段直接用 Langfuse（自托管版）。

### 8.2 集成 Langfuse

```java
@Component
public class LangfuseAdvisor implements BaseAdvisor {

    private final LangfuseClient client;

    @Override
    public ChatClientRequest before(ChatClientRequest req, AdvisorChain chain) {
        Trace trace = client.trace(req.context().get("sessionId").toString())
                .name("chat")
                .userId(req.context().get("userId").toString())
                .create();
        req.context().put("__langfuse_trace_id__", trace.getId());
        return req;
    }

    @Override
    public ChatClientResponse after(ChatClientResponse resp, AdvisorChain chain) {
        client.generation(traceId(resp))
                .name("llm")
                .model(model(resp))
                .input(promptText(resp))
                .output(responseText(resp))
                .usage(usage(resp))
                .create();
        return resp;
    }
}
```

---

## 9. Grafana 看板的关键面板

### 9.1 系统层

- QPS / 错误率 / P50/P95 延迟
- 各模型调用量堆叠图（路由分布）
- Token 消耗趋势
- Cost 趋势

### 9.2 质量层

- Faithfulness 时间序列（均值 + P5）
- Relevance 时间序列
- 用户 👍/👎 率
- Drift 检测面板

### 9.3 业务层

- 每日活跃会话数
- 会话长度分布
- 热门 query Top 20
- 失败 query 聚类

---

## 10. 告警分级

| 级别 | 触发条件 | 通知方式 |
|------|---------|---------|
| P0 紧急 | 错误率 > 10%、主 LLM 全挂 | 电话 + 短信 + IM |
| P1 严重 | P95 > 10s、faithfulness < 0.7 | 短信 + IM |
| P2 警告 | Cost 超日预算 80%、Drift 触发 | IM |
| P3 提示 | 异常流量、单租户异常 | IM 群 |

---

## 11. 实战避坑

### 11.1 "采样率太低漏掉了关键 trace"

关键路径（支付、退款、敏感操作）100% 采样；普通路径 5-10%；错误请求强制保留。

### 11.2 "Prompt 内容泄漏到日志"

写日志前 PII 脱敏；敏感字段单独存受限访问的表；日志保留期合规。

### 11.3 "Trace 太深看不懂"

用 OTel 的 span kind；自定义业务标签（`agent.name`、`tool.name`）；Langfuse 自动渲染 trace 树。

### 11.4 "成本超预算"

每日预算硬上限；每用户每月配额；异常流量告警。

### 11.5 "Prompt Cache 命中率低"

每次 prompt 前缀都不一样（把 timestamp、userId 拼在 system prompt 里）。**静态部分保持稳定，不掺入运行时变量**。

### 11.6 "供应商挂了全挂"

多供应商 fallback 链：primary → secondary → tertiary → cached_response。

---

## 12. 实战任务

1. 集成 Spring AI Actuator + OTel，把 trace 上报给 Jaeger。
2. 配置 `include-prompt: true`，在 trace 中看到完整 prompt。
3. 实现 `PromptLineageAdvisor`，记录 prompt 版本 + 内容。
4. 实现 `ToolCallTracer`（AOP），所有工具调用落 trace。
5. 实现 Drift 检测脚本，每天比较近 7 天 vs 之前 30 天。
6. 实现 `PricingCalculator`，按 token 实时算钱。
7. 实现 `QuotaService`，给每个用户每天 10 万 token 配额。
8. 部署 Langfuse（自托管 Docker），把所有 trace 上报。
9. （进阶）搭一套 Grafana 看板，覆盖系统/质量/业务三层。

---

## 13. 理解检查

1. 传统 APM 在 LLM 应用上有什么不足？
2. Prompt 血缘要记录哪些字段？
3. Drift Detection 怎么实现？常见原因？
4. Prompt Cache 的命中条件是什么？怎么提高命中率？
5. 自建 Observability vs 用 Langfuse，怎么选？
6. 关键告警的分级标准是什么？

---

## 14. 相关文档

- [`./11-评估闭环与Prompt版本管理.md`](./11-评估闭环与Prompt版本管理.md) —— 评估与监控的结合
- [`./15-多模型路由与国产化.md`](./15-多模型路由与国产化.md) —— 路由降本
- [`./13-安全工程与红队.md`](./13-安全工程与红队.md) —— Toxicity 监控
- [OpenTelemetry GenAI Semantic Conventions](https://opentelemetry.io/docs/specs/semconv/gen-ai/)
- [Langfuse 文档](https://langfuse.com/docs)

---

回到 [`./00-目录索引.md`](./00-目录索引.md)。
