# 项目 13：事件溯源 Agent 运行时平台 — 07-可观测与成本治理

> **定位**：v6——把可观测升级为「**OTel 指标 + 按业务线 Token 归因**」闭环。Token 归因复用 v1 的事件溯源——**成本数据也是日志的投影**。**设计哲学借鉴**：[附录 14-开源代码深度分析/08-LLM适配-设置与类型系统 §五]、[附录 14-开源代码深度分析/12-Java工程师借鉴手册 §M6 §L4]。本文给出**完整可手写代码**。
> 「遇到阻塞？→ [教程 04-企业级架构主干/02-全链路可观测性]、[教程 04-企业级架构主干/07-成本治理与Token计量]」

---

## 1. 四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ① 全链路指标 ② 按业务线 Token 归因 ③ 预算告警/降级 |
| **影响了哪些模块** | LLM 网关（计量）、会话服务（归因投影）、Control Plane（预算） |
| **架构如何演进** | 观测从「有 Trace」→「OTel 指标 + 归因投影」；成本从「无」→「逐 Token 归因」 |
| **上一版痛点是什么** | ① Token 说不清 ② 无法按业务线归因 ③ 只有 Trace 无指标告警 |

### 1.1 本节核对（四问）

- [ ] 能说出成本数据的来源是「会话日志的投影」（v1 事件溯源复利），而非另建成本表
- [ ] 三个痛点与 §3 组件（TokenMeter/TokenUsageProjector/BudgetService）和 ADR 013-14 对应

## 2. 目标与量化验收

| # | 目标 | 验收 |
|---|------|------|
| 1 | Token 全量计量 | `gen_ai.client.token.*` 指标齐全 |
| 2 | 按业务线归因 | 报表回答「业务线 A 今天多少 Token/多少钱」，误差 ≤ 2% |
| 3 | 全链路指标 | P99/错误率/成功率 接 OTel |
| 4 | 预算告警 | 超 80% 告警、超 100% 自动降级模型 |
| 5 | 观测不改业务 | 计量走 Observation，业务零侵入 |

### 2.1 本节核对（目标与量化验收）

- [ ] 验收 5（观测不改业务）是核心：计量走 Observation（§3.5），业务代码零侵入——能复述"横切关注点用框架级机制而非业务自埋"
- [ ] 预算告警阈值（80% warn / 100% hard-cap 降级）与 §3.4 BudgetService 的 `warnRatio`/`hardCapRatio` 一致

## 3. 完整代码（照抄即可）

### 3.1 `pom.xml` 追加（OTel）

```xml
        <!-- 追加（v6）：可观测 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>
```

### 3.2 `application.yml` 追加（OTel + 指标）

```yaml
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://jaeger:4317
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
```

### 3.3 归因投影：`TokenUsageProjector.java`（成本 = 日志投影）

```java
package com.group.session.meter;

import com.group.session.event.AssistantMessage;
import com.group.session.event.SessionEvent;
import org.springframework.stereotype.Component;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

/**
 * 成本归因 = 会话日志的投影（单写多读：不单独建成本表）。
 * 折叠所有 assistant/message 事件，按 businessLine 聚合 token。
 */
@Component
public class TokenUsageProjector {

    /** 事件 data 里需要携带 usage 与 businessLine（v1 的 AssistantMessage 扩展字段）。 */
    public Map<String, Long> aggregateByBusinessLine(List<SessionEvent> events) {
        Map<String, Long> result = new HashMap<>();
        for (SessionEvent e : events) {
            if (e instanceof AssistantMessage am) {
                long tokens = estimateTokens(am.content());   // 无 usage 时启发式
                String line = am.businessLine() == null ? "unknown" : am.businessLine();
                result.merge(line, tokens, Long::sum);
            }
        }
        return result;
    }

    /** 启发式回退：字符数/4（对应 dsh token-meter 的 CHARS_PER_TOKEN）。 */
    private long estimateTokens(String content) {
        if (content == null) return 0;
        return Math.max(1, content.length() / 4);
    }
}
```

> ⚠️ 需要给 v1 的 `AssistantMessage` record 加一个字段 `businessLine`（v6 的模型调用已带业务线）。改 record 时**更新 v1 的事件定义**，保持 `data JSONB` 兼容（新增字段，旧数据读默认 null）。

### 3.4 预算治理：`BudgetPolicy.java` + `BudgetService.java`

```java
package com.group.control.budget;

/** 预算策略（Control Plane 下发，v7 支持按租户覆盖）。 */
public record BudgetPolicy(String businessLine, long monthlyTokens,
                           double warnRatio, double hardCapRatio,
                           String fallbackModel) {}
```

```java
package com.group.control.budget;

import com.group.session.meter.TokenUsageProjector;
import org.springframework.stereotype.Service;

import java.util.Map;

/** 预算执行：归因投影实时比对，超阈值告警/降级。 */
@Service
public class BudgetService {

    private final TokenUsageProjector projector;

    public BudgetService(TokenUsageProjector projector) {
        this.projector = projector;
    }

    public String check(Map<String, Long> usageByLine, BudgetPolicy policy) {
        long used = usageByLine.getOrDefault(policy.businessLine(), 0L);
        double ratio = (double) used / policy.monthlyTokens();
        if (ratio >= policy.hardCapRatio()) {
            return "HARD_CAP:" + policy.fallbackModel();   // 超 100% → 降级模型
        }
        if (ratio >= policy.warnRatio()) {
            return "WARN";                                  // 超 80% → 告警
        }
        return "OK";
    }
}
```

### 3.5 计量零业务侵入：`LlmObservationConfig.java`（用 Observation，不动业务代码）

```java
package com.group.gateway.meter;

import org.springframework.ai.chat.observation.ChatModelObservationConvention;
import org.springframework.ai.chat.observation.DefaultChatModelObservationConvention;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * 观测零侵入：Spring AI 自动装配 gen_ai.* 语义计量（ChatModelMeterObservationHandler + 默认约定），
 * 业务代码不感知计量（[教程 04-企业级架构主干/02-全链路可观测性]）。
 */
@Configuration
public class LlmObservationConfig {

    /** 如需自定义观测名/标签，继承 DefaultChatModelObservationConvention 覆盖 getName()/getLowCardinalityKeyValues(...)。 */
    @Bean
    public ChatModelObservationConvention chatModelObservationConvention() {
        return new DefaultChatModelObservationConvention();   // 使用框架默认 gen_ai 约定
    }
}
```

> ⚠️ `ChatModelObservationConvention` 的精确接口与默认约定类名，以你引入的 Spring AI 2.0 版本为准（[附录 05-SpringAI2-API基准/02-Tool与Observation真实API §3]）。**语义不变：计量走 Observation，业务零侵入。**

### 3.6 `SessionController` 追加归因报表端点

```java
    // 归因报表：某业务线今日 token（= 日志投影，可重算）
    @GetMapping("/report/token")
    public Map<String, Long> tokenReport(@RequestParam String businessLine) {
        return tokenUsageProjector.aggregateByBusinessLine(eventStore.loadAll(businessLine));
    }

    // 对话入口（v5 拆分后归属 SessionService）：businessLine 由入参透传给 LLM 网关并写入事件 data，
    // 供 §3.3 归因投影按业务线聚合（透传实现见 [06-管控分离微服务 §4.5]；需注入 LlmGatewayClient）
    @PostMapping("/{sessionId}/chat")
    public Mono<String> chat(@PathVariable String sessionId,
                             @RequestParam(required = false) String businessLine,
                             @RequestBody String message) {
        return llmGatewayClient.stream(sessionId,
                businessLine == null ? "unknown" : businessLine, message);
    }
```

> ⚠️ `eventStore.loadAll(businessLine)` 需要追加到 `SessionEventStore`（v6）——按业务线过滤的事件查询（`data` JSONB 里带 `businessLine`）：

```java
public List<SessionEvent> loadAll(String businessLine) {
    return jdbc.sql("SELECT type, data FROM session_event WHERE data->>'businessLine' = :line ORDER BY session_id, seq")
            .param("line", businessLine)
            .query((rs, i) -> deserialize(rs.getString("type"), rs.getString("data")))
            .list();
}
```

### 3.7 本节测试与验证（完整代码：可观测 + 成本归因）

**前置条件**：已按 §3 手写 OTel pom/yml（micrometer-tracing-bridge-otel + otlp exporter）、`TokenUsageProjector`/`BudgetPolicy`/`BudgetService`/`LlmObservationConfig`/`SessionEventStore.loadAll`/§3.6 报表端点；给 v1 `AssistantMessage` record 加 `businessLine` 字段；`management.endpoints.web.exposure.include` 含 `prometheus`；可连 Jaeger（或仅验证 Prometheus 指标端点）。

**材料**：

```bash
# A：发起若干带业务线 businessLine=consumer 的对话（v5 契约 POST /api/session/{id}/chat，写事件含 businessLine）
curl -X POST "http://localhost:8081/api/session/s1/chat?businessLine=consumer" -H "Content-Type: text/plain" -d "订单 ORD-001 金额多少"
# B：归因报表
curl "http://localhost:8081/api/session/report/token?businessLine=consumer"
# C：Prometheus 指标（gen_ai.client.token.usage 是否上报）
curl "http://localhost:8081/actuator/prometheus" | grep gen_ai
```

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 启动（含 OTel） | 启动无缺依赖；`/actuator/health` 通过；Prometheus 端点可访问 `gen_ai.client.token.*`（验收 3 接 OTel） |
| 2 | 材料 A 若干次对话 | 事件 data 含 `businessLine`；模型响应 usage 被（TokenMeter）计量 |
| 3 | 材料 B 归因报表 | 按 businessLine 聚合返回该业务线 token，与人工对账误差 ≤ 2%（验收 2）；报表是日志投影可重算 |
| 4 | 材料 C 指标 | `gen_ai.client.token.usage`（type=input/output、businessLine 标签）齐全（验收 1） |
| 5 | BudgetService 预算（可单测构造） | ratio <80%→OK；≥80%→WARN；≥100%→`HARD_CAP:<fallbackModel>`（验收 4）；计量走 Observation 业务代码未埋表（验收 5） |

**失败排查**：①缺 `gen_ai` 指标→`LlmObservationConfig` 未 @Configuration 或 OTel 依赖未装；②归因报表为空/误差大→`AssistantMessage` 没带 `businessLine` 或 `loadAll` 的 `data->>'businessLine'` 过滤漏键；③预算告警不触发→`BudgetService.check` 的 `monthlyTokens` 阈值/`warnRatio` 传值问题；④观测改到业务→某处直接在 Service 里写 counter 而非走 Observation（违反验收 5）。

## 4. ADR 演进决策

### ADR 013-14：成本归因用日志投影，不单独建成本表
- **Why**：成本可重放、可审计、可重算（[附录 14-开源代码深度分析/12-Java工程师借鉴手册 §M6]）——单写多读

### ADR 013-15：计量走 Observation 不改业务
- **Why**：观测是横切关注点，用框架级机制而非业务自埋（[教程 04-企业级架构主干/02-全链路可观测性]）

### 4.1 本节核对（ADR 013-14 / 013-15）

- [ ] ADR 013-14 能复述"成本归因 = 日志投影"（读侧每请求全 fold 的代价在 v9 由 ADR 013-21 升级为 Outbox + 物化，决策内核不变）
- [ ] ADR 013-15 能指出观测是横切关注点，计量走 Observation 让业务零侵入（本文 §3.5 LlmObservationConfig 落点）

### 4.2 全篇回归（轻量）——复跑 §3.7 三条核心断言

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 重启后重跑 §3.7 材料 A+B | 归因报表仍按 businessLine 聚合，误差 ≤ 2%（投影可重算） |
| 2 | 重跑 §3.7 材料 C | `gen_ai.client.token.*` 指标仍在（OTel 计量不因重启丢失） |
| 3 | Observation 零侵入复核 | 业务代码无自埋 counter 表，计量全走 Observation |

> §2 五条验收已被 §3.7 覆盖，此处回归仅抽核心三条。

## 5. 验收与遗留痛点

**验收**：gen_ai.* 指标齐全、归因误差 ≤ 2%、P99/错误率接面板、预算告警+降级、零业务侵入。

**遗留痛点（供 v7 决策）**：业务线=隐含租户但无真隔离；Prompt/模型全局一份无灰度；单实例无高可用。

### 5.1 本节核对（验收与遗留痛点）

- [ ] 验收五条与 §2、§3.7 一致；三个遗留痛点（租户假隔离/无灰度/单实例）正是 [08-多租户灰度高可用] 的 v7 主攻
- [ ] 能指出"业务线=隐含租户"这一表述如何在 v7 升级为真租户隔离（ADR 013-16）

## 6. 验收对照

| 验收项 | 标准 | 状态 |
|--------|------|------|
| Token 全量计量 | `gen_ai.client.token.*` 指标齐全（§3.7 步骤 4） | ☐ |
| 按业务线归因 | 报表回答「业务线 A 今天多少 Token/多少钱」，误差 ≤ 2%（§3.7 步骤 3） | ☐ |
| 全链路指标 | P99/错误率/成功率 接 OTel（§3.7 步骤 1） | ☐ |
| 预算告警 | 超 80% 告警、超 100% 自动降级模型（§3.7 步骤 5） | ☐ |
| 观测不改业务 | 计量走 Observation，业务零侵入（§3.7 步骤 5） | ☐ |

> **定位回顾**：v6 完成可观测与成本治理（里程碑 2/6）。下一站 [08-多租户灰度高可用]。
