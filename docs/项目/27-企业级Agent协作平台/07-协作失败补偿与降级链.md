# 07-协作失败补偿与降级链

> **定位**：让协作失败**不中断业务**——按失败类型走**确定性降级链**。核心：**① 失败分类（协商拒/超时/降级/结果拒收）② 降级链（重试→换 Agent→本地代理→人工）③ 补偿动作（对已产生副作用的逆操作）**。呼应 [教程 54 §5](../../教程/54-Agent间协作协议工程化.md)、[30-容错](../../教程/30-容错与弹性设计.md)。前置阅读：[06-注入边界与结果校验](06-注入边界与结果校验.md)。
>
> **铁律 0**：降级链/补偿自研「概念代码」；重试用 Reactor `retryWhen`（附录06 实证体系）。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①失败分类路由 ②降级链（按类型选策略）③补偿动作注册（对可逆副作用执行逆操作） |
| **影响了哪些模块** | CollabMediator → 包降级器；新增 Compensator |
| **架构如何演进** | 从"失败即报错"演进为"失败分类 → 分级降级 → 可补偿" |
| **上一版痛点** | 协商拒/超时/拒收都直接抛错，委托方业务中断；已触发副作用无人管 |

**本迭代验收**：①各失败类型走对降级路径 ②同能力可换 Agent 重委托 ③已触发副作用的补偿动作可执行/可登记。

## 二、失败分类 → 降级链

```java
// 概念代码：失败降级链（按类型确定性路由）
public Mono<DelegationResult> resilientDelegate(DelegationRequest req) {
    return doDelegate(req)
        .retryWhen(Retry.backoff(2, Duration.ofSeconds(2))     // 瞬时故障:指数退避重试
            .filter(this::isTransient))                          // 只重试瞬时(网络抖动)
        .onErrorResume(e -> switch (failureType(e)) {
            case NEGOTIATION_REJECTED, CAPABILITY_MISS ->
                tryAlternativeAgent(req);                        // 协商拒/能力不匹配→换Agent
            case TIMEOUT -> queryThenFallback(req);              // 超时→先查状态再降级(04)
            case RESULT_REJECTED -> reDelegateWithHint(req);     // 结果拒收→带拒收原因重委托
            case DEGRADED -> localFallback(req);                 // 对方降级→本地兜底模式
            default -> escalateToHuman(req, e);                  // 兜底:转人工(HITL)
        });
}
```

**链序**：瞬时重试 → 换 Agent → 带因重委托 → 本地兜底 → 人工。**前面的便宜，后面的贵**——和 [23-兜底降级链](../../项目/23-企业级Agent意图路由网关/07-兜底降级链与HITL闸门.md)、[32-模型降级](../../教程/32-模型路由与降级.md) 一脉的方法论。

## 三、补偿动作（对已发生的副作用）

```java
// 概念代码：副作用补偿注册表
public interface Compensable { Mono<Void> compensate(DelegationTask t); }

// 例：委托"群发通知"后取消——已发的消息不能撤回，但可发更正；委托"创建报表"取消——删除半成品
compensator.register("notify.broadcast", t -> sendCorrection(t));   // 逆操作
compensator.register("report.create",   t -> cleanupDraft(t));      // 清理
compensator.register("payment.*",       t -> escalateToHuman(t));   // 不可逆→人工
```

**纪律**：**不可逆副作用（支付/外发）不允许自动补偿**——升人工裁决（呼应 [28-HITL](../../教程/28-Human-in-the-Loop与审批流.md)）；可逆的登记逆操作自动执行。

## 四、验收

| 测试 | 期望 |
|------|------|
| 网络抖动 | 退避重试成功 |
| 协商拒绝 | 自动换同能力 Agent |
| 结果拒收 | 带原因重委托 |
| 全链失败 | 转人工不中断报错 |
| 取消含副作用任务 | 补偿动作按注册执行 |

> **下一步**：主体迭代（01-07）完成——协作平台已能发现/委托/安全/可控/降级。08 起进入进阶：**协作全链路可观测**。
