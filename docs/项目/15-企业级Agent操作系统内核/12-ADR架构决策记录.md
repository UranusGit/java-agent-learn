# 12-ADR 架构决策记录：Agent OS 内核决策资产

> **定位**：汇齐内核全部架构决策（上下文/备选≥2/取舍/可回滚）。改内核前先查这里。前置阅读：[00-需求分析与架构设计](00-需求分析与架构设计.md)、[附录 08-架构决策方法论]。

---

## 一、ADR 总览

| # | 决策 | 落地篇 |
|---|------|--------|
| ADR 15-00 | OS 隐喻作为组织骨架（六机制） | 00 |
| ADR 15-01 | 内核态=管控面，进程只经 syscall 触资源 | 00/03 |
| ADR 15-02 | Agent 进程承载于虚拟线程 | 01 |
| ADR 15-03 | 进程挂起=快照外化+线程退出（非睡眠等待） | 02 |
| ADR 15-04 | 父子进程+init 收养（复用 OS 亲缘语义） | 02 |
| ADR 15-05 | 工具执行上收内核：内核代理 ToolCallback | 03 |
| ADR 15-06 | 记忆三层挂载（session/longterm/shared 只读） | 04 |
| ADR 15-07 | 配额三维度：Token/并发/记忆（cgroup 式组账） | 05 |
| ADR 15-08 | 抢占只在排队边界（不撕已计费调用） | 05 |
| ADR 15-09 | 信号在 syscall 边界同步处理 | 06 |
| ADR 15-10 | SIG_APPROVE：HITL 挂起零占用 | 06 |
| ADR 15-11 | IPC 三形态 + 四类命名空间 | 07 |

---

## 二、关键详解

### ADR 15-00：OS 隐喻作为组织骨架

- **上下文**：Agent 数量爆炸后，平台需要组织"多进程共享资源"的复杂度。
- **备选**：①业务化组织（按客服/风控等场景分包）②OS 隐喻组织。
- **决策**：②——OS 是该问题域唯一被 40 年验证的成熟答案；隐喻同时给出沟通语言（"挂起""信号""配额"团队秒懂）。
- **取舍**：隐喻有过度套用风险（如强行做"内核锁"）——纪律：**映射到机制才用，只借名词不用**。
- **可回滚**：模块边界真实，去隐喻重命名不影响结构。

### ADR 15-03：挂起=快照+线程退出

- **上下文**：HITL 审批可能 24h 后回来，长任务可休眠数天。
- **备选**：①线程睡眠等待 ②状态外化（快照）+线程退出。
- **决策**：②——睡眠占线程/内存/配额；外化后零占用，唤醒=新线程续跑。
- **取舍**：快照有序列化成本（ms 级）与版本兼容负担（进程定义演进需兼容旧快照）。
- **可回滚**：短期等待（<5min）可退化线程内 park（权衡点可配）。

### ADR 15-08：抢占只在排队边界

- **上下文**：RT 进程（风控）需 100ms 级响应，但模型调用发出即计费。
- **备选**：①取消在途 HTTP 调用 ②只抢占排队位。
- **决策**：②——撕已发出调用=白付钱+上游状态不确定；排队位插队零成本即达标。
- **可回滚**：极端场景（灾难响应）可开"硬取消"开关（自担成本）。

### ADR 15-10：SIG_APPROVE 挂起零占用

- **上下文**：高危工具需人工审批；等待期不能占资源。
- **决策**：WOULD_BLOCK 返回 + BLOCKED 状态 + SIG_APPROVE 唤醒（与 ADR 15-03 同构）。
- **取舍**：进程需理解 WOULD_BLOCK 重发语义（编排循环多一个分支）——由内核 SDK 封装对业务透明。

---

## 三、使用纪律

1. 新决策先登记（四要素）再动码；2. 安全类（15-01/15-05/15-11）改动双人评审；3. 推翻不删除——标 superseded。

**测试**：11 条四要素齐备；抽 ADR 15-03/15-08 做回滚演练。

**下一篇**：[13-工业前沿与演进方向](13-工业前沿与演进方向.md)。
