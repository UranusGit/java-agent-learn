# 08-ADR 架构决策记录：MCP 工具网关决策资产

> **定位**：把项目 03 从 00 到 07 的架构决策汇集成体系 ADR——每条含**决策上下文 / 备选方案 / 取舍理由 / 可回滚性**。这是网关项目的"决策 DNA"：改架构前先来这里查"当时为什么这么定"。读者画像：接手/重构网关的读者。前置阅读：[00-需求分析与架构设计](00-需求分析与架构设计.md)（其 §10 已有 ADR 03-00 起步）、[附录 08-架构决策方法论]。
>
> **铁律 0**：涉及 MCP API 的决策均与基准 §11 一致。

---

## 一、ADR 总览

| # | 决策 | 落地篇 | 状态 |
|---|------|--------|------|
| ADR 03-00 | 双重身份架构（对内 MCP Server + 对外 MCP Client） | 00 | ✅ 采纳 |
| ADR 03-01 | 工具准入 + 签名校验（描述指纹冻结） | 00/01 | ✅ 采纳 |
| ADR 03-02 | 连接池 + 故障转移（多 Server 管理） | 01 | ✅ 采纳 |
| ADR 03-03 | 审查通过版描述下发（防实时自述污染） | 01 | ✅ 采纳 |
| ADR 03-04 | 自定义 MCP 服务端走 starter-webflux | 02 | ✅ 采纳 |
| ADR 03-05 | 传输统一 Streamable HTTP（废弃 HTTP+SSE） | 05 | ✅ 采纳 |
| ADR 03-06 | OAuth 2.1 三重身份（资源服务器/客户端/token 交换） | 05 | ✅ 采纳 |
| ADR 03-07 | 市场计费三模式（PER_CALL/PER_SEAT/PACKAGE） | 06 | ✅ 采纳 |
| ADR 03-08 | 评分阈值自动下架 + 安全事故直下 | 06 | ✅ 采纳 |
| ADR 03-09 | 联邦最终一致目录（30s）而非强一致 | 07 | ✅ 采纳 |
| ADR 03-10 | 联邦 token 最小授权（30s TTL 单工具单次） | 07 | ✅ 采纳 |
| ADR 03-11 | 就近路由本地优先（非全局最优） | 07 | ✅ 采纳 |

### 1.1 本节核对（总览完整性）

- [ ] 12 行 ADR 均有"落地篇"且该篇真实存在（00–07），无孤儿决策
- [ ] 每条决策一句话能概括，且与对应落地篇正文中的 ADR 条目（编号可能按各篇内序）语义一致
- [ ] 状态列无空值；无已被 superseded 却仍标"采纳"的条目

---

## 二、关键 ADR 详解

### ADR 03-00：双重身份架构

- **上下文**：网关既要向 Agent 暴露统一工具入口，又要连接真实工具（各类 MCP Server）。
- **备选**：①纯代理（透传 MCP，无自有语义）②双重身份（对内 Server/对外 Client）③只做 Server（工具全部内联）。
- **决策**：②——"不用 MCP 连不上任何工具"成为结构性事实（协议收口），同时网关可在中间层做准入/审计/授权。
- **取舍**：③最简单但工具无法服务化；①无法插入治理。②复杂度最高但治理能力完整。
- **可回滚**：双面各自独立，任一面可替换实现。

### ADR 03-03：审查通过版描述下发

- **上下文**：LLM 依据工具 description 决策调用；描述被恶意/无意篡改会直接操纵 Agent 行为。
- **备选**：①透传实时自述 ②冻结准入时描述。
- **决策**：②——准入时指纹冻结，Agent 看到的永远是审查版；描述漂移即告警（防 Tool Poisoning）。
- **取舍**：①总有最新信息但不可信；②牺牲实时性换安全。版本更新走重新准入。
- **可回滚**：指纹表可重建（重放准入记录）。

### ADR 03-05：传输统一 Streamable HTTP

- **上下文**：旧 HTTP+SSE 双端点已被 2025-03+ 规范废弃；Streamable HTTP 单端点是现行标准。
- **备选**：①维持旧传输 ②全量 Streamable HTTP ③stdio（本地场景）。
- **决策**：②（远程）+③（本地工具保留）——配置键 `spring.ai.mcp.client.streamable-http.connections`（javap 实证）。
- **取舍**：①技术债；②需下游配合升级（不支持的 Server 暂时经适配层桥接）。
- **可回滚**：连接配置按 Server 粒度切换。

### ADR 03-10：联邦 token 最小授权

- **上下文**：跨集群转发需要凭证，但长期宽权限 token 一旦泄露危害大。
- **备选**：①集群间长期服务账号 ②短 TTL 最小授权委托。
- **决策**：②——30s TTL、单工具、单次调用、禁止再委托。
- **取舍**：①实现简单但违背最小权限；②多了签发开销但爆炸半径极小。
- **可回滚**：签发开关可关（退化为无跨区能力，不影响本地）。

### ADR 03-09：联邦目录最终一致

- **上下文**：目录跨集群同步，强一致代价高。
- **备选**：①强一致（同步锁/共识）②最终一致（30s 增量）。
- **决策**：②——工具登记是低频操作，30s 不可见窗口可接受；冲突走消解规则（PUBLISHED 优先/高版本胜/指纹一致双活/否则人工）。
- **取舍**：①对低频写是过度设计。
- **可回滚**：同步可暂停（各集群自治运行）。

### 2.1 本节核对（详解四要素）

- [ ] 五条详解各含四要素（上下文/备选≥2/取舍/可回滚），无缺项
- [ ] ADR 03-05 的配置键 `spring.ai.mcp.client.streamable-http.connections` 与 05 篇 §2.2 实证键结构一致
- [ ] ADR 03-10 的 30s TTL/单工具/单次与 07 篇 §五 纪律及 §5.1 验证一致
- [ ] 每条详解可在"落地篇"反查到实现代码位置

---

## 三、ADR 使用纪律

1. 新决策先登记（上下文/备选≥2/取舍/可回滚），再动代码
2. 推翻旧 ADR 不删除——标注 superseded 并注明推翻原因
3. 每条 ADR 标注落地篇，可反查实现
4. 安全类决策（03-01/03-03/03-10）改动需双人评审

### 3.1 本节核对（纪律可执行性）

- [ ] 第 4 条列出的安全类 ADR 编号（03-01/03-03/03-10）在 §一 总览中真实存在
- [ ] "superseded 不删除"机制有存放位置（本表状态列或详解标注）

---

## 四、全篇回归验证（ADR 资产完整性）

**材料**——原「测试与验证」的自检项（保留为本节主体）：

```java
// ADR 完整性自检：11 条 ADR 四要素齐全；代码注释中的决策能对应到 ADR 编号
// 抽 3 条做回滚演练：03-05（切回旧传输）/03-09（暂停同步）/03-10（关跨区）
```

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 逐条核对总览 12 条 | 四要素齐全，无缺项（对应材料自检第一句） |
| 2 | grep 代码注释中的"ADR" | 每处决策注释能映射到总览编号 |
| 3 | 回滚演练 03-09：暂停同步开关 | 各集群仍自治可调本地工具（可回滚性真实有效） |
| 4 | 回滚演练 03-10：关跨区签发 | 本地调用不受影响，跨区调用被明确拒绝而非失败 5xx |

**失败排查**：①要素缺失→按 §三 纪律 1 补登记再动代码；②回滚演练伤及主链路→"可回滚"表述与实现不符，修正 ADR 或补开关。

**下一篇**：[09-工业前沿与演进方向](09-工业前沿与演进方向.md)——2026 前沿校准与收官。
