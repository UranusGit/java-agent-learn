# 11-核心代码讲解

> **定位**：把项目 26 的核心实现串成主线——以**一条"退货政策问答答错"差例从发现到修复上线**贯穿各迭代代码的协作。前置阅读：[01-最小Demo](01-最小Demo.md) 至 [10-飞轮加速与主动采集](10-飞轮加速与主动采集.md) 全部。
>
> **铁律 0**：Observation/`entity()`/`EmbeddingModel` 实证基座；飞轮管道为「概念代码」。

---

## 一、一条差例的飞轮全旅程

```mermaid
sequenceDiagram
    participant U as 用户
    participant S as 哨兵(04)
    participant C as 采集(02/07)
    participant L as 血缘(03)
    participant D as 归因(05)
    participant G as 门禁(04)
    participant R as 灰度(06)

    U->>S: "退货要几天?"答错
    S->>C: 低分样本回流(在线哨兵)
    C->>C: 免疫(07): 来源可信+交叉验证
    C->>L: 入湖(血缘: kb=v3/prompt=v8)
    L->>D: 归因: KB_GAP(政策文档未收录)
    D->>D: 修复建议: 补知识(文档草稿,人审)
    D->>G: 改进项过双卡门禁
    G->>R: 灰度10%→50%→100%
    R-->>U: 新版答对; 新差例继续回流
```

## 二、飞轮主循环（串联各迭代）

```java
// 概念代码：飞轮驱动器
@Component
public class FlywheelDriver {
    public Flux<ReleaseEvent> drive(String bizLine) {
        return collect.probeAndSnoop(bizLine)                   // 02+10 采集(含主动探针)
            .map(immune::screen)                                  // 07 免疫(带权重)
            .doOnNext(lake::saveWithLineage)                      // 03 血缘入湖
            .transform(diagnoser::prioritizeAndSuggest)           // 05 归因+优先级
            .concatMap(item -> gate.gate(item)                    // 04 双卡门禁
                .filter(GateVerdict::passed)
                .flatMap(v -> release.rollout(item)));            // 06 灰度阶梯
    }   // 09 健康度全程打点
}
```

## 三、分层职责（读代码抓手）

| 类 | 迭代 | 职责 |
|----|------|------|
| `CollectPipeline`/`Immunizer` | 02/07/10 | 多源采集+免疫+探针 |
| `LineageStore` | 03 | 五层血缘坐标 |
| `EvalPipeline`/`GateKeeper`/`SentryHub` | 04 | 门禁+哨兵回流 |
| `Diagnoser`/`FixSuggester` | 05 | 归因分型+定向修复 |
| `ReleaseOrchestrator` | 06 | 灰度阶梯+回滚 |
| `Federation`/`FlywheelHealthJob` | 08/09 | 联邦+健康度 |

## 四、最容易踩的三个坑

1. **无血缘的飞轮** → 差例无法归因到层，改进盲打。**解法**：血缘强制（03）。
2. **免疫缺位** → 恶意反馈带偏优化。**解法**：入口免疫（07）。
3. **无灰度守门的改进** → 上线=赌博。**解法**：门禁+阶梯强制（04/06）。

## 五、验收自测（对照 00 量化指标）

| 指标 | 目标 | 实现 |
|------|------|------|
| 差例→上线转化率 | ≥30% | 05+06 |
| 周期 P50 | ≤7天 | 09 度量+10 加速 |
| 回归率 | ≤5% | 04 门禁+06 灰度 |
| 污染检出 | ≥95% | 07 |
| 血缘覆盖 | 100% | 03 |

> **下一步**：代码串讲完成。12-ADR 记录 8 条关键决策；13-前沿做数据飞轮业界对照与演进收官。
