# 07-防污染与标注免疫

> **定位**：飞轮的**免疫系统**——防恶意反馈/标注投毒/红队样本后门。核心：**① 来源可信分级加权 ② 标注交叉验证（LLM 判 + 抽样人审不一致监控）③ 异常冻结（来源差评率突变→冻结待查）**。呼应 [教程 53 §3](../../教程/53-企业级数据飞轮闭环.md)、[19-金标防污染](../../项目/19-企业级Agent信任与评测平台/02-金标资产化.md)。前置阅读：[06-灰度联动与守门放量](06-灰度联动与守门放量.md)。
>
> **铁律 0**：免疫规则自研「概念代码」（统计检测确定性）；抽样人审流程化。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①来源分级加权（AUTH/ANON/EXTERNAL 权重与限额）②标注交叉验证（双判不一致率监控）③来源异常检测（差评率突变冻结） |
| **影响了哪些模块** | 采集 → 免疫前置于入湖；评估 → 权重生效 |
| **架构如何演进** | 飞轮从"来者不拒"演进为"有免疫的摄入" |
| **上一版痛点** | 竞品恶意差评可带偏优化方向；污染标注可让评估失真 |

**本迭代验收**：①低信源权重生效（同量恶意差评影响 ≤ 高信源 1/10）②标注不一致率超阈告警 ③突变来源自动冻结 ④污染检出率 ≥95%（00 验收④）。

## 二、三层免疫

```java
// 概念代码：免疫管线
public IngestResult immune(RawItem raw) {
    // ①来源分级: 权重与配额(匿名低权重+限额,外部最低)
    double w = trustWeight(raw.source());
    if (quotaExceeded(raw.source())) return IngestResult.rateLimited();

    // ②交叉验证: LLM判 vs 人审抽样 不一致率监控
    if (annotator.disagreementRate(raw.source()) > DISAGREE_MAX)
        alert(AnnotationSuspicious.of(raw.source()));

    // ③异常冻结: 该来源差评率 z-score 突变 → 冻结待查
    if (stats.zscoreOfNegative(raw.source()) > FREEZE_Z) {
        sources.freeze(raw.source());                     // 冻结期内不进优化环
        return IngestResult.frozenPendingReview();
    }
    return IngestResult.accepted(w);                      // 带权重入湖
}
```

## 三、为什么免疫在采集而不在评估

污染数据一旦入湖进评估，**事后清洗成本远高于入口拦截**——免疫必须在摄入管线最前端（纵深防御的入口层，呼应 [08-供应链准入](../../项目/08-Agent供应链安全网关/02-工具准入与签名校验.md) 的准入思想）。

## 四、验收

| 测试 | 期望 |
|------|------|
| 匿名差评洪水 | 权重低+限额，影响可控 |
| 标注不一致飙升 | 告警 |
| 来源差评率突变 | 冻结待查 |
| 恶意差评攻击 | 优化方向不被带偏 |

> **下一步**：单业务飞轮有免疫了。08 进阶做**多业务联邦**——自治+脱敏共享，企业级收束。
