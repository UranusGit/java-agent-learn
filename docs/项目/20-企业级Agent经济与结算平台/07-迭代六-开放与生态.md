# 07-迭代六：开放与生态——商店分润 / 多币种 / 争议仲裁

> **定位**：把平台变成组织间的经济接口：**Agent 商店结算**（本企业 Agent 能力上架，按调用量分润）、**多币种与汇率快照**（跨区交易按成交时刻汇率入账）、**SLA 折扣与争议仲裁**（服务不达标自动折价、交易争议三级仲裁流程）。读者画像：要回答"能力怎么卖出去、买亏了怎么办"的读者。前置阅读：[06-迭代五-审计与反欺诈](06-迭代五-审计与反欺诈.md)、[教程 49-Agent经济与支付集成]。
>
> **铁律 0**：商店/分润/仲裁自研「概念代码」；汇率源外接行情 API（需引入依赖后实证）。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ① 商店结算：Agent 能力/MCP 工具上架，按调用量计费与分润（作者/平台/渠道三向比例）② 多币种：计量多币种归集、汇率**快照入账**（成交时刻汇率写死进事件，月末不因汇率翻旧账）③ SLA 折扣：可用性/延迟不达标按档自动折价（先约定后执行，账单自动体现）④ 争议仲裁三级：自动证据比对（双方日志+计量）→ 平台裁决 → 人工委员会；仲裁期间款项冻结 |
| **影响了哪些模块** | 新增 `market-svc`；账本事件加币种+汇率快照字段；分润沿 05 分账引擎扩展 |
| **架构如何演进** | 企业内账本 → 生态结算协议 |
| **上一版痛点** | 只会记账不会做生意：能力无法商品化变现、跨币种对账口径混乱、买亏了没有救济通道（06 §六） |

**本迭代验收**：① 分润三方到账与总账平（∑分润=总收入）② 汇率快照入账：月末重算账单金额不变 ③ SLA 折扣自动执行（注入超时 → 账单打折并留证）④ 争议三级流程可走通、期间款项冻结不可动。

---

## 二、商店分润（一次调用的钱怎么分）

```mermaid
sequenceDiagram
    participant B as 买方Agent
    participant MKT as market-svc
    participant LED as 账本(04)
    participant A as 能力作者
    B->>MKT: 调用上架能力(带委托凭证)
    MKT->>MKT: 授权闸(02)+预授权
    MKT->>LED: CAPTURE ¥1.0(币种+汇率快照)
    LED->>LED: 分润事件:作者0.7/平台0.2/渠道0.1
    Note over LED: ∑分润 == 收入(硬不变式)
    LED-->>A: 结算周期到账
```

**上架门槛**：能力必须挂评估凭证（外部信任分）——生态里"敢卖"与"敢买"对称：卖方过质量闸，买方过授权闸。第三方信任凭证的互认协议属外部依赖（本平台预留接口，见 13-前沿生态方向讨论）。

## 三、汇率快照纪律

- 入账事件携带 `(amount, currency, rate, rateAt)` 四元组，**报表层永远用快照汇率**——"月末按最新汇率重算"是财务对账的头号灾难源。
- 本位币唯一：内部总账只有一种本位币，外币交易全部在事件层换算入账。

## 四、争议仲裁三级

```mermaid
flowchart TB
    D["买方发起争议"]
    L1{"一级:自动证据比对<br/>双方日志+计量事件(03/06)"}
    L1 -->|"证据清晰"| A1["自动裁决<br/>(如:SLA 未达→06 折扣规则)"]
    L1 -->|"证据冲突"| L2["二级:平台裁决<br/>(双方举证,5工作日)"]
    L2 -->|"任方不服"| L3["三级:人工委员会<br/>(终裁)"]
    A1 & L2 & L3 --> SETT["执行:退款/折价/驳回"]
    D --> FRZ["争议款项冻结<br/>(仲裁期间不可动)"]
    style L1 fill:#fff9c4
    style FRZ fill:#ffcdd2
```

## 五、测试与验证

```bash
# 1. 分润：100 次调用 → 三方分账之和 == 总收入（逐笔+汇总）
# 2. 汇率：跨币种交易后行情波动 → 月末账单按快照不变
# 3. SLA：注入能力超时 → 自动折扣入账+证据留档
# 4. 仲裁：构造证据冲突争议 → 冻结→二级→执行退款全链
```

## 六、本迭代痛点

生态能跑了，但都是"事后账"——预算永远月超月补。→ 08 预算预测与成本治理（事前）。

## 七、验收对照

| 验收项 | 标准 | 状态 |
|--------|------|------|
| 分润平衡 | ∑分润=收入 | ✅ |
| 汇率快照 | 月末不翻账 | ✅ |
| SLA 折扣 | 自动+留证 | ✅ |
| 仲裁闭环 | 冻结→裁决→执行 | ✅ |

**下一篇**：[08-进阶一-预算预测与成本治理](08-进阶一-预算预测与成本治理.md)。
