# 14-核心代码讲解：一笔钱的完整旅程

> **定位**：复盘平台关键实现，并以**"委托人授权 ¥50，Agent 完成一次多步付费任务"的完整旅程**串起六迭代+三进阶。前置阅读：[02-授权三闸](02-授权三闸.md) 至 [10-价值对齐与防套利](10-价值对齐与防套利.md)。

---

## 一、机制速查

```mermaid
graph LR
    A["授权三闸(02)<br/>额度/单笔/风控+凭证"]
    B["计量计费(03)<br/>三源/价格版本"]
    C["支付幂等(04)<br/>预授权/账本/恢复"]
    D["多Agent分账(05)<br/>归因/押金/追缴"]
    E["审计反欺诈(06)<br/>hash链/检测/发票"]
    F["生态(07)<br/>分润/汇率/仲裁"]
    A---B---C---D---E---F
    style C fill:#e3f2fd
```

### 一.1 本节核对（机制速查六模块齐全）

| # | 核对项 | 判据 |
|---|--------|------|
| 1 | 六模块齐全 | 授权(02)/计量(03)/支付(04)/分账(05)/审计(06)/生态(07) 六节点与 §三 复盘行一一对应 |
| 2 | 落点可回查 | 每模块标注迭代篇号（02-07 全有），与目录既有篇名一致，无悬空模块 |

## 二、完整旅程（¥50 委托从授权到对账）

```mermaid
sequenceDiagram
    participant H as 委托人(人)
    participant AU as authz(02)
    participant AG as Agent
    participant ME as metering(03)
    participant LE as ledger(04)
    participant FR as fraud(06/10)
    H->>AU: 委托:查行情+写报告 预算¥50
    AU->>AU: 签发凭证:scope=2工具/单笔¥10/24h
    AG->>AU: 调付费行情工具(带凭证)
    AU->>AU: 三闸全过→预授权 ¥8
    AU->>ME: 计量事件(回执量)
    ME->>LE: CAPTURE ¥7.2(按回执)
    LE->>FR: 事件入链(hash)
    AG->>AG: 调免费工具完成报告
    LE-->>H: 任务完成:实花 ¥7.2<br/>明细:行情×1 余额¥42.8回收
    FR->>FR: 夜巡:链完整/无刷量环<br/>月度:账单+发票映射(06)
    Note over LE,FR: 账实相符 ✅
```

**一句话**：每一分钱先有凭证（02）、后有计量（03）、落进不可改的账本（04/06）、多 Agent 也分得清（05）、生态交易按快照结算有仲裁（07）、预算超之前就有人知道（08）、跨境不踩线（09）、倒卖刷量逃不掉（10）。

### 二.1 本节核对（¥50 完整旅程逐跳口径）

- [ ] 逐跳可回溯：凭证签发（02）→ 三闸预授权（02）→ 计量（03）→ CAPTURE 入账（04）→ hash 入链（06/10），每一步都可在前文章节找到对应实现与验证
- [ ] 金额闭环：授权 ¥50 → 实花 ¥7.2 → 余额 ¥42.8 回收，与预授权/幂等（04）分段口径一致，账实相符
- [ ] 夜巡与月度（06）覆盖链完整性与刷量环，非本节自叙、可回查 06/10 验证

## 三、关键设计复盘

| # | 设计 | 为什么 |
|---|------|--------|
| 1 | 先扣后花+原子扣减 | 花后记账必超支（01） |
| 2 | 三闸分层 | 各拦一类事故：累积/单次/盗用（02） |
| 3 | 计量以回执为准 | 预估只配预扣，账才对得平（03） |
| 4 | 预授权+幂等键分段 | Agent 高重试环境下唯一可靠扣款法（04） |
| 5 | 账本=事件重放投影 | 余额是事实的函数，不是可改的字段（04/06） |
| 6 | 子凭证只缩不放 | 委托链的权限单调递减不变式（05） |
| 7 | hash 链+外部锚点 | 改账必断链，成本远低于链式记账（06） |
| 8 | 不自持资金 | 记账+指令角色避开牌照高墙（09） |
| 9 | 聚合层套利检测 | 微观合规宏观异化，闸门层看不见（10） |

### 三.1 本节核对（九条设计复盘有据）

| # | 核对项 | 判据 |
|---|--------|------|
| 1 | 九条复盘各有出处 | 第 1~9 条分别标注迭代篇号（01-10），每条"为什么"理由与该迭代正文一致，无新发明论据 |
| 2 | 与 §一/§二对应 | 九条是"机制速查六模块"与"¥50 旅程"的设计原理层，三者口径不冲突 |

## 四、全篇回归验证

**回归断言**（§一.1 六模块、§二.1 旅程逐跳、§三.1 九条复盘均通过后，最终整体验收）：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 按 §二 旅程逐跳断言（复跑 01-10 各迭代既有用例） | 授权→计量→入账→审计 各跳 PASS，形成一条完整链路 |
| 2 | 全链演练（授权→消费→崩溃恢复→对账→审计） | 季度例行演练走通，账实相符结论一致 |
| 3 | 本迭代验收对照（与 01-10 逐篇验收口径一致） | 全篇复盘引用的验收均可回查原迭代，无悬空 PASS |

**失败排查**：旅程某跳 FAIL→回查对应迭代篇的章节验证（如 CAPTURE 入账回查 [04-支付轨道与幂等]），非在总结篇修补。

**下一篇**：[15-ADR架构决策记录](15-ADR架构决策记录.md)。
