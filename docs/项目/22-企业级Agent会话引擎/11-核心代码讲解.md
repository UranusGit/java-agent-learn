# 11-核心代码讲解：一条消息的引擎之旅

> **定位**：复盘引擎关键实现，并以**"用户插话+工具执行+打断+恢复"的一条完整消息旅程**串起六迭代+三进阶。前置阅读：[02-Actor核与任务生命周期](02-Actor核与任务生命周期.md) 至 [10-投影层与多前端](10-投影层与多前端.md)。

---

## 一、机制速查

```mermaid
graph LR
    A["Actor核(02)<br/>单写者/双通道/三段取消"]
    B["工具编排(03)<br/>失败回填/并行门"]
    C["审批缓存(04)<br/>三态/key/升级"]
    D["上下文压缩(05)<br/>版本号/三路径"]
    E["持久化恢复(06)<br/>JSONL/快照/重放"]
    F["交互语义(07)<br/>steering/裁决/唤醒"]
    A---B---C---D---E---F
    style A fill:#e3f2fd
```

### 一.1 本节核对（机制速查）

- [ ] 六个节点（02 Actor 核 / 03 工具编排 / 04 审批 / 05 压缩 / 06 持久化 / 07 交互）与对应迭代篇一一挂钩
- [ ] 每个节点的一句话摘要能说出来（如 06=JSONL/快照/重放），与 §二 旅程中的落点一致

## 二、完整旅程（插话→工具→打断→恢复）

```mermaid
sequenceDiagram
    participant FE as 前端(投影层)
    participant A as Actor(01/02)
    participant T as DialogTask
    participant O as 工具编排(03/04)
    participant CTX as 上下文(05)
    participant LOG as 事件日志(06)
    FE->>A: Op.UserInput("重构这段代码")
    A->>T: start(任务化)
    T->>CTX: token 检查→正常
    T->>O: 工具调用 read_file(已批key,免问)
    O-->>T: 回填(失败也回填)
    FE->>A: Op.UserInput("顺便加注释")<br/>(插话!)
    Note over A: steering 队列(07)<br/>不打断
    T->>T: 采样间隙合并注入
    T->>O: run_tests(高危→Guardian(08)审:通过)
    FE->>A: Op.Interrupt(用户等急了)
    A->>T: 三段取消(02)→工具定格
    T->>LOG: 部分结果+interrupted 标记→flush
    LOG-->>FE: TurnAborted(flush 后发出)
    Note over FE: 30 分钟后进程崩溃重启
    FE->>A: RESUMED 恢复(06)
    A->>T: 重放到一致点继续
    Note over FE: 投影层(10)全程三端同步 ✅
```

**一句话**：一条消息先入有界队列（01）、变成任务（02）、工具失败不崩（03）、审批问一次（04）、上下文压着跑（05）、崩了能恢复（06）、插话不打断（07）、高危 AI 先审（08）、多代理各占一会话（09）、三端看同一引擎（10）。

### 二.1 本节核对（完整旅程覆盖）

- [ ] sequence 的每一跳都能在对应迭代篇找到实现落点（如 Interrupt→02 三段取消、Guardian→08、RESUMED→06）
- [ ] 「30 分钟后崩溃重启→RESUMED 恢复」一步与 06 §三 的恢复入口语义一致

## 三、关键设计复盘

| # | 设计 | 为什么 |
|---|------|--------|
| 1 | 契约先行（Op/Event sealed） | 前端/持久化/测试对契约编程，引擎可重构 |
| 2 | 单写者+双通道 | 状态零锁、命令有反压、事件不丢 |
| 3 | 任务化+单点收尾 | 加任务类型零成本获得生命周期 |
| 4 | 三段取消+持久化先于终态 | 打断有语义、崩溃后无矛盾状态 |
| 5 | 失败回填 | 会话永不因工具崩，模型自纠 |
| 6 | 审批=纯函数+key 缓存 | 既安全又不骚扰 |
| 7 | 版本号快照+三路径压缩 | 超限续命、并发零地狱 |
| 8 | FORKED 继承源格式 | 分叉 bug 高发区的显式纪律 |
| 9 | steering+裁决状态机 | 插话不打断、用户视图一致 |
| 10 | 投影只读 | 双向分离，多前端零改引擎 |

### 三.1 本节核对（关键设计复盘）

- [ ] 十项设计每条能用一句话说清「为什么」（如 3 任务化=加任务类型零成本获生命周期、5 失败回填=会话永不因工具崩）
- [ ] 每条设计与对应 ADR（12 篇）编号挂钩（如 2 双通道→ADR 22-01、9 steering→ADR 22-11）

## 四、全篇回归验证

> §一.1/§二.1/§三.1 核对通过后的整体回归。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 按 §二 旅程逐跳复跑各迭代用例（01-10 全篇回归） | 每跳断言 PASS：插话→steering、工具→回填、Guardian→审阅、打断→定格、崩溃→RESUMED 恢复、多端→投影收敛 |
| 2 | 全链演练（插话→工具→Guardian→打断→崩溃→恢复→多端）季度例行 | 单次演练整链通过，无环节回归失败 |

**失败排查**：任一跳 FAIL 回溯到对应迭代篇（01-10）的本节测试与排查项。

**下一篇**：[12-ADR架构决策记录](12-ADR架构决策记录.md)。
