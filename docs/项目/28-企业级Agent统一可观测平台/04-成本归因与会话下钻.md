# 04-成本归因与会话下钻

> **定位**：让成本**可下钻**——总量→租户→业务线→会话→单次调用。核心：**① usage 挂链路（每次 LLM 调用的 Token 进 Trace 属性）② 归因聚合（按租户/业务线/模型维度）③ 成本异动定位（哪类会话烧钱）**。呼应 [27-成本治理](../../教程/27-成本治理与Token计量.md)、[20-计量](../../项目/20-企业级Agent经济与结算平台/03-计量与计费引擎.md)。前置阅读：[03-单一玻璃板与三视图](03-单一玻璃板与三视图.md)。
>
> **铁律 0**：usage 计量=实证 `Usage`（gen_ai 属性自动）；归因聚合自研「概念代码」。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①usage 与 traceId/会话绑定（Span 属性随链路）②多维护表聚合（租户×业务线×模型×时间）③烧钱会话 TopN 与异动归因 |
| **影响了哪些模块** | 02 管道 → usage 不采样直进指标；玻璃板 → 成本视图 |
| **架构如何演进** | 成本从"月度账单"演进为"实时可下钻" |
| **上一版痛点** | 只知总花费；超预算不知是哪类会话烧的 |

**本迭代验收**：①成本下钻到会话 100%（00 验收③）②TopN 烧钱会话可见 ③异动（突增）定位到维度。

## 二、usage 挂链路与聚合

```java
// 概念代码：usage 归因（Span 属性 → 多维聚合）
// 每次 LLM 调用的 gen_ai.usage.* 属性带 traceId/sessionId/tenantId(02管道增补)
public CostView cost(RangeWindow w, DimSlice dim) {
    return metrics.query("gen_ai_token_total", w, dim)     // 按租户/业务线/模型切片
        .map(m -> CostView.of(m.dims(), m.value().multiply(priceTable.of(m.modelDim()))));
}
public List<SessionCost> topExpensive(RangeWindow w, int n) {  // 烧钱会话 TopN
    return store.topSessionsByUsage(w, n);   // → 3 跳下钻到该会话链路(03)
}
```

## 三、验收

| 测试 | 期望 |
|------|------|
| 某会话跑大任务 | 该会话 usage 可单独查 |
| 月成本突增 | 定位到租户/模型/会话类型 |
| TopN | 烧钱会话排行可下钻 |

> **下一步**：成本挂上链路了，**效果信号（评测/漂移）还没挂**。05 迭代做**评测信号汇入**——效果与链路同屏。
