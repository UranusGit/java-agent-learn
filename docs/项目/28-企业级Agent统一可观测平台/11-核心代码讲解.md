# 11-核心代码讲解

> **定位**：把项目 28 的核心实现串成主线——以**一次"夜间模型网关抖动引发 SLO 快烧 → 自动回滚"**事件贯穿各迭代代码的协作。前置阅读：[01-最小Demo](01-最小Demo.md) 至 [10-容量与性能观测](10-容量与性能观测.md) 全部。
>
> **铁律 0**：Observation/OTel/gen_ai 实证基座；聚合/根因/联动为「概念代码」。

---

## 一、一次夜间故障的观测旅程

```mermaid
sequenceDiagram
    participant W as 模型网关(变更v12)
    participant P as OTel管道(02)
    participant S as SLO(07)
    participant H as AlertHub(06)
    participant R as 回滚(08)
    participant ON as 值班

    W->>P: 5xx Span 上报(gen_ai)
    P->>P: 脱敏+tenant增补+错误全采样
    P->>S: 错误率指标
    S->>H: 快烧告警(1h燃烧>14.4)
    H->>H: 聚合30条→根因:网关v12首发
    H->>R: 关联变更(v12,2h前发布)
    R->>R: 低风险→自动回滚v11+通知
    R-->>ON: 通知(MTTR 4分钟,无人值守)
```

### 一.1 本节核对（一次夜间故障的观测旅程）

- [ ] 能按 sequenceDiagram 的时间序复述：5xx 上报 → 脱敏/采样 → 错误率指标 → 快烧告警 → 聚合根因 → 关联变更 → 自动回滚 → 通知
- [ ] 旅程中的每个参与者（P/S/H/R）对应到具体迭代（02/07/06/08），无错位

## 二、中枢主链（串联各迭代）

```java
// 概念代码：可观测中枢核心链
Flux<Signal> signals = pipeline.stream()                     // 02 管道(脱敏/采样/标签)
    .publishTo(meterRegistry, traceStore, llmStore);          // 三路
sloEngine.watch(signals)                                     // 07 SLO/燃烧率
    .compose(alertHub::aggregateAndRca)                      // 06 聚合根因
    .filter(Alert::actionable)
    .subscribe(actionRouter::execute);                       // 08 回滚/扩容动作
glassBoard.serve(SRE, BIZ, FINANCE, tenantScope);            // 03/04/05/09 视图
```

### 二.1 本节核对（中枢主链）

- [ ] 主链的数据流（02 管道 → 07 SLO → 06 聚合根因 → 08 动作 → 03 视图）能逐段复述，每段标注的迭代号一致
- [ ] 能指出"动作订阅"与"视图服务"两条支线分别消费同一信号流的使用

## 三、分层职责（读代码抓手）

| 类 | 迭代 | 职责 |
|----|------|------|
| `Pipeline`(配置) | 02 | 脱敏/采样/租户标签/三路导出 |
| `GlassBoard` | 03/09 | 三视图+租户双视角 |
| `CostAttribution` | 04 | usage 多维下钻 |
| `SignalLinker` | 05 | 评测/漂移挂链路 |
| `AlertHub`/`RcaEngine` | 06 | 聚合+拓扑根因 |
| `SloEngine` | 07 | 双层 SLO+错误预算 |
| `ActionRouter` | 08 | 自动回滚(双防护门) |
| `CapacityJob` | 10 | 分层画像+容量预警 |

### 三.1 本节核对（分层职责）

- [ ] 9 个类（Pipeline/GlassBoard/CostAttribution/SignalLinker/AlertHub/RcaEngine/SloEngine/ActionRouter/CapacityJob）各自的迭代来源与职责能对应
- [ ] 表内每个类的迭代号与该迭代篇（02-10）匹配，无串期

## 四、最容易踩的三个坑

1. **应用直连后端** → 管理混乱无脱敏。**解法**：统一管道（02）。
2. **指标无租户标** → 无法聚合下钻。**解法**：管道增补标签（02/09）。
3. **无变更关联的自动回滚** → 误回滚历史问题。**解法**：变更窗口限定（08）。

### 四.1 本节核对（最容易踩的三个坑）

- [ ] 三个坑各自能说出"症状 → 解法"映射（直连→管道 / 无标签→增补 / 无窗口回滚→限定），并各自对应到迭代 02/09/08
- [ ] 在后续代码中能主动识别这三类反模式，不再踩同坑

## 五、全篇回归验证（对照 00 量化指标）

**回归断言**（各迭代章节内的本节验证均通过后，按下表做全篇五指标验收）：

| 指标 | 目标 | 实现 | 回归操作 |
|------|------|------|---------|
| 一块屏定位 | P95≤3min | 03/06 | 复跑 `## 二.1`（03）下钻与（06）根因测试 |
| Trace 覆盖 | ≥95% | 02 | 复跑 `## 二.1`（02）Span 覆盖/采样测试 |
| 成本下钻 | 会话级100% | 04 | 复跑 `## 二.1`（04）usage 聚合测试 |
| 根因建议率 | ≥80% | 06 | 复跑 `## 二.1`（06）建议命中率测试 |
| 脱敏 | 100% | 02 | 复跑 `## 二.1`（02）redaction 测试 |

> **下一步**：代码串讲完成。12-ADR 记录 8 条关键决策；13-前沿做可观测前沿对照与演进。
