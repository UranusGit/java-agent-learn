# 08-多租户上下文隔离

> **定位**：上下文的**租户边界**——**① 供给层按租户隔离（记忆/RAG 召回不跨租户）② 缓存按租户分键 ③ 租户级上下文策略（配额/保留策略差异）**。呼应 [26-多租户](../../教程/26-多租户隔离与资源治理.md)、[25-作用域](../../项目/25-企业级Agent记忆中枢/05-作用域隔离与多租户.md)。前置阅读：[07-场景化配额模板](07-场景化配额模板.md)。
>
> **铁律 0**：隔离机制自研「概念代码」（Reactor Context 传租户，WebFlux 铁律）。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①供给隔离（记忆/检索召回强制租户过滤）②缓存分键（防跨租户缓存串数据）③租户策略差异（VIP 大预算/免费小预算） |
| **影响了哪些模块** | 供给层/缓存层 → 租户维度；配置 → 租户级 |
| **架构如何演进** | 上下文中枢补齐多租户安全边界 |
| **上一版痛点** | 缓存键无租户可能串答案；免费租户挤占资源 |

**本迭代验收**：①跨租户召回为 0 ②缓存键含租户 ③租户策略分级生效。

## 二、隔离三处

```java
// 概念代码：租户贯穿（Reactor Context）
public Mono<Assembled> assemble(String input) {
    return Mono.deferContextual(ctx -> {
        String tenant = ctx.get("tenant.id");          // 响应式上下文取租户(WebFlux铁律)
        var mem = memory.recall(tenantScoped(tenant), input);      // ①供给隔离
        var rag = retrieval.search(tenantScoped(tenant), input);
        var cached = semanticCache.get(key(tenant, input));        // ②缓存分键
        return render(policy.of(tenant), mem, rag, input);        // ③租户策略
    });
}
```

## 三、验收

| 测试 | 期望 |
|------|------|
| 租户 A 召回 | 不含 B 的记忆/文档 |
| 缓存命中 | 只命自己租户的 |
| VIP 租户 | 大配额生效 |

> **下一步**：隔离齐了。09 进阶做**上下文漂移检测**——装配质量的退化防线。
