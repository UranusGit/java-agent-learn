# 27-模型内容审核与安全护栏：输入审核 + 输出审核 + 审核策略

> **定位**：给平台加**内容安全层**：**输入审核**（用户请求安全）、**输出审核**（模型回复安全：暴力/仇恨/色情/个人身份等）、**审核策略**（分档处理：拦截/降级/标记）。这是合规底线（尤其面向公众/未成年/金融场景）。读者画像：想让平台"不输出违禁内容、不被滥用"的读者。前置阅读：[35-工业前沿调研与特性补充](35-工业前沿调研与特性补充.md)、[附录 09-Agent安全深度]。
>
> **铁律 0**：`OpenAiModerationModel` 已 javap 实证（spring-ai-openai）；spring-security 本地未下载标注「需引入依赖后实证」。

---

## 一、四问（本轮：内容安全）

| 问 | 答 |
|----|----|
| **新增了什么需求** | ① 输入审核（用户请求）② 输出审核（模型回复）③ 审核策略分档（拦截/降级/标记） |
| **影响了哪些模块** | `policy-service`（审核服务）、`agent-executor`（出入站钩子） |
| **架构如何演进** | 无内容安全 → 输入+输出双审核 |
| **上一版本的痛点是什么** | ① 违禁输入/输出无拦截 ② 无分档策略 ③ 合规风险（27 前遗留） |

**本迭代验收**：① 违禁输入被拦截/标记 ② 违禁输出被阻断/降级 ③ 审核结果入审计 ④ 审核延迟 ≤300ms 不拖垮链路。

### 1.1 本节核对（四问）

- [ ] "上一版痛点"（无内容审核/无分档/合规风险）指向本迭代，与安全扩展板块定位一致
- [ ] 验收四项分别有验证承接：输入→§6.1、输出→§6.2、审计→§6.4、性能→§6.3

---

## 二、内容审核架构

```mermaid
flowchart LR
    U["用户输入"] --> IN["输入审核"]
    IN -->|"拦截"| BLOCK["拒绝 + 审计"]
    IN -->|"放行"| AE["agent-executor"]
    AE --> LLM["模型"]
    LLM --> OUT["输出审核"]
    OUT -->|"拦截"| BLOCK2["替换/降级 + 审计"]
    OUT -->|"标记"| MARK["带警示返回"]
    OUT -->|"放行"| RES["正常返回"]

    style IN fill:#e8f5e9
    style OUT fill:#e8f5e9
    style BLOCK fill:#ffcdd2
```

### 2.1 本节核对（内容审核架构）

- [ ] 能对照 §二流程图，说清输入侧（拦截/放行）与输出侧（拦截/标记/放行）的审核落点，以及两道审核都在 agent-executor 出入站
- [ ] 架构与 §五安全体系联动（统一审计/HITL/DLP）一致

---

## 三、输入审核（用户请求安全）

```java
package com.example.policyservice.moderation;

import org.springframework.ai.moderation.ModerationModel;   // javap 实证：spring-ai-model 有 ModerationModel
import org.springframework.ai.openai.OpenAiModerationModel; // javap 实证：spring-ai-openai
import org.springframework.stereotype.Service;
import reactor.core.publisher.Mono;

/** 输入审核——调用审核模型，返回是否违规及类别。 */
@Service
public class InputModerationService {

    private final OpenAiModerationModel moderation;   // 审核模型（真实 API，javap 实证）

    public InputModerationService(OpenAiModerationModel moderation) {
        this.moderation = moderation;
    }

    /** 审核用户输入；违规返回拦截建议。 */
    public Mono<ModerationVerdict> checkInput(String userText) {
        return Mono.fromCallable(() -> moderation.call(userText))   // 真实 API：call(String) → ModerationResponse
                .map(resp -> classify(resp))
                .onErrorReturn(ModerationVerdict.allow());          // 审核服务故障 → fail-open（非高危）
    }

    public record ModerationVerdict(boolean blocked, String category, double score) {
        static ModerationVerdict allow() { return new ModerationVerdict(false, "none", 0); }
    }

    private ModerationVerdict classify(Object resp) {
        // 根据审核响应分类（hate/harassment/sexual/self-harm 等类别）判定是否拦截
        return new ModerationVerdict(false, "none", 0);   // 概念代码：解析真实 ModerationResponse
    }
}
```

> **概念代码标注**：`ModerationResponse` 具体结构以所引 spring-ai-openai 版本 javap 为准；本迭代先接 `OpenAiModerationModel.call(...)` 真实入口，分类逻辑留概念。

### 3.1 本节测试与验证（输入审核）

**前置条件**：`OpenAiModerationModel` 已装配（javap 实证 spring-ai-openai）。

**材料**：§三 `InputModerationService`（真实 `moderation.call(String)` 入口）。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 手写 `InputModerationService` 后编译 | `BUILD SUCCESS`；`OpenAiModerationModel`/`ModerationModel` 真实 API（javap 实证） |
| 2 | 违禁样本（暴力/仇恨/自残）输入 | 分类判定违规→返回拦截建议（blocked=true） |
| 3 | 正常样本输入 | 放行（blocked=false） |
| 4 | 审核服务故障 | `onErrorReturn(allow())` fail-open（非高危放行），不拖垮链路 |

**失败排查**：①编译不过→`ModerationModel.call` 签名与本地版本不一致（回 [附录 05-SpringAI2-API基准]）；②分类不拦截→`classify` 为概念代码，需按真实 `ModerationResponse` 类别字段解析；③故障阻断链路→确认 `onErrorReturn` 兜底生效。

---

## 四、输出审核（模型回复安全）

```java
// agent-executor：回复出站前过输出审核（复用 18 的 OutputDlpAdvisor 模式）
// ① 规则级：正则命中暴力/仇恨/敏感词 → 拦截
// ② 模型级：OpenAiModerationModel 对回复审核 → 违规降级
// ③ 分档：拦截(直接阻断) / 降级(换安全回复) / 标记(带警示返回)
```

### 审核策略分档

| 分档 | 条件 | 处理 |
|------|------|------|
| 拦截 | 严重违规（自残/暴力/违法） | 阻断回复 + 审计 + 可选升级人工 |
| 降级 | 一般违规 | 替换为安全回复（"抱歉，该内容不便回答"） |
| 标记 | 边缘内容 | 带警示返回（"内容含争议信息，请谨慎对待"） |
| 放行 | 合规 | 正常返回 |

```mermaid
flowchart TB
    O["模型回复"] --> R{"规则命中?"}
    R -->|"严重"| B["拦截"]
    R -->|"一般"| D["降级"]
    R -->|"无"| M{"模型审核?"}
    M -->|"违规"| D
    M -->|"边缘"| T["标记"]
    M -->|"合规"| P["放行"]

    style B fill:#ffcdd2
```

### 4.1 本节测试与验证（输出审核）

**前置条件**：输出审核策略（规则级正则 + 模型级 `OpenAiModerationModel`）已接出站链。

**材料**：§四 分档策略（拦截/降级/标记/放行）。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 构造含严重违规（自残/暴力）的回复 | 拦截：阻断回复 + 审计 + 可选升级人工 |
| 2 | 一般违规回复 | 降级：替换为安全回复（"抱歉，该内容不便回答"） |
| 3 | 边缘内容 | 标记：带警示返回（"内容含争议信息…"） |
| 4 | 合规回复 | 放行正常返回 |

**失败排查**：①严重违规未断→规则级正则未覆盖或升级路径缺失；②降级/标记错乱→分档条件阈值判定；③误伤合规→规则过紧（先规则→再模型双保险，召回驱动调参）。

---

## 五、与安全体系联动

```mermaid
graph TB
    subgraph sec["安全体系"]
        M["内容审核(本迭代)"]
        D["DLP/脱敏(18)"]
        I["注入检测(08)"]
        H["HITL 审批(08)"]
    end

    M --> A["统一审计(07/17)"]
    D --> A
    I --> A
    H --> A
    A --> C["合规报告"]

    style A fill:#c8e6c9
```

- **审核结果入审计**：每次审核（输入/输出）记录 → 可回放（17 事件溯源）
- **与 HITL**：严重违规可升级人工复核（08）
- **与 DLP**：审核 + 脱敏双保险

### 5.1 本节核对（与安全体系联动）

- [ ] 审核结果入审计可回放（17 事件溯源）、严重违规升级 HITL（08）、审核+DLP 双保险（18）三联动口径一致
- [ ] §五示意（内容审核/注入/HITL/DLP 统一入审计→合规报告）覆盖了平台的安全纵深元素

---

## 六、全篇回归验证

**前置条件**：§1.1-§5.1 各节核对/测试均通过；审核服务与审计链路就绪。

**材料**：§3.1/§4.1 已覆盖的审核探针 + 性能与审计。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 输入违禁样本（暴力/仇恨/自残） | 拦截或标记；正常样本放行 |
| 2 | 构造含违禁内容回复 | 降级/拦截；边缘内容带警示标记 |
| 3 | 压测审核链路 | P99 ≤300ms，不拖垮端到端 |
| 4 | 审核结果入审计 | 可回放、可出合规报告（联动 17/07） |

**失败排查**：①失败看审计事件流定位；②审核不拦→§3.1/§4.1 回查；③性能不达标→审核模型调用并行/降级策略优化。

---

## 七、验收对照

| 验收项 | 达标标准 | 本迭代 |
|--------|---------|--------|
| 输入审核 | 违禁输入拦截/标记 | ✅ |
| 输出审核 | 违禁输出降级/拦截 | ✅ |
| 分档策略 | 拦截/降级/标记/放行 | ✅ |
| 审计联动 | 审核入审计可回放 | ✅ |
| 性能 | 审核 P99 ≤300ms | ✅ |

### 7.1 本节核对（验收对照）

- [ ] 五项验收项各有前文支撑：输入审核→§3.1、输出审核→§4.1、分档策略→§4.1、审计联动→§5.1、性能→§六回归 3
- [ ] "下一篇 28-API网关认证与防滥用"顺延编号，持续安全扩展板块

**下一篇**：28-API网关认证与防滥用。
