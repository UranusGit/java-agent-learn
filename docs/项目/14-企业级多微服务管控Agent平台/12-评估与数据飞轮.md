# 13-评估与数据飞轮——让 Agent 效果可度量、可回归、可进化

> **定位**：把项目从"能跑"推到"**效果可度量**"：官方 `Evaluator` 体系做 **LLM-as-Judge** 评估、**金标回归**防新增回归、**用户反馈采集**进数据飞轮（反馈→评测→优化→灰度→闭环）。本迭代同时给 09 的灰度发布补上"**评估通过才放量**"的闸门。读者画像：想让 Agent 效果"说得清、守得住、能进化"的读者。前置阅读：[11-响应式强化](11-响应式强化.md)、[教程 08-架构师进阶/03-自我反思与Agent评估]、[教程 08-架构师进阶/07-数据飞轮与持续改进]。
>
> **演进纪律**：本迭代做评估与飞轮；多 Agent 编排（14）、高级 RAG（15）不提前实现。
> **铁律 0**：官方 Evaluator 已 javap 实证（`org.springframework.ai.evaluation` + `org.springframework.ai.chat.evaluation`）。

---

## 一、四问（本轮：评估与数据飞轮）

| 问 | 答 |
|----|----|
| **新增了什么需求** | ① 官方 Evaluator 评估（相关性/忠实度）② 金标回归（防回归）③ 用户反馈采集 → 数据飞轮闭环 ④ 灰度评估闸门 |
| **影响了哪些模块** | 新增 `eval-service`（评估）；`admin-portal`（反馈采集）；`agent-control-center`（灰度闸门） |
| **架构如何演进** | 无度量 → 效果可度量、可回归、可进化 |
| **上一版本的痛点是什么** | ① 改 Prompt/模型不知好坏 ② 上线新版本无回归保护 ③ 用户反馈无闭环（12 前遗留） |

**本迭代验收**：① 金标集上相关度/忠实度可量化 ② 新版本回归通过才允许灰度放量 ③ 用户"👍/👎+原因"进入优化闭环。

### 1.1 本节核对（四问）

- [ ] "上一版痛点"（改 Prompt/模型不知好坏/无回归保护/反馈无闭环）与演进纪律一致，是本次评估化动因
- [ ] 新增需求四项（官方 Evaluator/金标回归/反馈飞轮/灰度闸门）分别落到 §三/§四/§五
- [ ] 验收三项分别有验证承接：量化→§六1、回归闸门→§六2、反馈闭环→§六3

---

## 二、评估架构

```mermaid
graph TB
    subgraph src["评估输入"]
        S1["金标数据集<br/>(问题-期望答案-上下文)"]
        S2["线上采样<br/>(真实请求+回复)"]
        S3["用户反馈<br/>(👍/👎+原因)"]
    end

    subgraph eval["eval-service"]
        E1["官方 Evaluator<br/>RelevancyEvaluator<br/>FactCheckingEvaluator"]
        E2["LLM-as-Judge<br/>(可定制 Judge)"]
        E3["回归比对<br/>基线 vs 候选"]
    end

    subgraph out["输出"]
        O1["指标报告<br/>相关度/忠实度/安全"]
        O2["回归判定<br/>通过/阻断"]
        O3["优化建议<br/>进灰度"]
    end

    S1 --> E1
    S2 --> E1
    S3 --> E2
    E1 --> E3
    E2 --> E3
    E3 --> O1
    E3 --> O2
    E3 --> O3

    style eval fill:#e8f5e9
```

### 2.1 本节核对（评估架构）

- [ ] 能对照 §二架构图，说清评估三输入（金标/线上采样/用户反馈）与 eval-service 的输出去向（指标报告/回归判定/优化进灰度）
- [ ] 官方 Evaluator（Relevancy/FactChecking）与 LLM-as-Judge 的分工（基准 vs 定制）口径与 §三坐标一致

---

## 三、官方 Evaluator（javap 实证：真实存在，非虚构）

### 3.1 坐标确认（铁律 0）

| 类 | 包 | 说明 |
|----|----|------|
| `Evaluator` | `org.springframework.ai.evaluation` | 接口：`evaluate(EvaluationRequest)` → `EvaluationResponse` |
| `EvaluationRequest` | `org.springframework.ai.evaluation` | 构造：`(userText, responseContent)` / `(dataList, responseContent)` / `(userText, dataList, responseContent)` |
| `EvaluationResponse` | `org.springframework.ai.evaluation` | `isPass()` / `getScore()` / `getFeedback()` |
| `RelevancyEvaluator` | `org.springframework.ai.chat.evaluation` | 相关性评估，基于 `ChatClient.Builder` |
| `FactCheckingEvaluator` | `org.springframework.ai.chat.evaluation` | 事实核查，基于 `ChatClient.Builder` |

> 这些是 **Spring AI 2.0.0 官方真实类**（javap 实证）——之前体系曾误判"无官方 Evaluator"，已纠正（见基线 §17）。

### 3.2 评估服务

```java
package com.example.evalservice;

import org.springframework.ai.chat.evaluation.RelevancyEvaluator;
import org.springframework.ai.chat.evaluation.FactCheckingEvaluator;
import org.springframework.ai.evaluation.EvaluationRequest;
import org.springframework.ai.evaluation.EvaluationResponse;
import org.springframework.stereotype.Service;

/** 评估服务——用官方 Evaluator 对 Agent 回复打分。 */
@Service
public class EvalService {

    private final RelevancyEvaluator relevancy;
    private final FactCheckingEvaluator factChecking;

    public EvalService(RelevancyEvaluator relevancy, FactCheckingEvaluator factChecking) {
        this.relevancy = relevancy;
        this.factChecking = factChecking;
    }

    /** 相关度评估（需上下文文档）。 */
    public EvaluationResponse checkRelevance(String question, String response, java.util.List<org.springframework.ai.document.Document> docs) {
        return relevancy.evaluate(new EvaluationRequest(question, docs, response));
    }

    /** 忠实度/事实核查。 */
    public EvaluationResponse checkFact(String question, String response) {
        return factChecking.evaluate(new EvaluationRequest(question, response));
    }
}
```

### 3.3 本节测试与验证（官方 Evaluator 服务）

**前置条件**：`RelevancyEvaluator`/`FactCheckingEvaluator` Bean 已装配（javap 实证官方类）。

**材料**：§3.2 `EvalService` + 金标样例（问题/期望/上下文）。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 手写 `EvalService` 后编译 | `BUILD SUCCESS`；`org.springframework.ai.chat.evaluation.RelevancyEvaluator`/`FactCheckingEvaluator`、`org.springframework.ai.evaluation.*` 真实存在（javap 实证） |
| 2 | 对一条金标样例调 `checkRelevance`（带 docs） | 返回 `EvaluationResponse`，`isPass()`/`getScore()` 符合预期（相关度阈值） |
| 3 | 调 `checkFact` | 返回忠实度评分，事实不符可被 `getFeedback()` 指出 |

**失败排查**：①Bean 找不到→官方 Evaluator 需 `ChatClient.Builder` 构造，确认自动装配；②`EvaluationRequest` 构造参数不符→核对三参/双参构造重载（javap 实证签名）；③评分恒稳不区分好坏→评估 prompt 语境或金标上下文未给足。

---

## 四、金标回归（上线闸门）

```mermaid
flowchart LR
    C["候选版本(定义/模型/Prompt)"] --> E["金标集回归"]
    E -->|"相关度≥0.9 且 忠实度≥0.85 且 无回归"| P["通过 → 允许灰度放量"]
    E -->|"任一项跌破基线"| B["阻断 → 不发版"]
    P --> G["灰度(09)"]
```

```java
// 回归判定：候选 vs 当前基线，跌破阈值即阻断
// 本迭代验收：金标集 100 条，候选相关度/忠实度 ≥ 基线-0.02 才放行灰度
```

### 4.1 本节测试与验证（金标回归闸门）

**前置条件**：金标集 100 条 + 当前基线已记录。

**材料**：候选 vs 基线回归比对逻辑。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 对候选调用金标集回归 | 相关度/忠实度分别统计 |
| 2 | 候选 ≥ 基线-0.02（相关度≥0.9 且 忠实度≥0.85） | 判定"通过"，允许灰度放量 |
| 3 | 构造故意引入回归的候选（如删关键语料） | 相关度/忠实度跌破基线，判定"阻断"，不发版 |

**失败排查**：①不达标仍放行→回归阈值/基线未正确计算；②删语料不降分→评估上下文未真正使用被删语料；③基准漂移→补充用例后基线需重采样。

---

## 五、数据飞轮（反馈 → 优化 → 灰度 → 闭环）

### 5.1 反馈采集

```java
// admin-portal/对话页：用户对每条回复 👍/👎 + 可选原因
// 反馈事件入 Kafka（feedback topic），带 traceId 可回链到完整上下文
public record UserFeedback(String traceId, String sessionId, boolean helpful, String reason) {}
```

### 5.2 飞轮闭环

```mermaid
graph LR
    F["用户反馈"] --> P["筛选/聚类<br/>(低分原因归因)"]
    P --> O["优化<br/>Prompt/RAG/模型"]
    O --> E["回归评估"]
    E -->|"通过"| G["灰度放量"]
    G --> D["线上"]
    D --> F

    style O fill:#fff9c4
    style E fill:#c8e6c9
```

**飞轮环节**：
1. **采集**：用户反馈 + 自动采样（低置信度/超时/重试过的）
2. **归因**：失败原因聚类（上下文缺失 / 工具失败 / 模型幻觉 / 指令不清）
3. **优化**：Prompt 版本、RAG 语料、工具描述、模型选择——都走版本化
4. **评估**：金标回归把关
5. **灰度**：小流量验证线上指标（满意率/解决率）→ 放量

> **决策原则**（[教程 08-架构师进阶/07-数据飞轮与持续改进]）：先试 Prompt/RAG（低成本），无效才微调模型；反馈驱动 vs 拍脑袋。

### 5.3 本节核对（数据飞轮）

- [ ] 能说清飞轮五环节（采集→归因→优化→评估→灰度）成环，且每环有落地载体（反馈事件/聚类/版本化 Prompt/金标回归/小流量）
- [ ] 反馈事件 `UserFeedback` 带 `traceId` 可回链，与 07 审计流打通；决策原则（先 Prompt/RAG 后微调）与 [教程 08-架构师进阶/07-数据飞轮与持续改进] 一致

---

## 六、全篇回归验证

**前置条件**：§1.1-§5.3 各节核对/测试均通过；金标集、评估服务、反馈事件流就绪。

**材料**：§3.3/§4.1 已覆盖的评估与回归探针 + 飞轮闭环。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 金标样例 → `RelevancyEvaluator` 打分 | `isPass()`/`getScore()` 符合阈值 |
| 2 | 构造故意引入回归的候选（删关键语料） | 回归评估阻断发布（跌破基线） |
| 3 | 造 100 条反馈（60 条👎"答案不全"） | 归因→Prompt/RAG 优化→金标回归通过→灰度→模拟满意率回升（飞轮闭环） |
| 4 | 候选 v4 触发回归（相关度 0.92） | 通过→允许 v4 灰度 10%；线上满意率 ≥ 基线才继续放量 |

**失败排查**：①失败看审计事件流定位；②打分不区分好坏→评估上下文不足；③回归闸门不阻断→阈值/基线核对；④飞轮不闭环→反馈事件未回链到优化版本。

---

## 七、验收对照

| 验收项 | 达标标准 | 本迭代 |
|--------|---------|--------|
| 官方 Evaluator | Relevancy/FactChecking 可用且打分 | ✅ |
| 金标回归 | 跌破基线阻断发布 | ✅ |
| 反馈闭环 | 👍/👎→归因→优化→灰度 | ✅ |
| 灰度闸门 | 评估通过才放量 | ✅ |
| 未提前引入后续能力 | 无多 Agent/高级 RAG | ✅ |

### 7.1 本节核对（验收对照）

- [ ] 五条验收项各有前文支撑：官方 Evaluator→§3.3、金标回归→§4.1、反馈闭环→§5.3/§六回归 3、灰度闸门→§六回归 4、未提前引入→§1.1 口径
- [ ] "下一篇 13-多Agent协作与工作流编排"顺延编号，且各回引处（标题/正文）与 12 文件号一致

**下一篇**：13-多Agent协作与工作流编排。
