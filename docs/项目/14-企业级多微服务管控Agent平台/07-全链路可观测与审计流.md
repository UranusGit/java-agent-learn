# 07-全链路可观测与审计流——gen_ai 语义约定 + 工具审计 + 决策回放

> **定位**：本迭代把平台变成**可观测**的：OTel 全链路（`gen_ai` 语义约定统一 LLM/工具/检索 Span）、`traceId` 贯穿日志与指标（三支柱关联）、工具调用审计流（结构化事件 + 回放 + 决策可解释）。读者画像：理解检索/记忆服务化，想看到"排障一次定位全链路"的读者。前置阅读：[06-检索与记忆服务化](06-检索与记忆服务化.md)、[教程 04-企业级架构主干/02-全链路可观测性]、[教程 04-企业级架构主干/03-工具执行可观测与审计]。
>
> **演进纪律**：本迭代做可观测 + 审计流；HITL 审批（07）、灰度成本（09）不提前实现。
> **铁律 0**：代码均经本地 jar `javap` 实证；`io.micrometer.tracing.Tracer` 本地未下载，标注「需引入依赖后实证」。

---

## 一、四问（本轮：全链路可观测）

| 问 | 答 |
|----|----|
| **新增了什么需求** | ① OTel 全链路 Trace（gen_ai 语义约定）② trace→log→metric 三支柱关联 ③ 工具审计流 + 决策回放 ④ SLO 指标 |
| **影响了哪些模块** | 全部服务接 OTel；`tool-executor` 发结构化审计事件；新增审计回放查询 |
| **架构如何演进** | 各服务独立埋点 → 统一可观测平面 |
| **上一版本的痛点是什么** | ① 无全链路 Trace ② 审计不可回放 ③ 排障靠人肉拼时间线（06 §八） |

**本迭代验收**：① 一次 Agent 请求在 Grafana 看到一个完整 Trace 树 ② 工具调用审计 100% 落库可回放 ③ 故障定位从"跨 5 个服务猜"变成"点开一个 Trace"。

### 1.1 本节核对（四问）

- [ ] "上一版痛点"（无全链路 Trace/审计不可回放/排障人肉拼时间线）与 [06 §八] 痛点一一对应，是本次可观测化动因
- [ ] 新增需求四项（OTel Trace/三支柱/审计流回放/SLO）分别落到 §二/§三/§四/§五
- [ ] 验收三项"一条 Trace 树 / 审计可回放 / 排障变点开一个 Trace"均有对应验证章节（§6.1/§6.2）

---

## 二、可观测架构

```mermaid
flowchart LR
    subgraph svc["各服务（javaagent 埋点）"]
        S1["agent-executor"]
        S2["model-gateway<br/>gen_ai.client.chat"]
        S3["tool-executor<br/>gen_ai.tool.call"]
        S4["retrieval-service<br/>db.system/vector"]
    end

    subgraph col["采集"]
        C1["OTel Agent"]
        C2["OTLP"]
        C3["OTel Collector<br/>tail 采样"]
    end

    subgraph back["后端"]
        B1["Tempo(Trace)"]
        B2["Prometheus(Metric)"]
        B3["Loki(Log)"]
        B4["Grafana 看板+告警"]
    end

    S1 --> C1
    S2 --> C1
    S3 --> C1
    S4 --> C1
    C1 --> C2 --> C3
    C3 --> B1
    C3 --> B2
    C3 --> B3
    B1 --> B4
    B2 --> B4
    B3 --> B4

    style back fill:#e3f2fd
```

### 2.1 本节核对（可观测架构）

- [ ] 能对照 §二流程图，说清"各服务 javaagent 埋点 → OTLP → Collector(tail 采样) → Tempo/Prometheus/Loki → Grafana"的数据流向，以及数据面哪些服务产出 `gen_ai` 语义 Span
- [ ] 三支柱（Trace/Metric/Log）在架构中各自有后端承接（Tempo/Prometheus/Loki），并能说明 `traceId` 如何贯穿三者

---

## 三、gen_ai 语义约定（真实 Span 结构）

```mermaid
flowchart TB
    ROOT["agent.session<br/>session=abc"] --> CHAT["chat.prompt 轮次"]
    CHAT --> GW["llm-gateway.proxy"]
    GW --> M1["gen_ai.client.chat<br/>gen_ai.request.model=deepseek-chat<br/>gen_ai.usage.total_tokens=812"]
    CHAT --> T1["gen_ai.tool.call<br/>spring.ai.tool.definition.name=queryOrder<br/>spring.ai.tool.call.id=call_01"]
    T1 --> T2["tool-service.execute<br/>db.query=120ms"]

    style M1 fill:#f3e5f5
    style T1 fill:#e8f5e9
```

> **标签名实证**（javap `ToolCallingObservationDocumentation` 字节码）：工具 Span 用 `spring.ai.tool.definition.name` / `spring.ai.tool.type` / `spring.ai.tool.call.id` / `spring.ai.tool.call.arguments` / `spring.ai.tool.call.result`；LLM Span 用 `gen_ai.request.model` / `gen_ai.usage.total_tokens` 等（详见 [教程 04-企业级架构主干/02-全链路可观测性 §4]）。

### 3.1 本节核对（gen_ai 语义约定）

- [ ] 能对照 §三 Span 树说出一次请求的父→子 Span 层级（agent.session → chat 轮次 → llm-gateway → gen_ai.client.chat / gen_ai.tool.call → tool-service.execute）
- [ ] 工具 Span 标签名与 javap 实证一致（`spring.ai.tool.definition.name`/`call.id`/`call.arguments`/`call.result`），未用 1.x 旧命名

---

## 四、工具审计流（结构化事件 + 回放）

### 4.1 工具观测 Handler（真实 API：`ObservationHandler<ToolCallingObservationContext>`）

```java
package com.example.toolexecutor.obs;

import io.micrometer.observation.Observation;
import io.micrometer.observation.ObservationHandler;
import io.micrometer.tracing.Tracer;   // ⚠ 需引入依赖 io.micrometer:micrometer-tracing（本地未下载，未 javap 实证）
import org.springframework.ai.tool.observation.ToolCallingObservationContext;   // javap 实证：真实类
import org.springframework.stereotype.Component;

/** 工具审计——参数/结果脱敏后发 Kafka（可回放、可解释）。 */
@Component
public class ToolAuditHandler implements ObservationHandler<ToolCallingObservationContext> {

    private final AuditEventEmitter emitter;
    private final Tracer tracer;

    public ToolAuditHandler(AuditEventEmitter emitter, Tracer tracer) {
        this.emitter = emitter;
        this.tracer = tracer;
    }

    // Observation.Context 无 getDuration()（javap 实证）——用 ctx.put/get 计时
    private static final Object START_KEY = new Object();

    @Override
    public void onStart(ToolCallingObservationContext ctx) {
        ctx.put(START_KEY, System.nanoTime());
    }

    @Override
    public boolean supportsContext(Observation.Context context) {
        return context instanceof ToolCallingObservationContext;
    }

    @Override
    public void onStop(ToolCallingObservationContext ctx) {
        Long start = ctx.get(START_KEY);
        long ms = start != null ? (System.nanoTime() - start) / 1_000_000 : -1;
        String traceId = tracer.currentSpan() == null ? "n/a" : tracer.currentSpan().context().traceId();
        // 脱敏后入审计（参数/结果含敏感信息，见脱敏器）
        emitter.emit(new ToolAudit(
                ctx.getToolDefinition().name(),
                mask(ctx.getToolCallArguments()),
                truncate(ctx.getToolCallResult()),
                ms, traceId));
    }

    private String mask(String s) { return s == null ? null : s.replaceAll("(token|key)=\"[^\"]*\"", "$1=***"); }
    private String truncate(String s) { return s == null || s.length() <= 500 ? s : s.substring(0, 500) + "..."; }
}
```

### 4.2 审计回放（决策可解释）

```mermaid
sequenceDiagram
    participant U as 审计员
    participant AP as admin-portal
    participant AS as audit-service
    participant K as Kafka/审计库

    U->>AP: 按 traceId/sessionId 回放
    AP->>AS: 查询审计链
    AS->>K: 按 traceId 聚合事件
    K-->>AS: 工具调用序列
    AS-->>AP: 完整决策轨迹(含 LLM 意图/工具参数/结果)
    AP-->>U: 可视化回放
```

### 4.3 本节测试与验证（工具审计流与回放）

**前置条件**：`micrometer-tracing` 依赖已引入（未 javap 实证标注）；Kafka 审计事件已接。

**材料**：§4.1 `ToolAuditHandler`（`ObservationHandler<ToolCallingObservationContext>`，javap 实证类）+ §4.2 回放链路。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 手写 `ToolAuditHandler` 后编译 | `BUILD SUCCESS`；`ToolCallingObservationContext`/`ObservationHandler` 真实 API；`onStart`/`onStop`/`supportsContext` 方法体成立 |
| 2 | 触发一次工具调用 | 审计事件发出；`ctx.get(START_KEY)` 计时生效（`Context` 无 `getDuration()`，用 `put/get` 计时——javap 实证） |
| 3 | 审计回放：`curl "http://localhost:8084/v1/audit/trace?traceId=trace_xxx"` | 返回完整决策轨迹：LLM 意图 → 工具调用(queryOrder, 参数脱敏) → 结果 → 耗时 |
| 4 | 脱敏断言 | 事件中 `token`/`key` 字段已被掩码（`mask()` 生效）、长结果被截断 |

**失败排查**：①`Tracer.currentSpan()` 为 null→Tracer Bean 未装配（依赖未引）；②回放拿不到轨迹→`traceId` 在工具事件与审计库之间未对齐；③脱敏失效→`mask()` 的正则未覆盖字段形态。

---

## 五、SLO 指标（可量化）

```java
// 各服务暴露核心指标（Micrometer，真实 API）：
//   agent.request.duration        → 端到端 P99
//   gen_ai.client.token.usage     → Token 计量（类型标签 prompt/completion）
//   agent.tool.call.duration      → 工具执行 P99
//   agent.retrieval.duration      → 检索 P99
//   agent.slo.error_budget        → SLO 错误预算
```

| SLO | 目标 | 告警 |
|-----|------|------|
| 端到端 P99 | ≤ 8s | P99>8s 持续 5 分钟 |
| 模型网关 P99 | ≤ 50ms | 持续 2 分钟 |
| 工具执行 P99 | ≤ 2s | 持续 5 分钟 |
| 可用性 | ≥ 99.9% | 错误率>0.1% |

### 5.1 本节核对（SLO 指标）

- [ ] 4 条 SLO（端到端/模型网关/工具/可用性）各目标值与告警阈值能背出，且与 00 篇量化验收（§八）对应迭代目标一致
- [ ] 指标名（`agent.request.duration`/`gen_ai.client.token.usage`/`agent.tool.call.duration`/`agent.retrieval.duration`）与 Micrometer/Observation 埋点命名一致，`gen_ai.client.token.usage` 术语与 Spring AI 成本计量一致

---

## 六、全篇回归验证

**前置条件**：§1.1-§5.1 各节核对/测试均通过；OTel 采集链路与审计库已就绪。

**材料**：一次跨服务请求 + 工具调用探针。

**步骤与断言**：

| # | 操作 | 预期（PASS 判据） |
|---|------|------------------|
| 1 | 触发一次跨服务请求 | Grafana/Tempo 中看到完整 Trace 树（4+ Span），且 `traceId` 在多个服务日志中一致（三支柱关联） |
| 2 | 触发工具调用，`curl "http://localhost:8084/v1/audit/trace?traceId=trace_xxx"` | 返回完整决策轨迹：LLM 意图 → 工具调用(queryOrder, 参数脱敏) → 结果 → 耗时 |
| 3 | 脱敏复查 | 审计事件中 token/key 字段已掩码 |

**失败排查**：①失败看审计事件流定位"入口闸与执行层"；②Trace 断链→各服务 OTel 埋点/`traceId` 透传未对齐；③断言不符优先核对前置数据构造。

---

## 七、本迭代痛点（下一步）

```mermaid
graph LR
    P1["危险操作无人审批<br/>高危工具直接执行"]
    P2["无安全策略纵深<br/>注入/DLP/RBAC 未落地"]
    P3["无合规留存治理<br/>GDPR/留存期限"]
    P1 --> NEXT["迭代七：HITL 审批 + 安全合规"]
    P2 --> NEXT
    P3 --> NEXT
```

1. **无 HITL**：危险工具（删除/外发/写库）无人工审批（07 做，落点是 ToolCallingManager/ToolCallback）
2. **无安全纵深**：Prompt 注入、DLP、RBAC、mTLS 未落地（07 做）
3. **无合规留存**：审计无留存期限/被遗忘权（07 做）

### 7.1 本节核对（本迭代痛点）

- [ ] 三类痛点（无 HITL/安全纵深/合规留存）全部指向迭代七（08），且 HITL 落点标注正确为 `ToolCallingManager` / `ToolCallback` 包装层（非 Advisor，铁律）
- [ ] 痛点源于可观测刚落地、安全/审批尚未跟上，与演进纪律一致

---

## 八、验收对照

| 验收项 | 达标标准 | 本迭代 |
|--------|---------|--------|
| 全链路 Trace | 一次请求完整 Span 树、traceId 贯穿 | ✅ |
| 三支柱关联 | trace→log→metric 可互查 | ✅ |
| 工具审计 | 100% 落库、可回放、参数脱敏 | ✅ |
| SLO 指标 | 核心指标暴露 + 告警 | ✅ |
| 未提前引入后续能力 | 无 HITL/安全纵深/GDPR | ✅ |

### 8.1 本节核对（验收对照）

- [ ] 五条验收项各有前文支撑：全链路 Trace→§六回归 1、三支柱→§2.1、工具审计→§4.3、SLO→§5.1、未提前引入→§1.1 口径
- [ ] "下一篇 08-HITL 审批与安全合规"与 §七 痛点①②③ 全对接，为演进起点

**下一篇**：08-HITL 审批与安全合规。
