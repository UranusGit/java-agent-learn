# 00-需求分析与架构设计：企业级多微服务管控分离 Agent 平台

> **定位**：本文是《企业级多微服务管控 Agent 平台》的奠基篇——给出一个**工业可落地**的、**多微服务**、**管控分离（Control Plane / Data Plane）** 的大型 Agent 平台的完整架构设计。读者画像：已经能写出单体 Agent 服务、掌握 Spring AI 2.0 核心用法，想要理解"一个生产级 Agent 平台到底拆成哪些服务、管控面与执行面如何分离、如何演进到工业落地"的 Java 架构师。前置阅读：[教程 04-企业级架构主干/00-管控分离架构]、[教程 04-企业级架构主干/01-微服务拆分与Agent部署]、[教程 04-企业级架构主干/02-全链路可观测性]。
>
> **API 真实性铁律（铁律 0）**：本文所有 Spring AI 2.0.0 代码/配置均经本地 jar `javap` 实证（`scripts/api-baseline-spring-ai-2.0.0.md` 为 ground truth）；未实证的写法一律标注「概念代码」。

---

## ★ 阅读顺序总览（序号 = 你的实际阅读顺序）

> 本子文件夹 36 个文件，**文件名编号 00-35 即阅读顺序**。按"全貌 → 起步 → 十八迭代 → 代码讲解 → 支撑参考 → 安全扩展 → 合规与可持续 → 前沿收官"组织：

| 阶段 | 文件 | 读者定位 |
|------|------|---------|
| ① 全貌 | 00-需求分析与架构设计 | **先读**：目标架构、管控分离、工业特性矩阵、迭代路线、量化验收 |
| ② 起步 | 01-最小Demo搭建 | **动手**：跑通最小闭环，验证技术栈可编译可测 |
| ③ 主体迭代 | 02~19（迭代一~十八） | **严格顺序**：一~九打地基（拆分/管控/多租户/工具/检索/观测/HITL/灰度/高可用），十~十八进阶（响应式/评估/多Agent/RAG/模型供应/可解释/安全纵深/前端/多模态）——每迭代先跑测试再进下一步 |
| ④ 代码讲解 | 20-核心代码讲解 | **回顾**：8 个基础实现模式（管控回调/工具循环/HITL/灰度/预算/检查点/缓存/审计） |
| ⑤ 代码讲解 | 21-进阶代码讲解 | **回顾**：进阶模式（响应式/Evaluator/DAG/RAG/降级/事件溯源/DLP/多模态） |
| ⑥ 支撑参考 | 22-ADR / 23-数据模型 / 24-事件驱动 / 25-SRE / 26-运营纵深 | **按需查阅**：决策依据、数据库全景、消息设计、运维手册、压测/计费/迁移——读到相关迭代时回头翻 |
| ⑦ 安全扩展 | 27-内容审核 / 28-网关认证 | **安全深化**：读完 08（安全）、04（网关）后可深入 |
| ⑧ 合规与可持续 | 29-内容标识适龄 / 30-信创国密 / 31-BCM与法律保全 / 32-ESG碳治理 / 33-隐私计算DSAR / 34-API生命周期与公平性 | **合规深化**：AIGC标识（法定）、信创国密、业务连续、碳治理、数据权利、长期演进地基——面向公众/政企/跨境的必读 |
| ⑨ 前沿 | 35-工业前沿调研 | **扩展**：2026 演进方向，随时可看 |

> 按此顺序，读者从"知道平台长什么样"一路演进到"能照着搭到生产"。**序号不对应文件，只对应学习阶段**。

---

## 一、项目背景与目标

### 1.1 为什么需要这样一个平台

单体 Agent 服务（一个 Controller + 一个 ChatClient + 若干工具）在企业里只能支撑 demo 和边缘场景。当 Agent 进入核心业务流（客服、审批、风控、运维处置、研发提效），会同时撞上八面墙：

1. **模型墙**：多个模型（DeepSeek/OpenAI/自建 vLLM）要按租户、按业务、按成本路由，还要降级、Key 池管理、配额
2. **工具墙**：工具数量上百，要准入、发现、沙箱执行、签名校验，不能谁都能注册
3. **编排墙**：复杂任务要 Plan-and-Execute、多 Agent 协作、HITL 审批，编排逻辑散落在业务服务里无法治理
4. **上下文墙**：会话记忆、长期记忆、RAG 检索要独立演进，不能塞在对话服务里
5. **治理墙**：限流、预算、灰度、审计、可解释——每个业务服务各写一套就失控
6. **可观测墙**：从 LLM 调用到工具执行到检索，全链路 Span 要对齐
7. **多租户墙**：数据隔离、配额隔离、模型隔离、审计隔离
8. **工业落地墙**：K8s 部署、优雅停机、容灾、容量规划、滚动发布

**结论**：当 Agent 承载的业务价值足够高时，它必须是一个"平台"而不是"服务"。平台 = 一组职责单一、独立扩缩容、通过契约协作的微服务，且**决策（管控）与执行（数据）分离**。

### 1.2 项目目标

构建一个工业可落地的企业级多微服务管控分离 Agent 平台：

| 维度 | 目标 |
|------|------|
| **规模** | 10+ 微服务，按负载独立扩缩容 |
| **管控分离** | Control Plane（决策）与 Data Plane（执行）物理/逻辑分离，策略/配置/编排定义走管控面下发 |
| **多租户** | 租户级数据/配额/模型/审计隔离，SaaS 化 |
| **多模型** | 模型网关统一路由、降级、Key 池、配额、成本归因 |
| **工具治理** | 工具注册中心 + 沙箱执行 + 准入/签名/审计 |
| **工业落地** | K8s + OTel 全链路 + 优雅停机 + 灾备 + 容量规划 + 灰度发布 |
| **可演进** | 从最小单体起步，按企业真实节奏迭代到大型微服务 |

### 1.3 与已有项目的关系

本项目是**独立项目**（CLAUDE.md 项目独立性铁律），不引用、不依赖任何其他项目。它的知识来源是**教程体系**（[教程 04-企业级架构主干/00-管控分离架构] 到 [教程 08-架构师进阶/10-多模型协作与供应策略]），是教程企业级能力的一次**大型组合落地**。

### 1.4 本节核对（项目背景与目标）

- [ ] 能不看正文说出"八面墙"中的至少 6 面（模型/工具/编排/上下文/治理/可观测/多租户/工业落地）与项目目标六维（规模/管控分离/多租户/多模型/工具治理/工业落地）的一一映射
- [ ] 平台目标表中"可演进"与"管控分离"两条与本文后续迭代路线（§七）、覆盖矩阵（§九）口径一致，无相互矛盾
- [ ] 1.3「独立项目」表述与 CLAUDE.md 项目独立性铁律一致，全文未引用任何其他项目文件

---

## 二、四问（本轮：需求分析阶段）

| 问 | 答 |
|----|----|
| **新增了什么需求** | 需求分析阶段明确平台边界：多微服务拆分、管控分离、多租户、多模型、工具治理、可观测、HITL、灰度、成本、高可用——作为后续迭代的验收基线 |
| **影响了哪些模块** | 新平台，无历史模块 |
| **架构如何演进** | 定义目标架构（本文）→ 01 最小单体 → 02 起按迭代拆分/强化 |
| **上一版本的痛点是什么** | 单体 Agent 服务撞上"八面墙"（见 §1.1），无法支撑核心业务 |

### 2.1 本节核对（四问）

- [ ] 能复述本迭代（需求分析阶段）的"新增需求"正是后续七个迭代要落地的验收基线（拆分/管控/多租户/多模型/工具治理/可观测/HITL/灰度/成本/高可用），非空话
- [ ] "上一版痛点=单体撞八面墙"与 §1.1 的八面墙一一对应，未引入本文不存在的版本
- [ ] 演进线"00→01→02"与 §七 迭代路线中的起步/拆分顺序一致

---

## 三、整体架构：管控分离全景

### 3.1 架构总览

```mermaid
graph TB
    subgraph entry["接入层"]
        APIGW["API Gateway<br/>认证 / 限流 / 流量分发"]
    end

    subgraph cp["Control Plane（管控面：决策）"]
        direction TB
        CCS["agent-control-center<br/>管控中心：策略 / 编排定义 / 配置下发"]
        MGS["model-gateway<br/>模型网关：路由 / 降级 / Key 池 / 配额"]
        TRS["tool-registry<br/>工具注册中心：准入 / 发现 / 元数据"]
        PS["policy-service<br/>策略引擎：限流 / 审批 / 安全策略"]
        AP["admin-portal<br/>管理端：租户 / 发布 / 治理操作台"]
    end

    subgraph dp["Data Plane（数据面：执行）"]
        direction TB
        AEX["agent-executor<br/>Agent 执行器：编排 / 上下文组装 / 多Agent"]
        CHS["chat-service<br/>对话服务：ChatClient 会话"]
        TEX["tool-executor<br/>工具执行器：沙箱 / 调用 / 审计"]
        RTS["retrieval-service<br/>检索服务：RAG / 向量库 / 混合检索"]
        MMS["memory-service<br/>记忆服务：会话 / 长期 / 语义记忆"]
        AUS["audit-service<br/>审计服务：日志 / 回放 / 合规留存"]
    end

    subgraph infra["基础设施"]
        direction TB
        REG["服务注册发现<br/>Nacos / K8s Service"]
        MQ["Kafka 事件总线<br/>会话事件 / 工具事件 / 审计事件"]
        DB[("PostgreSQL<br/>业务 + 向量(pgvector)")]
        RD[("Redis<br/>缓存 / 会话 / 限流")]
        OBS["OTel Collector + Prometheus + Grafana + Loki"]
    end

    APIGW --> CP
    APIGW --> DP
    CP -. 策略 / 配置 / 编排定义下发 .-> DP
    DP -->|"指标 / 审计 / Trace"| OBS
    CP -->|"指标 / 状态上报"| OBS
    DP --> DB
    DP --> RD
    DP <--> MQ
    CP <--> MQ
    CP --> REG
    DP --> REG

    style CP fill:#e3f2fd
    style DP fill:#e8f5e9
    style infra fill:#f5f5f5
```

**核心思想**：**决策与执行分离**——
- **管控面（Control Plane）**：决定"应该做什么"——模型选哪个、工具能不能用、租户配额够不够、要不要人工审批、策略/配置/编排定义长什么样。变更频繁、有状态、少量实例。
- **数据面（Data Plane）**：执行"具体怎么做"——编排推理、调模型、执行工具、检索、读写记忆、生成响应。变更低频、尽量无状态、按负载大量扩缩容。

### 3.2 一次请求的完整旅程（管控/数据如何协作）

```mermaid
sequenceDiagram
    participant C as 客户端
    participant GW as API Gateway
    participant AEX as Agent执行器(Data)
    participant MGS as 模型网关(Control)
    participant TRS as 工具注册(Control)
    participant TEX as 工具执行(Data)
    participant OBS as 可观测(Infra)

    C->>GW: 请求(带租户+会话)
    GW->>AEX: 路由到 Agent 执行器
    AEX->>MGS: ① 取策略/配额/路由决策(含租户上下文)
    MGS-->>AEX: 模型选择 + 是否允许继续
    AEX->>AEX: 编排推理(Plan-and-Execute)
    AEX->>TRS: ② 解析到工具 → 查准入/签名
    TRS-->>AEX: 工具定义(审查通过版)
    AEX->>TEX: ③ 执行工具(沙箱)
    TEX-->>AEX: 工具结果
    AEX->>AEX: 续流生成最终响应
    AEX-->>GW: 流式响应
    GW-->>C: SSE 响应
    Note over AEX,TEX: 全程 Span 进入 OBS；工具/审计事件入 Kafka
```

**关键点**：数据面在**每个决策点**回调管控面（模型路由、工具准入、配额、审批），但**执行本身**（推理、工具调用、检索）不经过管控面——保证低延迟关键路径不被打断。

### 3.3 多租户隔离设计

```mermaid
graph TB
    subgraph tenant["租户边界"]
        T1["租户 A<br/>业务线 / 会话 / 数据"]
        T2["租户 B<br/>业务线 / 会话 / 数据"]
    end

    subgraph iso["隔离层"]
        I1["数据隔离<br/>行级（tenant_id 过滤）"]
        I2["配额隔离<br/>租户 Token / QPS / 并发"]
        I3["模型隔离<br/>租户可选模型 / Key 池"]
        I4["审计隔离<br/>租户审计独立留存"]
    end

    subgraph exec["执行面"]
        E1["agent-executor / chat-service"]
        E2["retrieval-service / memory-service"]
    end

    T1 --> I1
    T1 --> I2
    T1 --> I3
    T1 --> I4
    T2 --> I1
    T2 --> I2
    T2 --> I3
    T2 --> I4
    I1 --> E1
    I2 --> E1
    I3 --> E2
    I4 --> E2

    style iso fill:#fff3e0
```

**隔离实现**：租户上下文经 Reactor Context 贯穿（WebFlux 铁律，禁 ThreadLocal）；数据访问统一走 `tenant_id` 过滤（`Filter.Expression`/行级权限）；配额/模型/审计在管控面按租户评估。

### 3.4 可观测数据流（Trace→Log→Metric 三支柱）

```mermaid
flowchart LR
    subgraph src["埋点源（各服务）"]
        S1["gen_ai 语义约定<br/>LLM/工具/检索 Span"]
        S2["结构化日志<br/>traceId 贯穿"]
        S3["Micrometer 指标<br/>延迟/Token/错误"]
    end

    subgraph col["采集层"]
        C1["OTel Agent<br/>(javaagent)"]
        C2["OTLP 导出"]
    end

    subgraph back["后端"]
        B1["OTel Collector<br/>(tail 采样)"]
        B2["Prometheus<br/>指标"]
        B3["Loki<br/>日志"]
        B4["Tempo<br/>Trace"]
        B5["Grafana<br/>看板 / 告警"]
    end

    S1 --> C1
    S2 --> C1
    S3 --> C1
    C1 --> C2 --> B1
    B1 --> B2
    B1 --> B3
    B1 --> B4
    B2 --> B5
    B3 --> B5
    B4 --> B5

    style back fill:#e3f2fd
```

**三支柱打通**：一个 `traceId` 从 LLM 调用 → 工具执行 → 检索查询贯穿到日志与审计，排障"一次 trace 定位全链路"（[教程 04-企业级架构主干/02-全链路可观测性]）。

### 3.5 工具治理生命周期

```mermaid
stateDiagram-v2
    [*] --> 登记 : 工具作者提交
    登记 --> 准入审查 : 签名/描述/输入输出 Schema
    准入审查 --> 沙箱验证 : 通过
    准入审查 --> 驳回 : 不通过
    沙箱验证 --> 发布 : 隔离执行通过
    发布 --> 运行 : 进入 tool-registry 目录
    运行 --> 审计 : 每次调用
    运行 --> 降级 : 异常/超阈值
    降级 --> 下架 : 确认风险
    下架 --> [*]
    驳回 --> [*]
```

**治理闭环**：登记→准入→沙箱验证→发布→运行→审计→下架，全生命周期在管控面（tool-registry）管理，执行面（tool-executor）只负责隔离运行（[教程 04-企业级架构主干/03-工具执行可观测与审计]）。

### 3.6 部署拓扑（K8s 工业落地形态）

```mermaid
graph TB
    subgraph cluster["K8s 集群"]
        subgraph ns-cp["命名空间 cp（管控面）"]
            P1["agent-control-center"]
            P2["model-gateway"]
            P3["tool-registry"]
            P4["policy-service"]
            P5["admin-portal"]
        end

        subgraph ns-dp["命名空间 dp（数据面）"]
            D1["agent-executor<br/>HPA 横向扩缩"]
            D2["chat-service"]
            D3["tool-executor"]
            D4["retrieval-service"]
            D5["memory-service"]
            D6["audit-service"]
        end

        subgraph ns-infra["命名空间 infra"]
            INF1["OTel Collector"]
            INF2["Prometheus / Grafana"]
            INF3["Kafka"]
            INF4["PostgreSQL / Redis"]
        end
    end

    ING["Ingress/API Gateway"] --> ns-cp
    ING --> ns-dp
    ns-cp -. 策略/配置下发 .-> ns-dp
    ns-dp --> INF3
    ns-dp --> INF4
    ns-cp --> INF2

    style ns-cp fill:#e3f2fd
    style ns-dp fill:#e8f5e9
    style ns-infra fill:#f5f5f5
```

**部署要点**：管控面少量实例（1-3）、数据面按负载 HPA 横向扩缩；命名空间隔离管控/数据/基础设施；滚动发布 + 优雅停机 + 就绪/存活探针（[教程 04-企业级架构主干/13-部署与运维]）。

### 3.7 本节核对（管控分离全景架构）

- [ ] 能沿 §3.2 时序图走完"一次请求的完整旅程"，标出每个管控面回调点（模型路由/工具准入/配额/审批）与数据面执行段（推理/工具/检索），确认"决策走管控、执行不走管控"
- [ ] 架构总览（§3.1）中 12 个服务能按 Control Plane / Data Plane / 基础设施三组正确归类，且与 §四 微服务清单一致
- [ ] 三个架构维度图（§3.1 全景、§3.3 多租户、§3.5 工具治理）各自的隔离/治理闭环表述相互一致，无自相矛盾（如多租户隔离层与 §9.1 安全特性对齐）

---

## 四、微服务清单

| 服务 | 平面 | 职责 | 关键技术 | 教程锚点 |
|------|------|------|---------|---------|
| `agent-control-center` | Control | 管控中心：租户/策略/编排定义/配置下发 | Spring Boot + PG | [教程 04-企业级架构主干/00-管控分离架构] |
| `model-gateway` | Control | 模型路由/降级/Key池/配额/成本归因 | Spring AI + Resilience4j | [教程 04-企业级架构主干/12-模型路由与降级]、[教程 04-企业级架构主干/07-成本治理与Token计量] |
| `tool-registry` | Control | 工具准入/签名/发现/元数据 | Spring AI ToolCallback | [教程 04-企业级架构主干/03-工具执行可观测与审计] |
| `policy-service` | Control | 限流/审批/安全策略评估 | Spring WebFlux + Redis | [教程 04-企业级架构主干/11-安全与权限控制] |
| `admin-portal` | Control | 管理端操作台 | React（教程 00-基础与核心/03-工具调用 前端层） | [教程 03-React前端与AgenticUI/03-Agentic-UI设计] |
| `agent-executor` | Data | 编排/上下文组装/多Agent协作 | ChatClient + ToolCallingManager | [教程 00-基础与核心/07-ReAct推理模式]、[教程 08-架构师进阶/02-Agent工作流编排] |
| `chat-service` | Data | 对话会话管理 + **多页面流式**（跨页签 SSE/断线重连/中断恢复） | ChatClient + ChatMemory + WebFlux SSE | [教程 00-基础与核心/02-ChatClient与对话模型]、[教程 00-基础与核心/04-记忆与会话管理]、[教程 04-企业级架构主干/04-多页面流式响应与会话管理] |
| `tool-executor` | Data | 工具沙箱执行/审计 + **结构化工具事件**（参数/结果/耗时，供可视化消费） | ToolCallback + 沙箱 + Kafka | [教程 00-基础与核心/03-工具调用]、[教程 03-React前端与AgenticUI/04-流式工具调用与事件协议] |
| `retrieval-service` | Data | RAG/向量检索/混合检索 | VectorStore + RetrievalAugmentationAdvisor | [教程 00-基础与核心/05-RAG检索增强生成]、[教程 08-架构师进阶/01-高级RAG与AgenticRAG] |
| `memory-service` | Data | 会话/长期/语义记忆 | ChatMemoryRepository(自研) + VectorStore | [教程 08-架构师进阶/05-高级记忆架构] |
| `audit-service` | Data | 审计日志/回放/合规留存 | Kafka + PG | [教程 04-企业级架构主干/05-历史记录持久化与合规] |
| `api-gateway` | 接入 | 认证/限流/路由 | Spring Security + WebFlux | [教程 04-企业级架构主干/11-安全与权限控制] |
| `admin-portal`（前端） | Control | 管理端操作台 + **工具调用可视化**（工具名/参数/结果/耗时/审批状态实时呈现） | React + SSE/WebSocket 消费工具事件 | [教程 03-React前端与AgenticUI/03-Agentic-UI设计]、[教程 03-React前端与AgenticUI/02-React与SSE流式UI] |

### 4.1 本节核对（微服务清单与职责）

- [ ] 清单中每个服务能说出其"平面归属"（Control/Data/接入）与一条核心职责，随机抽查 5 个不卡壳
- [ ] 数据面（agent-executor/chat-service/tool-executor/retrieval-service/memory-service/audit-service）与 §3.1 架构图中 Data Plane 列表一一对应，无遗漏或多余
- [ ] 每个服务的"关键技术"标注与对应教程锚点一致，且均落在本文档声明的技术栈内（Spring AI 2.0/WebFlux/Java 21）

---

## 五、管控分离设计原则

### 5.1 为什么分离（不是"多个服务堆在一起"）

```mermaid
flowchart LR
    subgraph before["单体堆叠（错误示范）"]
        B1["策略逻辑散在<br/>每个业务服务"]
        B2["模型选择<br/>各写各的"]
        B3["工具准入<br/>不可审计"]
    end

    subgraph after["管控分离（本项目）"]
        A1["策略集中在<br/>Control Plane"]
        A2["模型决策集中<br/>model-gateway"]
        A3["工具准入集中<br/>tool-registry"]
    end

    before -. "变更要全量回归" .-> after
```

分离带来的四个工业价值：

1. **变更隔离**：策略/配置/模型路由的变更只动管控面，数据面无状态滚动、不回归业务
2. **独立扩缩容**：检索服务（CPU/内存密集）与对话服务（IO 密集）按各自瓶颈扩缩
3. **权限边界**：工具准入、审批、审计集中在管控面，数据面"无决策权"
4. **可回滚**：编排定义/策略版本化，一次错误下发可秒级回滚

### 5.2 决策点与执行点的切分（本项目关键设计）

| 决策点 | 由谁决策（Control） | 由谁执行（Data） |
|--------|-------------------|-----------------|
| 选哪个模型 | model-gateway（租户/成本/可用性） | agent-executor 发起调用 |
| 工具能不能用 | tool-registry（准入/签名/租户权限） | tool-executor 沙箱执行 |
| 配额够不够 | policy-service（限流/预算） | 调用前拦截 |
| 要不要人工审批 | policy-service（HITL 判定） | tool-executor 挂起等审批 |
| 编排用什么 | agent-control-center（版本化定义） | agent-executor 解释执行 |

> **HITL 落点**（铁律）：人工审批挂在 **`ToolCallingManager` 装饰器 / `ToolCallback` 包装层**（`org.springframework.ai.model.tool`），不是 Advisor——见 [教程 04-企业级架构主干/08-Human-in-the-Loop与审批流]。

### 5.3 服务间契约

- **同步查询**：管控面决策用 HTTP（`WebClient`，低基数、毫秒级）——模型路由、工具准入、配额
- **异步事件**：会话/工具/审计事件走 Kafka（高吞吐、解耦、回放）——工具结果、审计流、记忆写入
- **上下文传递**：租户/会话上下文用 **Reactor Context**（WebFlux 铁律：禁 ThreadLocal）；跨服务经 `traceparent`/业务头透传

### 5.4 本节核对（管控分离设计原则）

- [ ] 能给出管控分离的四个工业价值各一句话（变更隔离/独立扩缩容/权限边界/可回滚），并各举一个落地载体
- [ ] 能对照 §5.2 决策点表，说明 5 个决策点"由谁决策、由谁执行"，并确认 HITL 落点在 `ToolCallingManager` 装饰器 / `ToolCallback` 包装层（非 Advisor，符合铁律）
- [ ] §5.3 三种协作方式（HTTP 同步决策/Kafka 异步事件/Reactor Context 透传）与 §6 ADR-2、ADR-4 决策口径一致

---

## 六、关键技术决策（ADR 风格）

| # | 决策 | 备选 | 取舍理由 |
|---|------|------|---------|
| ADR-1 | 管控面/数据面物理拆分为多服务 | 单体+模块化 | 变更隔离 + 独立扩缩容是"工业落地"前提（见 §5.1） |
| ADR-2 | 管控决策走 HTTP 同步、事件走 Kafka | 全异步 | 决策延迟敏感（路由/准入），事件高吞吐；混合最稳 |
| ADR-3 | 模型网关自研（基于 Spring AI + OpenAI 兼容） | 引入第三方 LLM 网关 | 自研可深度集成 Spring AI 2.0 的 ChatClient/Observation；外部网关作备选（见 [教程 08-架构师进阶/10-多模型协作与供应策略]） |
| ADR-4 | 工具执行走独立沙箱服务 | 内联在 Agent 执行器 | 工具是最高风险面，隔离执行 + 单独扩容 + 审计闭环 |
| ADR-5 | 记忆服务自研 `ChatMemoryRepository` | 依赖框架 JDBC/Redis 记忆 | 本地 2.0.0 官方仅 `InMemoryChatMemoryRepository`（javap 实证），持久化/多服务共享需自研 |
| ADR-6 | 全链路 OTel（javaagent + OTLP Collector） | 只埋业务日志 | gen_ai 语义约定统一 LLM/工具/检索 Span（见 [教程 04-企业级架构主干/02-全链路可观测性]） |

### 6.1 本节核对（关键技术 ADR）

- [ ] 能对 6 条 ADR 各说出"备选方案"与"取舍理由"一句话——若说不出取舍理由，说明只记住了结论没理解权衡
- [ ] ADR-5（记忆自研 `ChatMemoryRepository`）的决策依据与铁律一致：本地 2.0.0 官方仅 `InMemoryChatMemoryRepository`（javap 实证），与 [附录 00-Advisor与ChatMemory] 口径一致
- [ ] ADR-1/2/3/5/6 各自能在后续迭代篇找到落地承接（拆分→02、HTTP+Kafka→02/24、模型网关→04、记忆自研→06、OTel→07）

---

## 七、迭代路线（工业落地演进）

```mermaid
timeline
    title 项目 14 迭代路线
    00 需求分析与架构设计 : 本文<br/>定义目标架构与验收基线
    01 最小 Demo : 单服务 ChatClient + 工具<br/>先跑通最小闭环
    02 迭代一 微服务拆分 : Agent / LLM 网关 / 工具服务<br/>三个服务独立部署
    03 迭代二 Control Plane : 管控中心 + 策略引擎<br/>决策与执行开始分离
    04 迭代三 多租户模型网关 : 租户隔离 + 路由降级<br/>Key 池 + 配额 + 成本归因
    05 迭代四 工具注册沙箱 : 准入签名 + 沙箱执行<br/>工具全生命周期治理
    06 迭代五 检索记忆服务 : RAG 独立 + 记忆独立<br/>上下文工程服务化
    07 迭代六 全链路可观测 : OTel 全链路 + 审计流<br/>gen_ai 语义约定统一
    08 迭代七 HITL 安全合规 : 审批流 + 安全策略<br/>ToolCallingManager 装饰器
    09 迭代八 灰度成本治理 : 灰度发布 + 预算上限<br/>版本化策略秒级回滚
    10 迭代九 高可用灾备 : K8s 多活 + 容量规划<br/>优雅停机 + 断点续跑
    20 核心代码讲解 : 基础实现模式全景
    11 迭代十 响应式强化 : WebFlux 铁律异步化
    12 迭代十一 评估与数据飞轮 : 效果可度量/可回归
    13 迭代十二 多Agent编排 : DAG/任务委派
    14 迭代十三 高级RAG上下文 : Agentic RAG/压缩
    15 迭代十四 模型供应边缘 : 多供应商/决策/容量
    16 迭代十五 可解释事件溯源 : 置信度/重放恢复
    17 迭代十六 安全纵深深化 : Tool Poisoning/供应链
    18 迭代十七 前端体验 : 看板/审批卡/多页签
    19 迭代十八 多模态浏览器 : 看图/操作Web
    21 进阶讲解 : 进阶模式全景
    22 ADR 决策记录 : 全项目决策资产
    23 数据模型设计 : 统一 ER + 全表
    24 事件驱动设计 : Kafka/schema/幂等/死信
    25 SRE 运维手册 : SLO/告警Runbook/值班
    26 运营纵深 : 压测/微调/计费/迁移
    27 工业前沿调研 : 2026 演进方向
```

**演进原则**：每次迭代从"上一版痛点"出发，回答"新增需求→影响模块→架构演进→痛点解决"四问；企业级能力（微服务拆分、管控分离、多租户、灰度、HITL、可观测、成本治理）作为**迭代里程碑落地**，不是文末展望。

### 7.1 本节核对（迭代路线）

- [ ] 对照 timeline，能说出迭代一~十八按"地基（02-10）→进阶（11-19）"主线的主题划分，且 00→01 起步顺序与演进原则吻合
- [ ] timeline 中 5 个里程碑能力（拆分/管控/多租户/灰度/HITL/可观测/成本治理）至少能标出各自的主承载迭代篇（拆分→02、管控→03、多租户模型网关→04、HITL→08、可观测→07、灰度成本→09）
- [ ] 确认演进原则"里程碑落地而非文末展望"在后续每篇迭代末尾以「量化验收」体现（抽查后续迭代篇）

---

## 八、量化验收标准（工业可落地的底线）

| 类别 | 指标 | 目标 | 对应迭代 |
|------|------|------|---------|
| **性能** | 端到端 P99 延迟（含 LLM） | ≤ 8s（流式首 token ≤ 1s） | 02-09 |
| **性能** | 模型网关路由决策 P99 | ≤ 50ms | 04 |
| **可靠性** | 服务可用性 SLO | ≥ 99.9%（月） | 10 |
| **可靠性** | 单服务故障自动恢复 | ≤ 60s 自愈 | 10 |
| **可靠性** | 无状态服务滚动发布 | 零停机 | 09 |
| **成本** | Token 成本可归因到租户/业务 | 100% 覆盖 | 04/09 |
| **成本** | 模型降级命中率（备用模型） | ≥ 95% 可用性保障 | 04 |
| **安全** | 工具准入覆盖率 | 100%（无未登记工具执行） | 05 |
| **安全** | 审计事件留存 | 100% 落库、可回放 | 07 |
| **安全** | 工具沙箱超时/资源限额 | 100% 工具受约束，超限即熔断 | 05 |
| **安全** | 敏感数据脱敏覆盖率（LLM 入参/出参） | 100% 命中规则 | 07 |
| **安全** | Prompt 注入/外发拦截率 | ≥ 99%（基准集） | 07 |
| **多租户** | 租户数据隔离违规数 | 0 | 04 |
| **评估** | Agent 评测回归通过率（金标集） | ≥ 95% 且无新增回归 | 08 |
| **性能** | 语义缓存命中率 | ≥ 20%（高频查询） | 09 |
| **运维** | 配置错误下发回滚 | ≤ 60s 全量回滚 | 09 |
| **运维** | 长任务断点恢复 | 100% 可续跑 | 09 |

### 8.1 本节核对（量化验收标准）

- [ ] 验收标准六类（性能/可靠性/成本/安全/多租户/评估/运维）各至少能复述一条目标值与"对应迭代"列，且"对应迭代"与 timeline（§七）一致（如模型网关路由 P99≤50ms→04）
- [ ] 能按验收项反推其验证手段（性能→§回归压测、多租户隔离→租户数据隔离断言、审计留存→审计流落库断言），即每条标准有可落地验证路径而非口号

---

## 九、工业级落地特性覆盖矩阵

> 「工业可落地」= 从"能演示"到"能生产"。以下矩阵把生产命门级特性**逐一**列出，标注**承载服务 / 落地迭代 / 教程锚点**。每项特性都在对应迭代作为**里程碑落地**（不是文末展望），并纳入该迭代的「测试与验证」。

### 9.1 安全与合规（纵深防御）

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 工具沙箱隔离（进程/容器/WASM）+ 超时 + CPU/内存/磁盘限额 | tool-executor | 05 | [教程 04-企业级架构主干/11-安全与权限控制] |
| 沙箱网络隔离（出网白名单 / 禁内网探测） | tool-executor | 05 | [教程 04-企业级架构主干/11-安全与权限控制] |
| 危险操作白名单（高危工具须审批） | tool-registry + policy-service | 07 | [教程 04-企业级架构主干/08-Human-in-the-Loop与审批流] |
| 凭证注入（Vault / 环境注入，不落日志、不回显） | tool-executor + config | 05 | [教程 04-企业级架构主干/11-安全与权限控制] |
| Prompt 注入检测 / 间接注入（外部数据进上下文前过滤） | policy-service + agent-executor | 07 | [附录 08-Agent安全深度/00-Prompt注入分类与案例] |
| 输出安全过滤 / DLP（外发内容审查） | agent-executor（ToolCallback 包装层） | 07 | [附录 08-Agent安全深度/02-数据泄露防护] |
| LLM 入参/出参数据脱敏掩码（PII/密钥） | agent-executor（Masking Advisor） | 07 | [附录 08-Agent安全深度/02-数据泄露防护] |
| mTLS / 零信任（服务间双向认证） | api-gateway + 服务间 | 07 | [教程 04-企业级架构主干/11-安全与权限控制] |
| RBAC 角色权限（租户管理员/审计员/开发者） | api-gateway + admin-portal | 07 | [教程 04-企业级架构主干/11-安全与权限控制] |
| GDPR / 个人信息保护法（留存期限、被遗忘权删除） | audit-service + memory-service | 07 | [教程 04-企业级架构主干/05-历史记录持久化与合规] |
| 数据主权/驻留（多区域数据隔离） | 部署层 | 09 | [教程 04-企业级架构主干/11-安全与权限控制] |
| 供应链安全（SCA 依赖审计 / SBOM / 镜像签名） | 平台 CI/CD | 08 | [教程 04-企业级架构主干/13-部署与运维] |

### 9.2 模型与知识资产治理

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 模型卡片 / 数据卡片（能力、局限、评测记录） | model-gateway + admin-portal | 04 | [附录 12-AI治理与合规/01-模型卡片与数据卡片] |
| 模型上线准入评估（先评测后上线） | 评估服务 + model-gateway | 08 | [教程 08-架构师进阶/03-自我反思与Agent评估] |
| 模型退役 / 灰度切换 | model-gateway | 04/09 | [教程 04-企业级架构主干/09-灰度发布与版本管理] |
| Prompt 仓库 / Prompt 版本管理 | agent-control-center | 03 | [教程 04-企业级架构主干/09-灰度发布与版本管理] |
| 知识库/文档版本化（RAG 语料可回滚） | retrieval-service | 06 | [教程 00-基础与核心/05-RAG检索增强生成] |
| 向量索引管理（重建/迁移/一致性校验） | retrieval-service | 06 | [教程 00-基础与核心/06-向量数据库选型] |

### 9.3 质量与评估

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| LLM-as-Judge 输出质量评估（忠实/相关/安全） | 评估服务（官方 Evaluator 或自研 Judge） | 08 | [教程 08-架构师进阶/03-自我反思与Agent评估] |
| 幻觉检测 / 引用溯源（回答须可回溯到证据） | retrieval-service + agent-executor | 06/08 | [教程 00-基础与核心/05-RAG检索增强生成] |
| 评测回归（金标数据集，防新增回归） | 评估服务 | 08 | [附录 04-测试策略/02-Eval评估] |
| 在线监控 + 离线评估闭环（数据飞轮） | 评估 + admin-portal | 08 | [教程 08-架构师进阶/07-数据飞轮与持续改进] |

### 9.4 可观测深化

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| gen_ai 语义约定全链路 Span（LLM/工具/检索） | OBS + 各服务 | 06 | [教程 04-企业级架构主干/02-全链路可观测性] |
| trace→log→metric 三支柱关联（traceId 贯穿） | OBS | 06 | [教程 04-企业级架构主干/02-全链路可观测性] |
| SLO / SLI / 错误预算 + 告警收敛 | OBS | 06/09 | [教程 04-企业级架构主干/02-全链路可观测性] |
| 审计回放 / 决策可解释（推理轨迹可视化） | audit-service + admin-portal | 06/07 | [教程 04-企业级架构主干/05-历史记录持久化与合规] |
| 工具调用观测（参数/结果/耗时，脱敏后） | tool-executor + ToolCallingObservationContext | 06 | [教程 04-企业级架构主干/03-工具执行可观测与审计] |

### 9.5 会话与体验

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 多页面流式输出（跨页签 SSE / 断线重连 / 中断恢复） | chat-service | 06 | [教程 04-企业级架构主干/04-多页面流式响应与会话管理] |
| 工具调用可视化（参数/结果/耗时/审批实时呈现） | admin-portal | 06 | [教程 03-React前端与AgenticUI/03-Agentic-UI设计] |
| 长任务异步化 + 检查点断点续跑 | agent-executor | 09 | [教程 08-架构师进阶/06-长任务持久化与中断恢复] |
| 失败转人工 / 工单升级 / SOP 集成 | agent-executor + 事件 | 08 | [教程 04-企业级架构主干/08-Human-in-the-Loop与审批流] |

### 9.6 发布与运维

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 平台 CI/CD + 蓝绿 / 金丝雀发布 | 部署层 | 09 | [教程 04-企业级架构主干/09-灰度发布与版本管理] |
| 动态配置中心 + 策略版本化 + 秒级回滚 | agent-control-center | 03/09 | [教程 04-企业级架构主干/00-管控分离架构] |
| 优雅停机 / 滚动发布 / 就绪存活探针 | 部署层 | 09 | [教程 04-企业级架构主干/13-部署与运维] |
| 压测基线 + 容量模型（QPS→实例数） | 部署层 + 可观测 | 09 | [教程 04-企业级架构主干/13-部署与运维] |
| HPA 自动扩缩容（数据面无状态横扩） | 部署层 | 09 | [教程 04-企业级架构主干/01-微服务拆分与Agent部署] |
| 故障演练 / 混沌工程（注入中断/延迟/超限） | 部署层 | 09 | [教程 04-企业级架构主干/10-容错与弹性设计] |
| 数据备份 / 恢复演练（RPO / RTO 达标） | 数据层 | 09 | [教程 04-企业级架构主干/05-历史记录持久化与合规] |

### 9.7 成本与性能

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 模型按任务定价 + Token 预算实时拦截 | model-gateway | 09 | [教程 04-企业级架构主干/07-成本治理与Token计量] |
| 预算自动暂停（租户/业务线超限自动停） | policy-service + model-gateway | 09 | [教程 04-企业级架构主干/07-成本治理与Token计量] |
| 语义缓存 / Prompt Cache / KV Cache 复用 | retrieval-service + model-gateway | 09 | [附录 09-语义缓存与性能] |
| 批量推理 / 请求合并（低峰优化） | model-gateway | 09 | [附录 09-语义缓存与性能/02-批量推理与请求合并] |
| 成本异常告警 / 按租户成本归因 | OBS + model-gateway | 09 | [教程 04-企业级架构主干/07-成本治理与Token计量] |

### 9.8 数据与合规

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 数据生命周期（保留/归档/过期删除） | audit-service + memory-service | 07 | [教程 04-企业级架构主干/05-历史记录持久化与合规] |
| 敏感数据分类分级 + 匿名化 | 数据层 + agent-executor | 07 | [附录 12-AI治理与合规/02-数据隐私与偏见检测] |
| 不可篡改审计（审计签名 / 追加写） | audit-service | 07 | [教程 04-企业级架构主干/05-历史记录持久化与合规] |
| 合规报告导出（按租户/时间窗） | audit-service + admin-portal | 07 | [附录 12-AI治理与合规/00-NIST-AI-RMF框架] |
| 数据血缘 / 溯源（回答→文档→来源） | retrieval-service + audit-service | 06 | [教程 00-基础与核心/05-RAG检索增强生成] |

### 9.9 服务治理与弹性

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 分布式限流分层（网关→服务→模型配额） | api-gateway + policy-service + model-gateway | 04 | [教程 04-企业级架构主干/06-多租户隔离与资源治理] |
| 熔断 / 降级 / 兜底（降级到规则引擎或人工） | model-gateway + 各服务 | 09 | [教程 04-企业级架构主干/10-容错与弹性设计] |
| 超时 / 重试 / 幂等（跨服务调用规范） | 各服务 | 02 | [教程 08-架构师进阶/08-响应式错误处理] |
| 会话跨服务一致性（事件溯源 / 幂等键） | agent-executor + Kafka | 09 | [教程 08-架构师进阶/06-长任务持久化与中断恢复] |

### 9.10 集成与组织

| 特性 | 承载服务 | 落地迭代 | 教程锚点 |
|------|---------|---------|---------|
| 企业系统集成（Webhook / 连接器 / 事件驱动） | api-gateway + 工具 | 05 | [教程 04-企业级架构主干/01-微服务拆分与Agent部署] |
| 多语言 / 国际化（模型/时区/文案） | chat-service + model-gateway | 04 | [教程 08-架构师进阶/10-多模型协作与供应策略] |
| 多区域就近访问 + 数据同步一致性 | 部署层 | 09 | [教程 04-企业级架构主干/13-部署与运维] |
| 多 Agent 协作（任务委派 / 工作流） | agent-executor | 08 | [教程 08-架构师进阶/02-Agent工作流编排] |

### 9.11 前沿治理与评估（2026 调研补充，见 [35-工业前沿调研与特性补充]）

> 依据 2026 Google/AWS/Microsoft/Nebius/PagerDuty 生产实践调研，作为平台演进方向（部分"规划中"）。

| 特性 | 承载服务 | 演进方向 | 来源 |
|------|---------|---------|------|
| Agent 级治理（身份徽章/注册表/网关/NL 安全策略/行为异常/安全看板） | 新增 agent-registry / agent-gateway | 规划中（P0） | [Google](https://cloud.google.com/blog/topics/developers-practitioners/five-guides-to-building-and-scaling-production-ready-ai-agents) |
| 影子 AI 治理（集中登记 Agent 所有者/数据/工具） | agent-registry | 规划中（P0） | [Google 20问](https://itbrief.com.au/story/google-sets-out-20-question-framework-for-ai-agents) |
| 评估驱动开发（八维评估/三层指标库/企业自持金标） | eval-service 深化 | 规划中（P0） | [AWS 指南](https://cloud.it168.com/a2026/0708/6939/000006939041.shtml) |
| 模拟先行（部署前模拟任务→评估集/回归套件） | eval-service + 模拟引擎 | 规划中（P1） | [Nebius](https://nebius.com/blog/posts/introducing-the-nebius-agents-blueprint) |
| 记忆 TTL / 失效感知 / 显式记住忘记 / 知识图谱 | memory-service | 规划中（P1） | [PagerDuty](https://www.pagerduty.com/eng/production-ai-agents-closing-the-gaps-between-idea-and-reality/) |
| A2A 协议 + Agent Cards（跨组织互操作） | A2A 适配层 | 规划中（P2） | [Google](https://cloud.google.com/blog/topics/developers-practitioners/five-guides-to-building-and-scaling-production-ready-ai-agents) |
| 语义安全双策略（传统 IAM + NL 语义检查） | policy-service / agent-gateway | 规划中（P1） | [Google](https://cloud.google.com/blog/topics/developers-practitioners/five-guides-to-building-and-scaling-production-ready-ai-agents) |
| Token 长尾监控（错误路径成本乘数） | model-gateway 成本治理 | 规划中（P1） | [Nebius](https://nebius.com/blog/posts/introducing-the-nebius-agents-blueprint) |

### 9.13 合规与可持续（2026-08-17 补充组，29-34 篇落地）

| 特性 | 承载服务 | 落地篇 | 依据 |
|------|---------|--------|------|
| AIGC 显式+隐式内容标识、C2PA 溯源、未成年人适龄 | chat-service 出站层 / policy-service | 29 | 《生成式AI服务管理暂行办法》/GB 45438-2025/EU AI Act |
| 信创适配（麒麟/达梦/昇腾）+ 国密 SM2/SM3/SM4 + 密评 | 部署层 / 全服务密码层 | 30 | 信创准入/GB/T 39786 |
| BCM 业务连续性（BIA/RTO-RTP/演练证据链）+ Legal Hold 法律保全 | agent-control-center / audit-service | 31 | ISO 22301/诉讼保全 |
| 能耗计量、碳足迹核算、碳感知调度、ESG 披露 | model-gateway / policy-service | 32 | EU CSRD/客户 ESG 问询 |
| DSAR 四权利流水线、联邦学习、隐私增强选型（DP/k匿名/TEE） | dsar-service / 联邦层 | 33 | GDPR/个保法/数据二十条 |
| API 版本化与 Sunset 废弃、Feature Flags 治理、Noisy Neighbor 公平性 | api-gateway / control-center / policy-service | 34 | 平台长期演进治理 |

### 9.12 覆盖核对（验收清单）

- **每项特性**都有 ①承载服务 ②落地迭代 ③可测试验证点（对应迭代「测试与验证」小节）
- **每个迭代**至少承载 2 项上述工业级特性，迭代末尾以「量化验收」确认落地
- 未在某迭代落地的特性，不允许写"已支持"——只能标"规划中"

### 9.14 本节核对（工业级特性覆盖矩阵）

- [ ] 从 9.1-9.13 各取一条特性，能说出其"承载服务 + 落地迭代"，并印证"工业级特性作为迭代里程碑而非文末展望"
- [ ] 抽查 §9.2/§9.8 涉模型能力与数据的特性，确认仅标"规划中"的（如 §9.11 影子 AI 治理）未写成"已支持"，符合 §9.12 覆盖核对铁律
- [ ] §9.13 合规可持续组（29-34 篇）与对应落地篇的自述主题一致，篇号均落在本子文件夹范围内

---

## 十、与教程体系的锚点

本项目是教程企业级与进阶能力的**组合落地**，各迭代在关键位置标注「遇到阻塞？→ [教程 XX-XXX §X]」：

| 教程篇目 | 项目落地点 |
|---------|-----------|
| [教程 04-企业级架构主干/00-管控分离架构] | 架构总览（§3）、迭代二 |
| [教程 04-企业级架构主干/01-微服务拆分与Agent部署] | 迭代一（服务拆分） |
| [教程 04-企业级架构主干/02-全链路可观测性] | 迭代六（OTel 全链路） |
| [教程 04-企业级架构主干/03-工具执行可观测与审计] | 迭代五/六（工具审计） |
| [教程 04-企业级架构主干/07-成本治理与Token计量] | 迭代四/九（成本归因） |
| [教程 04-企业级架构主干/08-Human-in-the-Loop与审批流] | 迭代七（HITL） |
| [教程 04-企业级架构主干/12-模型路由与降级] | 迭代四（模型网关） |
| [教程 08-架构师进阶/01-高级RAG与AgenticRAG] | 迭代五（检索服务） |
| [教程 08-架构师进阶/05-高级记忆架构] | 迭代五（记忆服务） |
| [教程 08-架构师进阶/06-长任务持久化与中断恢复] | 迭代九（断点续跑） |
| [教程 08-架构师进阶/10-多模型协作与供应策略] | 迭代四（多模型） |

### 10.1 本节核对（教程锚点）

- [ ] 对照教程篇目，能说明"这个教程能力在本项目的哪个迭代/章节落地"，抽查 3 篇不卡壳（如 82-高级记忆架构→迭代五记忆服务）
- [ ] 锚点覆盖与 §7 迭代路线、§9 覆盖矩阵的"落地迭代"列一致，无教程篇目引用了本项目不存在的落地篇号

---

## 十一、总结

本文定义了工业可落地的企业级多微服务管控 Agent 平台的**目标架构**：12 个服务按 **Control Plane（决策）/ Data Plane（执行）** 分离，管控面集中策略、路由、准入、审批，数据面专注编排、推理、工具执行、检索、记忆，中间经 HTTP 同步决策 + Kafka 异步事件协作，全链路 OTel 可观测，量化验收覆盖性能/可靠性/成本/安全/多租户。

### 11.1 本节核对（总结与演进起点）

- [ ] 总结的"12 服务 / CP-DP 分离 / HTTP 决策+Kafka 事件 / OTel 可观测"四要素与 §三、§四 正文口径一一对应
- [ ] "下一篇 01-最小 Demo（单服务 ChatClient+工具）" 与 §七 timeline 起步顺序一致，作为后续写作与阅读入口

**下一篇**：01-最小 Demo 搭建——先把最小闭环（单服务 ChatClient + 工具）跑通，再进入迭代拆分的演进之路。
