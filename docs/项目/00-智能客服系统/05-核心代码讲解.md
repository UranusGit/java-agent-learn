# 项目 00：智能客服系统 — 05-核心代码讲解

> **定位**：项目 00 的**代码地图 + 精讲**——每段完整代码已在前序迭代篇给出，本文告诉你「去哪段代码、为什么这样写、设计意图是什么」以及「手写顺序」。**完整代码以 01-04 各篇的代码块为准，本文不重复贴码**。
> **读者画像**：已跟随前四篇完成全部迭代，想深入理解「为什么这样设计」。
> **前置阅读**：[01-最小 Demo 搭建] → [02-工具集成] → [03-RAG 知识库] → [04-记忆与会话]。

---

## 1. 代码地图（完整代码在哪个文档）

| 代码 | 完整代码位置 | 设计要点 |
|------|-------------|---------|
| `pom.xml` + 两段式配置（`application.yaml` + `application-customerservice.yaml`）+ 启动类 | [01-最小Demo §2.1-2.3] | Spring Boot 4.1 + Spring AI 2.0 BOM + DeepSeek 兼容 |
| `ChatController` + `ChatService` | [01-最小Demo §2.4-2.5] | WebFlux + SSE 流式 |
| 工具（`FaqQueryTool`/`OrderQueryTool`） | [02-迭代一 §2.1-2.3] | `@Tool` + `@ToolParam` + 结构化输出 record |
| 工具注册（`ChatClientConfig`） | [02-迭代一 §2.4] | `defaultTools` 注册 `@Tool` Bean |
| RAG（`VectorStoreConfig`/`KnowledgeBaseLoader`） | [03-迭代二 §2.3-2.4] | ETL：Reader → Splitter → VectorStore |
| RAG Advisor（`QuestionAnswerAdvisor`） | [03-迭代二 §2.5] | `topK=3` + `similarityThreshold=0.7` |
| 记忆（`RedisChatMemoryRepository`） | [04-迭代三 §2.3] | **自研 `ChatMemoryRepository`**（官方无 Redis 实现） |
| 记忆（`MessageWindowChatMemory` + Advisor） | [04-迭代三 §2.4-2.5] | `ChatMemory.CONVERSATION_ID` 常量接会话 |

### 1.1 本节核对（代码地图完整性）

| # | 核对项 | PASS 判据 |
|---|--------|----------|
| 1 | 表中 8 行代码位置逐一打开 | 对应迭代篇锚点存在且代码块完整（无"同 vX"省略） |
| 2 | 自己工程中的类与表中一一对应 | 无缺类、无多余重复实现（如同时存在手写与自动配置 vectorStore） |

## 2. 手写顺序（照此顺序写，每步可验证）

| 顺序 | 里程碑 | 验证 |
|------|--------|------|
| 01 | 能对话（ChatClient + SSE） | `POST /api/chat/stream` 流式返回 |
| 02 | 能做事（工具） | 查订单/FAQ 正确返回 |
| 03 | 能读文档（RAG） | 手册问题可答，Top-3 ≥ 90% |
| 04 | 有记忆（ChatMemory） | 追问不重复、刷新不丢 |

## 3. 四段核心代码精讲

### 3.1 ChatClient 构建链（[02 §2.4]、[03 §2.5]、[04 §2.5]）

**为什么这样写**：
- `defaultSystem` 定人格（系统消息每次自动注入）
- `defaultTools` 注册能力（`@Tool` Bean 或 `ToolCallbackProvider`）
- `defaultAdvisors` 编排横切（RAG Advisor 先检索、Memory Advisor 后注入历史）
- **Advisor 顺序**：RAG → Memory → … 由 `@Order`/`getOrder()` 控制（[教程 01-WebFlux与响应式编程/04-WebFlux-vs-MVC §执行顺序]）

### 3.2 工具调用（[02 §2.2-2.3]）

**为什么这样写**：
- LLM 不执行代码，它只决定「调什么工具、传什么参数」；真正执行由 Spring AI 反射调用你的 Java 方法
- `@ToolParam(description)` 是 LLM 的唯一依据——**必须写清楚**
- 返回用 `record`（结构化输出），LLM 能稳定消费

### 3.3 RAG 检索注入（[03 §2.3-2.5]）

**为什么这样写**：
- ETL 三件套 `DocumentReader → TokenTextSplitter → VectorStore.add` 完成「文档 → 向量库」
- `QuestionAnswerAdvisor` 在每次请求时检索相关块注入上下文——**RAG 是横切关注点，用 Advisor 而非硬编码**
- `similarityThreshold=0.7` 防无关召回

### 3.4 记忆注入与保存（[04 §2.3-2.5]）

**为什么这样写**：
- `ChatMemory` 门面 + `ChatMemoryRepository` 存储两层组合
- **Redis 持久化必须自研 `ChatMemoryRepository`**（官方本地未实证 Redis 实现；`ChatMemoryRepository` 是同步 SPI，用同步 `RedisTemplate` + JSON 序列化，见 [04 迭代三 §2.3]）
- `MessageChatMemoryAdvisor` 流式也会自动写入（[附录 05-00 §2]）

### 3.5 本节核对（设计理解自测）

| # | 核对项 | PASS 判据（能不看正文回答） |
|---|--------|--------------------------|
| 1 | §3.1 | 说出 defaultSystem/defaultTools/defaultAdvisors 各自职责与 Advisor 顺序由什么控制 |
| 2 | §3.2 | 说出 LLM 在工具调用中"决定什么"、谁"执行什么" |
| 3 | §3.3 | 说出 ETL 三件套与 similarityThreshold 的作用 |
| 4 | §3.4 | 说出为什么 Redis 持久化必须自研 ChatMemoryRepository |

## 4. 手写下一步

1. 按 [01] → [04] 顺序手写，每篇过验收再前进
2. API 疑问查 [附录 05-SpringAI2-API基准]（Advisor/ChatMemory/Tool 三篇是唯一基准）
3. 主体链路写熟后 → 进入 [06-意图识别与多轮对话深化] 开始进阶迭代（意图分流/槽位/压缩 → HITL → 数据飞轮）
4. 想进阶生产级 → 进入 [教程 04-企业级架构主干/00-管控分离架构] 学习控制面/数据面分离等企业级架构主题

## 5. 可执行验证命令速查（01-04 各里程碑一锤定音）

主体迭代的每篇都给了「本节测试与验证」+ 全篇回归断言。以下是跨篇的关键命令汇总，写完一个迭代就执行对应命令，快速判断是否达标：

```bash
# ① 启动（两段式配置，基准端口 8081）
mvn spring-boot:run -Dspring-boot.run.profiles=customerservice

# ② 01 能对话（同步 + SSE 流式）
curl -X POST "http://localhost:8081/api/chat" -H "Content-Type: text/plain" -d "你好"
curl -N -X POST "http://localhost:8081/api/chat/stream" -H "Content-Type: text/plain" -d "你好"

# ③ 02 能做事（工具命中）
curl -X POST "http://localhost:8081/api/chat" -H "Content-Type: text/plain" -d "退换货政策是什么"
curl -X POST "http://localhost:8081/api/chat" -H "Content-Type: text/plain" -d "订单DD20240810状态"

# ④ 03 能读文档（RAG + 无关拒答）
curl -X POST "http://localhost:8081/api/chat" -H "Content-Type: text/plain" -d "人工客服的上班时间是什么时候？"
curl -X POST "http://localhost:8081/api/chat" -H "Content-Type: text/plain" -d "推荐一部好看的电影"

# ⑤ 04 有记忆（同一会话追问 + 跨会话隔离）
curl -X POST "http://localhost:8081/api/chat?sessionId=s1" -H "Content-Type: text/plain" -d "查订单DD20240810"
curl -X POST "http://localhost:8081/api/chat?sessionId=s1" -H "Content-Type: text/plain" -d "刚才那个订单到哪了"
curl -X POST "http://localhost:8081/api/chat?sessionId=s2" -H "Content-Type: text/plain" -d "刚才那个订单到哪了"

# ⑥ 编译闸门
mvn clean compile
```

| 里程碑 | 达标信号 | 失败排查 |
|--------|---------|---------|
| 01 | 流式 data: 块 ≥ 3、TTFT < 1s | 见 [01 §2.8] 失败排查 |
| 02 | FAQ/订单命中返回真实数据、无匹配拒答 | 见 [02 §2.6] 失败排查 |
| 03 | 手册问题 ≥ 6/7 命中、无关问题拒答 | 见 [03 §2.8] 失败排查 |
| 04 | s1 追问不重说订单号、s2 不串会话 | 见 [04 §2.7] 失败排查 |

> **定位回顾**：本文收束主体迭代（01-04）。核心链路：对话 + 工具 + RAG + 记忆，全部完整可手写。**下一步是你照着 01→04 手写，每篇过验收再前进；写熟后进入 [06-迭代四·意图识别与多轮对话深化] 开始进阶迭代。**
