# 项目 00：智能客服系统 — 04-迭代三·记忆与会话

> **定位**：为客服 Agent 接入 ChatMemory，实现跨请求的多轮对话记忆与会话持久化。涵盖 `MessageWindowChatMemory` 策略、Redis 持久化（自研 `ChatMemoryRepository`，**不用虚构的 `RedisChatMemory`**）、Advisor 链中的记忆注入与保存。本文给出**完整可手写代码**。
> **读者画像**：已完成工具集成和 RAG 知识库，需要实现真正的多轮对话。
> **前置阅读**：[03-迭代二-RAG 知识库]。
> **关联教程**：[教程 04-记忆与会话管理]、[教程 34-上下文工程]；API 真实性以 [附录 12-00 §2] 为准。

---

## 1. 为什么需要记忆

当前 Agent 每次请求是「无状态」的——上一轮的订单号、用户偏好都记不住。ChatMemory 让 Agent 能记住用户说的每一句话，追问不重复。

> **关键 API 澄清（[附录 12-00 §2]）**：`RedisChatMemory` / `MongoChatMemoryRepository` **不存在**。官方仅 `InMemoryChatMemoryRepository` 与 `JdbcChatMemoryRepository`。Redis 持久化必须**自研实现 `ChatMemoryRepository` 接口**（用 `ReactiveRedisTemplate`，WebFlux 铁律）。

## 2. 完整代码（照抄即可）

### 2.1 `pom.xml` 追加（Redis + JDBC 记忆依赖）

```xml
        <!-- 追加（记忆）：Redis（自研 ChatMemoryRepository 用）+ JDBC 仓库（可选） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-model-chat-memory-jdbc</artifactId>
        </dependency>
```

> 需要 JDBC 仓库时用 `JdbcChatMemoryRepository`（官方存在）；本迭代选 Redis 自研，走 `ReactiveRedisTemplate`。

### 2.2 `application.yml` 追加

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
  ai:
    chat:
      memory:
        window-size: 20        # 窗口内保留最近 20 条消息
```

### 2.3 `RedisChatMemoryRepository.java`（自研 ChatMemoryRepository，修复虚构 API）

```java
package com.shop.customer.memory;

import org.springframework.ai.chat.memory.ChatMemoryRepository;
import org.springframework.ai.chat.messages.Message;
import org.springframework.ai.chat.messages.MessageType;
import org.springframework.ai.chat.messages.AssistantMessage;
import org.springframework.ai.chat.messages.UserMessage;
import org.springframework.data.redis.core.ReactiveRedisTemplate;
import org.springframework.stereotype.Component;
import reactor.core.publisher.Mono;

import java.util.List;

/**
 * Redis 持久化的 ChatMemoryRepository（自研——官方无 Redis 实现，[附录 12-00 §2]）。
 * 用 ReactiveRedisTemplate（WebFlux 铁律：禁止阻塞 RedisTemplate）。
 */
@Component
public class RedisChatMemoryRepository implements ChatMemoryRepository {

    private static final String KEY_PREFIX = "chatmemory:";
    private final ReactiveRedisTemplate<String, Message> redis;

    public RedisChatMemoryRepository(ReactiveRedisTemplate<String, Message> redis) {
        this.redis = redis;
    }

    @Override
    public Mono<Void> add(String conversationId, List<Message> messages) {
        return redis.opsForList().rightPushAll(KEY_PREFIX + conversationId, messages).then();
    }

    @Override
    public Mono<List<Message>> findByConversationId(String conversationId, int lastN) {
        return redis.opsForList()
                .range(KEY_PREFIX + conversationId, -lastN, -1)
                .collectList();
    }

    @Override
    public Mono<Void> deleteByConversationId(String conversationId) {
        return redis.delete(KEY_PREFIX + conversationId).then();
    }
}
```

> ⚠️ `ChatMemoryRepository` 的精确方法签名（`add`/`findByConversationId`/`deleteByConversationId`）以你引入的 Spring AI 2.0 版本为准（[附录 12-00 §2]）。语义不变：**持久化走自研 repository，不用虚构类**。真实实现里 Message 序列化需自定义 codec。

### 2.4 `ChatMemoryConfig.java`（门面 + 仓库两层组合）

```java
package com.shop.customer.memory;

import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.ai.chat.memory.MessageWindowChatMemory;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class ChatMemoryConfig {

    @Bean
    public ChatMemory chatMemory(RedisChatMemoryRepository repository) {
        return MessageWindowChatMemory.builder()
                .chatMemoryRepository(repository)   // 官方仅 InMemory/Jdbc，Redis 用自研仓库
                .build();
    }
}
```

### 2.5 `ChatClientConfig` 更新（加记忆 Advisor，接会话 ID）

```java
package com.shop.customer.config;

import com.shop.customer.tool.FaqQueryTool;
import com.shop.customer.tool.OrderQueryTool;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.client.advisor.MessageChatMemoryAdvisor;
import org.springframework.ai.chat.client.advisor.QuestionAnswerAdvisor;
import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.ai.vectorstore.SearchRequest;
import org.springframework.ai.vectorstore.VectorStore;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class ChatClientConfig {

    @Bean
    public ChatClient chatClient(ChatClient.Builder builder,
                                 FaqQueryTool faqTool,
                                 OrderQueryTool orderTool,
                                 VectorStore vectorStore,
                                 ChatMemory chatMemory) {
        QuestionAnswerAdvisor ragAdvisor = QuestionAnswerAdvisor.builder(vectorStore)
                .searchRequest(SearchRequest.builder().topK(3).similarityThreshold(0.7).build())
                .build();
        MessageChatMemoryAdvisor memoryAdvisor = MessageChatMemoryAdvisor.builder(chatMemory).build();

        return builder
                .defaultSystem("你是电商客服小智，只能依据工具查询结果回答，不要编造订单/物流信息。")
                .defaultTools(faqTool, orderTool)
                .defaultAdvisors(ragAdvisor, memoryAdvisor)
                .build();
    }
}
```

### 2.6 `ChatService.java` 更新（传入会话 ID，走记忆）

```java
package com.shop.customer.service;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.stereotype.Service;
import reactor.core.publisher.Flux;

@Service
public class ChatService {

    private final ChatClient chatClient;

    public ChatService(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    public String chat(String sessionId, String message) {
        return chatClient.prompt()
                .user(message)
                .advisors(a -> a.param(ChatMemory.CONVERSATION_ID, sessionId))   // 常量，非字面量
                .call()
                .content();
    }

    public Flux<String> chatStream(String sessionId, String message) {
        return chatClient.prompt()
                .user(message)
                .advisors(a -> a.param(ChatMemory.CONVERSATION_ID, sessionId))
                .stream()
                .content();
    }
}
```

> 用 `ChatMemory.CONVERSATION_ID` 常量（[附录 12-00 §2]），**禁止抄字面量字符串**。

## 3. 验收

- [x] 追问「刚才那个订单呢」→ 能记住上一轮订单号
- [x] 刷新页面/新请求同 sessionId → 历史不丢（Redis 持久化）
- [x] 窗口超 20 条 → 只保留最近 20 条（MessageWindowChatMemory）

> **定位回顾**：本篇让 Agent 有记忆。至此项目 00 核心链路完成（对话 + 工具 + RAG + 记忆）。下一站 [05-核心代码讲解]——代码地图与精讲。
