# 项目 00：智能客服系统 — 04-迭代三·记忆与会话

> **定位**：为客服 Agent 接入 ChatMemory，实现跨请求的多轮对话记忆与会话持久化。涵盖 `MessageWindowChatMemory` 策略、Redis 持久化（自研 `ChatMemoryRepository`，**不用虚构的 `RedisChatMemory`**）、Advisor 链中的记忆注入与保存。本文给出**完整可手写代码**。
> **读者画像**：已完成工具集成和 RAG 知识库，需要实现真正的多轮对话。
> **前置阅读**：[03-迭代二-RAG 知识库]。
> **关联教程**：[教程 04-记忆与会话管理]、[教程 34-上下文工程]；API 真实性以 [附录 05-00 §2] 为准。

---

## 1. 为什么需要记忆

当前 Agent 每次请求是「无状态」的——上一轮的订单号、用户偏好都记不住。ChatMemory 让 Agent 能记住用户说的每一句话，追问不重复。

> **关键 API 澄清（[附录 05-00 §2]）**：`RedisChatMemory` / `MongoChatMemoryRepository` **不存在**。本地 javap 实证的内存实现是 `InMemoryChatMemoryRepository`（`org.springframework.ai.chat.memory`）；`ChatMemoryRepository` 是**同步 SPI**（`findConversationIds()` / `findByConversationId(String)` / `saveAll(String, List<Message>)` / `deleteByConversationId(String)`，javap 实证）。Redis 持久化必须**自研实现 `ChatMemoryRepository` 接口**。

## 2. 完整代码（照抄即可）

### 2.1 `pom.xml` 追加（Redis——自研 ChatMemoryRepository 用）

```xml
        <!-- 追加（记忆）：Redis（自研 ChatMemoryRepository 用） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
```

> `ChatMemory` / `ChatMemoryRepository` / `InMemoryChatMemoryRepository` 都在 `spring-ai-model`（已由 OpenAI starter 传递引入）。JDBC 记忆仓库的官方坐标本地未下载实证（[附录 05-00 §2] 口径），本迭代不引入；如需持久化仓库请自行核实官方 `spring-ai-starter-model-chat-memory` 再使用。Redis 用 `spring-boot-starter-data-redis`（同步 `RedisTemplate`，理由见 2.3）。

### 2.2 `application.yml` 追加

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
```

> 2.0.0 中 `spring.ai.chat.memory.window-size` 配置键已不存在（javap 实证：`ChatMemoryAutoConfiguration` 无对应 Properties 类）；窗口大小在 `MessageWindowChatMemory.Builder.maxMessages(int)` 上显式设置（见 2.4）。

### 2.3 `RedisChatMemoryRepository.java`（自研 ChatMemoryRepository，按 javap 实证签名修复）

```java
package com.shop.customer.memory;

import org.springframework.ai.chat.memory.ChatMemoryRepository;
import org.springframework.ai.chat.messages.Message;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Component;

import java.util.List;

/**
 * Redis 持久化的 ChatMemoryRepository（自研——本地未实证官方 Redis 实现，[附录 05-00 §2]）。
 *
 * 注意：ChatMemoryRepository 是 Spring AI 的同步 SPI（javap 实证 4 个同步方法）——
 * 官方 InMemory/JDBC 实现同为同步。这里用同步 RedisTemplate + 自定义 JSON 序列化
 * （见 ChatMemoryConfig 中的 RedisTemplate Bean）；如需在流式链路严格避免阻塞，
 * 应把记忆读写下沉到独立服务，或接受毫秒级 Redis 延迟。
 */
@Component
public class RedisChatMemoryRepository implements ChatMemoryRepository {

    private static final String KEY_PREFIX = "chatmemory:";
    private final RedisTemplate<String, Message> redis;

    public RedisChatMemoryRepository(RedisTemplate<String, Message> redis) {
        this.redis = redis;
    }

    @Override
    public List<String> findConversationIds() {
        return redis.keys(KEY_PREFIX + "*").stream().toList();
    }

    @Override
    public List<Message> findByConversationId(String conversationId) {
        List<Message> messages = redis.opsForList().range(KEY_PREFIX + conversationId, 0, -1);
        return messages != null ? messages : List.of();
    }

    @Override
    public void saveAll(String conversationId, List<Message> messages) {
        redis.opsForList().rightPushAll(KEY_PREFIX + conversationId, messages);
    }

    @Override
    public void deleteByConversationId(String conversationId) {
        redis.delete(KEY_PREFIX + conversationId);
    }
}
```

> ⚠️ `ChatMemoryRepository` 的真实方法签名（javap 实证）：`findConversationIds()` / `findByConversationId(String)` / `saveAll(String, List<Message>)` / `deleteByConversationId(String)`，全部同步（[附录 05-00 §2]）。**持久化走自研 repository，不用虚构类**。`Message` 是多态接口，Redis JSON 序列化需用启用默认类型信息的 `GenericJackson2JsonRedisSerializer`（其无参构造即开启 `@class` 类型信息，可正确还原 `UserMessage`/`AssistantMessage` 等子类型）。

### 2.4 `ChatMemoryConfig.java`（门面 + 仓库两层组合）

```java
package com.shop.customer.memory;

import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.ai.chat.memory.MessageWindowChatMemory;
import org.springframework.ai.chat.messages.Message;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.GenericJackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.StringRedisSerializer;

@Configuration
public class ChatMemoryConfig {

    /**
     * 自研仓库的 RedisTemplate（Message 序列化需自定义 codec）。
     */
    @Bean
    public RedisTemplate<String, Message> messageRedisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Message> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);
        template.setKeySerializer(new StringRedisSerializer());
        template.setValueSerializer(new GenericJackson2JsonRedisSerializer());
        return template;
    }

    @Bean
    public ChatMemory chatMemory(RedisChatMemoryRepository repository) {
        return MessageWindowChatMemory.builder()
                .chatMemoryRepository(repository)   // 自研 Redis 仓库（本地实证仅 InMemory）
                .maxMessages(20)                     // 窗口内保留最近 20 条消息（2.0.0 Builder 方法为 maxMessages）
                .build();
    }
}
```

### 2.5 `ChatClientConfig` 更新（加记忆 Advisor，接会话 ID）

```java
package com.shop.customer.config;

import com.shop.customer.tool.FaqQueryTool;
import com.shop.customer.tool.OrderQueryTool;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.client.advisor.MessageChatMemoryAdvisor;
import org.springframework.ai.chat.client.advisor.vectorstore.QuestionAnswerAdvisor;
import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.ai.vectorstore.SearchRequest;
import org.springframework.ai.vectorstore.VectorStore;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class ChatClientConfig {

    @Bean
    public ChatClient chatClient(ChatClient.Builder builder,
                                 FaqQueryTool faqTool,
                                 OrderQueryTool orderTool,
                                 VectorStore vectorStore,
                                 ChatMemory chatMemory) {
        QuestionAnswerAdvisor ragAdvisor = QuestionAnswerAdvisor.builder(vectorStore)
                .searchRequest(SearchRequest.builder().topK(3).similarityThreshold(0.7).build())
                .build();
        MessageChatMemoryAdvisor memoryAdvisor = MessageChatMemoryAdvisor.builder(chatMemory).build();

        return builder
                .defaultSystem("你是电商客服小智，只能依据工具查询结果回答，不要编造订单/物流信息。")
                .defaultTools(faqTool, orderTool)
                .defaultAdvisors(ragAdvisor, memoryAdvisor)
                .build();
    }
}
```
> **需在 pom.xml 中添加依赖**（`QuestionAnswerAdvisor` 所在模块）：2.0.0 起模块名从 `spring-ai-advisors-vector-store` 改为 `spring-ai-vector-store-advisor`（老坐标已不存在）——版本走 `spring-ai-bom`，不写 `<version>`：
>
> ```xml
> <dependency>
>     <groupId>org.springframework.ai</groupId>
>     <artifactId>spring-ai-vector-store-advisor</artifactId>
> </dependency>
> ```

### 2.6 `ChatService.java` 更新（传入会话 ID，走记忆）

```java
package com.shop.customer.service;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.stereotype.Service;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;
import reactor.core.scheduler.Schedulers;

@Service
public class ChatService {

    private final ChatClient chatClient;

    public ChatService(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    // 同步一次性对话：call() 是阻塞调用，收敛到 boundedElastic（WebFlux 铁律，与 01-最小Demo 一致）
    public Mono<String> chat(String sessionId, String message) {
        return Mono.fromCallable(() -> chatClient.prompt()
                .user(message)
                .advisors(a -> a.param(ChatMemory.CONVERSATION_ID, sessionId))   // 常量，非字面量
                .call()
                .content())
                .subscribeOn(Schedulers.boundedElastic());
    }

    public Flux<String> chatStream(String sessionId, String message) {
        return chatClient.prompt()
                .user(message)
                .advisors(a -> a.param(ChatMemory.CONVERSATION_ID, sessionId))
                .stream()
                .content();
    }
}
```

> 用 `ChatMemory.CONVERSATION_ID` 常量（[附录 05-00 §2]），**禁止抄字面量字符串**。

## 3. 验收

- [x] 追问「刚才那个订单呢」→ 能记住上一轮订单号
- [x] 刷新页面/新请求同 sessionId → 历史不丢（Redis 持久化）
- [x] 窗口超 20 条 → 只保留最近 20 条（MessageWindowChatMemory）

> **定位回顾**：本篇让 Agent 有记忆。至此项目 00 核心链路完成（对话 + 工具 + RAG + 记忆）。下一站 [05-核心代码讲解]——代码地图与精讲。
