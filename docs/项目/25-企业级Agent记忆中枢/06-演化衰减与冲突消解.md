# 06-演化衰减与冲突消解

> **定位**：让记忆有**生命周期**——不只是一直躺着。核心：**① score 强化衰减（反复命中加分、久不召回衰减归档）② 冲突消解（新旧矛盾怎么处理）③ 记忆质量评估（哪些记忆在帮/在害）**。呼应 [52-工业级记忆架构](../../教程/52-工业级记忆架构.md) §3-4、[01-记忆演化前沿](../../前沿/05-Agent记忆前沿.md)。前置阅读：[05-作用域隔离与多租户](05-作用域隔离与多租户.md)。
>
> **铁律 0**：衰减/消解算法自研「概念代码」（基于 03 的 score 字段）。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①score 强化衰减 + 定时衰减作业 ②冲突消解（时间/评分规则）③记忆质量评估（有效/失效标记） |
| **影响了哪些模块** | 03 的 score 字段 → 赋活；新增 衰减调度 + 冲突消解器 + 质量评估 |
| **架构如何演进** | 记忆从"静态存储"演进为"动态演化、可衰减、可消解" |
| **上一版痛点** | 旧偏好/错误事实一直存在误导 Agent；矛盾记忆无处理 |

**本迭代验收**：①反复命中记忆 score 升高常驻 ②久不召回的记忆衰减归档（不影响召回命中率）③新旧矛盾记忆按规则消解。

## 二、score 强化衰减 + 衰减作业

```java
// 概念代码：记忆 score 演化
public double onRecall(MemoryRecord r) { return Math.min(r.score()+DELTA, 1.0); }    // 被召回加强
public double onDecay(MemoryRecord r, long ageDays)                                  // 定时作业
    { return r.score() * Math.exp(-LAMBDA * ageDays); }                              // 指数衰减

@Scheduled(cron="0 3 * * *")                       // 每日衰减作业
public void decayJob() {
    for (var r : jdbc.queryAllMemories()) {        // 全记忆扫描衰减
        double s = onDecay(r, ageDays(r));
        if (s < ARCHIVE_THRESHOLD) archive(r);     // 衰减到阈值 → 归档(不参与召回)
        else jdbc.updateScore(r.id(), s);
    }
}
```

**收益**：①抑制"僵尸记忆"（长期没用还占召回位）②新近 + 反复 = 高 score 常驻（呼应用户真实偏好）。

## 三、冲突消解（新旧矛盾）

同一 scope 同主题新旧记忆矛盾（旧"偏好A"新"偏好B"）：

| 依据 | 处理 |
|------|------|
| 时间新 | 新版为主，旧版标 depreciated |
| score 高 | 反复强化的一方优先 |
| 两者都有依据 | 标"双版本+时间"，交 LLM 结合上下文判断（不武断删） |

**原则**：**宁显双版本 + 时间线索**，不暴力删旧——比"只看最新"更能处理"偏好会变但旧背景仍有用"的复杂情况。

## 四、记忆质量评估（为 08 飞轮铺路）

- 每次召回被 Agent 实际采用 → 记正信号；召回但没用 → 记负信号。
- 高负信号记忆 → 降级/建议清理；高正信号 → 保留强化。
- 这是 08 迭代"反馈回流"的雏形（呼应 [41-数据飞轮](../../教程/41-数据飞轮与持续改进.md)）。

## 五、验收

| 测试 | 期望 |
|------|------|
| 反复命中 | score 上升，常驻召回 |
| 久不召回 | 衰减归档，不影响召回 |
| 新旧矛盾 | 按时间/score 消解 |
| 双版本 | 标双版本 + 时间线索 |

> **下一步**：记忆会演化不误导了，但**合规还没落地**（用户要删记忆能删、召回别超 Token）。07 迭代做**遗忘权(GDPR) + Token 预算召回**——合法性与经济性。
