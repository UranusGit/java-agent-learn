# 06-断点续跑与崩溃闭合

> **定位**：把前几迭代的能力收敛为**崩溃后的完整恢复**。核心：**① 多步状态持久化（每步 Checkpoint）② 并发崩溃时的"孤儿步骤"幂等闭合 ③ 从崩溃点续跑（跳过已完成、闭合未完成）**。这是长任务引擎可靠性的"最后一公里"。前置阅读：[05-DAG步骤化与多Worker抢任务](05-DAG步骤化与多Worker抢任务.md)、[教程 40-长任务持久化与中断恢复](../../教程/40-长任务持久化与中断恢复.md)。
>
> **铁律 0**：Checkpoint 持久化自研「概念代码」（承教程40范式）；幂等重放用 03。

---

## 一、四问

| 问 | 答 |
|----|----|
| **新增了什么需求** | ①并发 DAG 的 Checkpoint（每步状态原子持久化）②孤儿步骤综合判定（幂等重放/安全重跑/人工）③聚合并发一致的恢复流程 |
| **影响了哪些模块** | Checkpoint 升级为"DAG + 并发步骤状态"；新增 恢复协调器 |
| **架构如何演进** | 从"单步 Checkpoint"演进为"并发 DAG 可视化恢复" |
| **上一版痛点** | 并发多个步骤崩溃时，哪些重放、哪些重跑、哪些要人？靠猜 |

**本迭代验收**：①崩溃后完整恢复，且**不重复副作用**（复用03）②孤儿步骤按规则确定性处理 ③恢复前后结果一致。

## 二、并发 Checkpoint 状态

```java
// 概念代码：每步原子状态（并发安全）
record StepRecord(String stepId, StepState state,
                  String opKey,            // 03 幂等键：崩溃判定依据
                  Map<String,Object> result) {}

// Checkpoint 保存：整张 stepState 表 + 每步原子 upsert
public void checkpoint(String runId, Map<String, StepRecord> steps) {
    for (var s : steps.values()) {
        stepStore.upsert(runId, s);   // 每步独立原子写，不做整表事务(可并发)
    }
}
```

## 三、并发恢复：孤儿步骤判定表

崩溃时可能有多个步骤停在 RUNNING。恢复协调器对每个孤儿步骤走**确定性判定**（用 03 幂等账本 + 该步是否外部副作用）：

| 账本状态 | 步骤性质 | 处理 |
|---------|---------|------|
| 已登记开始 + 已记结果 | 任意 | **重放结果**（副作用已发生一次） |
| 已登记开始 + 未记结果 | 有外部副作用(扣款/写库) | **人工/HITL 裁决**（不确定是否已触发） |
| 已登记开始 + 未记结果 | 无副作用(纯计算/读) | **安全重跑** |
| 未登记 | 任意 | **安全重跑**（副作用未发生） |

```java
// 概念代码：恢复协调器
public Mono<Void> recover(String runId) {
    return loadCheckpoint(runId)                     // 读取持久化步骤
        .flatMapMany(orphans -> Flux.fromIterable(orphans))
        .concatMap(orphan -> resolveOrphan(orphan))  // 按上表：重放/重跑/转HITL
        .then(continueFromLastDone(runId));          // 从未完成续跑
}
```

**核心纪律**：**宁可让"不确定是否触发副作用"的步骤转人工裁决，也不无脑重跑**——这是与"最大轮次""幂等"一起，构成可靠性的三重保险。

## 四、验收

| 测试 | 期望 |
|------|------|
| 多步骤崩溃 | 孤儿步骤按判定表确定性处理 |
| 不确定副作用 | 转 HITL，不误重跑 |
| 恢复续跑 | 从最后已完成步骤继续 |
| 结果一致性 | 恢复前后最终结果一致 |

> **下一步**：崩溃恢复已稳。但**长任务怎么让运维"看得见、可干预"**？07 迭代做**工作流可视回放 + 取消**——长任务的可观测与人工控制台。
