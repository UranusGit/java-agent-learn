# 02 - 环境准备

> 本系列的第一篇实操文档。完成后你将拥有：
> - 可运行的 Spring Boot 4 + JDK 21 后端骨架；
> - 可运行的 React + Vite 前端骨架；
> - 可用的 Docker 沙箱基础镜像；
> - 一个浏览器可访问的"ping/pong"演示页。

> ⚠️ 所有代码块仅作学习材料参考，需要你**自己手写**到对应文件。

---

## 本章五步地图

| 步 | 节 | 你要带走什么 |
|----|----|---------|
| ① 痛点 | §1 | 没这一章，03 章每个 import 都会红线——必须先把环境/骨架搭好 |
| ② 最小实现 | §2–§4 | pom 依赖 + WS Handler + React + Vite + Docker 沙箱镜像 |
| ③ 验证 | §2.6 + §4 | wscat 看到 pong；浏览器 ping REST 通；沙箱镜像 build 成功 |
| ④ 对照 | §5 | 与"裸 Spring AI demo"的环境差异 |
| ⑤ 避坑 | §6 | JDK 版本 / 端口冲突 / Docker socket 权限 / pnpm 版本 |

---

## 1. 痛点：为什么 01 章定下方案后不能直接写 Agent 代码

01 章设计完了架构，你可能想直接开写 AgentLoop——但你会发现：

- 没装 Docker Java client → 沙箱管理器编译不过
- 没装 Spring AI starter → `ChatClient` 找不到
- 没建 PostgreSQL 数据库 → JPA 启动报错
- 前端没建好 → 后端 WS 推了消息无人接
- 沙箱镜像不存在 → 后续 06 章启动容器失败

**这一章就是把这些"基础设施"一次性铺好**——之后每章都能聚焦业务逻辑而不是反复改 pom。

> 注意：本章的"功能"只有 ping/pong——这是**故意的**。我们用最小流程验证"前端 ↔ WS ↔ 后端"链路通，之后所有章节都建立在这个链路上。
>
> 不要在 02 章加任何业务逻辑（模型调用、状态管理、UI 组件……）。

## 2. 前置条件

| 工具 | 最低版本 | 检查命令 |
|------|---------|---------|
| JDK | 21 | `java -version` |
| Maven | 3.9 | `mvn -v` |
| Node.js | 20 | `node -v` |
| pnpm | 9 | `pnpm -v`（或 `npm i -g pnpm`）|
| Docker | 24 | `docker version` |
| Docker Compose | v2 | `docker compose version` |
| Git | 2.40 | `git --version` |
| ripgrep | 13 | `rg --version`（Bash 工具要用）|

> macOS 安装建议：`brew install openjdk@21 maven node pnpm ripgrep docker`

---

## 3. 后端骨架

### 3.1 在现有 pom.xml 中补充依赖

仓库根目录已有 `pom.xml`，编辑它，补充以下依赖（不要删除已有内容）：

```xml
<!-- 本代码仅作学习材料参考 -->
<properties>
    <java.version>21</java.version>
    <spring-boot.version>4.0.0</spring-boot.version>
    <spring-ai.version>2.0.0</spring-ai.version>
</properties>

<dependencies>
    <!-- 已有的依赖保留 -->

    <!-- Web + WebSocket -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-websocket</artifactId>
    </dependency>

    <!-- Spring AI 2.0（流式对话）-->
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>

    <!-- 数据库 -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
    </dependency>
    <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
    </dependency>

    <!-- Redis -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <!-- Docker Java client -->
    <dependency>
        <groupId>com.github.docker-java</groupId>
        <artifactId>docker-java-core</artifactId>
        <version>3.4.0</version>
    </dependency>
    <dependency>
        <groupId>com.github.docker-java</groupId>
        <artifactId>docker-java-transport-httpclient5</artifactId>
        <version>3.4.0</version>
    </dependency>

    <!-- S3 (用于 artifact 存储) -->
    <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>s3</artifactId>
        <version>2.27.0</version>
    </dependency>

    <!-- 工具库 -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <scope>provided</scope>
    </dependency>
</dependencies>
```

### 3.2 创建主应用类

新建 `src/main/java/org/demo02/webclaude/WebClaudeApplication.java`：

```java
// 本代码仅作学习材料参考
package org.demo02.webclaude;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WebClaudeApplication {
    public static void main(String[] args) {
        SpringApplication.run(WebClaudeApplication.class, args);
    }
}
```

### 3.3 WebSocket 配置

新建 `src/main/java/org/demo02/webclaude/ws/WebSocketConfig.java`：

```java
// 本代码仅作学习材料参考
package org.demo02.webclaude.ws;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.socket.config.annotation.EnableWebSocket;
import org.springframework.web.socket.config.annotation.WebSocketConfigurer;
import org.springframework.web.socket.config.annotation.WebSocketHandlerRegistry;

@Configuration
@EnableWebSocket
public class WebSocketConfig implements WebSocketConfigurer {

    private final SessionWebSocketHandler handler;

    public WebSocketConfig(SessionWebSocketHandler handler) {
        this.handler = handler;
    }

    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(handler, "/ws/session")
                .setAllowedOrigins("*");
    }
}
```

### 3.4 最简 WS Handler（ping/pong）

新建 `src/main/java/org/demo02/webclaude/ws/SessionWebSocketHandler.java`：

```java
// 本代码仅作学习材料参考
package org.demo02.webclaude.ws;

import org.springframework.stereotype.Component;
import org.springframework.web.socket.CloseStatus;
import org.springframework.web.socket.TextMessage;
import org.springframework.web.socket.WebSocketSession;
import org.springframework.web.socket.handler.TextWebSocketHandler;

import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

@Component
public class SessionWebSocketHandler extends TextWebSocketHandler {

    private final Map<String, WebSocketSession> sessions = new ConcurrentHashMap<>();

    @Override
    public void afterConnectionEstablished(WebSocketSession session) {
        sessions.put(session.getId(), session);
        System.out.println("[WS] connected: " + session.getId());
    }

    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) {
        String payload = message.getPayload();
        if ("ping".equals(payload)) {
            session.sendMessage(new TextMessage("pong"));
        }
    }

    @Override
    public void afterConnectionClosed(WebSocketSession session, CloseStatus status) {
        sessions.remove(session.getId());
        System.out.println("[WS] closed: " + session.getId());
    }
}
```

### 3.5 application.yaml 补充

编辑 `src/main/resources/application.yaml`，补充：

```yaml
# 本代码仅作学习材料参考
server:
  port: 8080

spring:
  application:
    name: web-claude
  datasource:
    url: jdbc:postgresql://localhost:5432/webclaude
    username: webclaude
    password: webclaude
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  ai:
    openai:
      base-url: ${AI_GATEWAY_URL:http://localhost:9090}
      api-key: ${AI_GATEWAY_KEY:dev-key}
      chat:
        options:
          model: claude-sonnet-4-6
  data:
    redis:
      host: localhost
      port: 6379

webclaude:
  sandbox:
    image: web-claude-sandbox:latest
    network: web-claude-net
  storage:
    s3-endpoint: ${S3_ENDPOINT:http://localhost:9000}
    s3-bucket: web-claude
    s3-access-key: ${S3_ACCESS_KEY:minioadmin}
    s3-secret-key: ${S3_SECRET_KEY:minioadmin}
```

### 3.6 检查点

启动后端：

```bash
mvn spring-boot:run
```

用 `wscat` 测试 WebSocket：

```bash
npx wscat -c ws://localhost:8080/ws/session
> ping
< pong
```

**检查点 02-1**：能看到 `< pong` 输出，说明 WebSocket 链路通。

---

## 4. 前端骨架

### 4.1 创建前端项目

仓库根目录：

```bash
pnpm create vite frontend --template react-ts
cd frontend
pnpm install
pnpm add @microsoft/fetch-event-source monaco-editor @monaco-editor/react \
         react-markdown remark-gfm mermaid xterm xterm-addon-fit
```

### 4.2 目录结构

```
frontend/
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   ├── api/
│   │   ├── ws.ts            ← WebSocket 客户端
│   │   └── rest.ts          ← REST 客户端
│   ├── components/
│   │   ├── ChatPanel.tsx    ← 对话面板
│   │   ├── ToolPanel.tsx    ← 工具调用面板
│   │   ├── ArtifactPanel.tsx ← 工件面板
│   │   ├── MessageBubble.tsx
│   │   └── Markdown.tsx
│   ├── hooks/
│   │   └── useSession.ts
│   ├── store/
│   │   └── sessionStore.ts
│   └── types/
│       └── message.ts
├── vite.config.ts
└── package.json
```

### 4.3 WebSocket 客户端

`src/api/ws.ts`：

```typescript
// 本代码仅作学习材料参考
export type WSMessage =
  | { type: 'ping' }
  | { type: 'pong' }
  | { type: 'state'; session_id: string; transition: string; message: any; seq: number }
  | { type: 'error'; reason: string };

export class SessionWS {
  private ws: WebSocket | null = null;
  private lastSeq = 0;
  private listeners: ((msg: WSMessage) => void)[] = [];

  constructor(private url: string) {}

  connect(onOpen?: () => void) {
    this.ws = new WebSocket(this.url);
    this.ws.onopen = () => onOpen?.();
    this.ws.onmessage = (e) => {
      const msg: WSMessage = JSON.parse(e.data);
      this.listeners.forEach((l) => l(msg));
    };
    this.ws.onclose = () => this.reconnect();
  }

  send(msg: WSMessage) {
    this.ws?.send(JSON.stringify(msg));
  }

  onMessage(l: (msg: WSMessage) => void) {
    this.listeners.push(l);
  }

  reconnect() {
    setTimeout(() => this.connect(), 1000);
  }
}
```

### 4.4 主 App

`src/App.tsx`：

```tsx
// 本代码仅作学习材料参考
import { useEffect, useState } from 'react';
import { SessionWS } from './api/ws';

export default function App() {
  const [status, setStatus] = useState<'idle' | 'connecting' | 'connected'>('idle');
  const [lastMsg, setLastMsg] = useState('');

  useEffect(() => {
    const ws = new SessionWS('ws://localhost:8080/ws/session');
    ws.onMessage((m) => {
      if (m.type === 'pong') setLastMsg('pong @ ' + new Date().toISOString());
    });
    setStatus('connecting');
    ws.connect(() => {
      setStatus('connected');
      ws.send({ type: 'ping' });
    });
  }, []);

  return (
    <div style={{ padding: 24 }}>
      <h1>Web Claude</h1>
      <p>Status: {status}</p>
      <p>Last: {lastMsg}</p>
      <button onClick={() => fetch('/api/ping').then(r => r.text()).then(setLastMsg)}>
        REST ping
      </button>
    </div>
  );
}
```

### 4.5 vite.config.ts（开发代理）

```typescript
// 本代码仅作学习材料参考
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:8080',
      '/ws': {
        target: 'ws://localhost:8080',
        ws: true,
      },
    },
  },
});
```

### 4.6 后端补一个 REST ping（前端要用）

新建 `src/main/java/org/demo02/webclaude/ws/HealthController.java`：

```java
// 本代码仅作学习材料参考
package org.demo02.webclaude.ws;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import reactor.core.publisher.Mono;

@RestController
public class HealthController {

    @GetMapping("/api/ping")
    public Mono<String> ping() {
        return Mono.just("pong-rest");
    }
}
```

### 4.7 检查点

启动前端：

```bash
cd frontend
pnpm dev
```

浏览器打开 http://localhost:5173。

**检查点 02-2**：
- 页面显示 `Status: connected`；
- 几秒后显示 `Last: pong @ ...`；
- 点 "REST ping" 按钮显示 `pong-rest`。

---

## 5. Docker 沙箱基础镜像

### 5.1 沙箱 Dockerfile

新建 `docker/sandbox/Dockerfile`：

```dockerfile
# 本代码仅作学习材料参考
FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive
ENV CLAUDE_JOB_DIR=/workspace

# 基础工具
RUN apt-get update && apt-get install -y --no-install-recommends \
    git curl wget jq ripgrep vim less ca-certificates \
    nodejs npm python3 python3-pip \
    openjdk-21-jdk-headless \
    && rm -rf /var/lib/apt/lists/*

# Playwright（用于自动化测试）
RUN npx -y playwright install --with-deps chromium

# 工作目录
RUN mkdir -p /workspace
WORKDIR /workspace

# 非 root 用户
RUN useradd -m -u 1000 claude
RUN chown -R claude:claude /workspace
USER claude

# 默认进入 bash
CMD ["sleep", "infinity"]
```

### 5.2 构建镜像

```bash
cd docker/sandbox
docker build -t web-claude-sandbox:latest .
```

### 5.3 创建专用网络

```bash
docker network create web-claude-net
```

### 5.4 测试沙箱

```bash
docker run --rm --network web-claude-net \
  -v $(pwd)/tmp-sandbox-test:/workspace \
  web-claude-sandbox:latest \
  bash -c "echo hello > /workspace/test.txt && rg --version && node --version"
```

**检查点 02-3**：
- 看到 ripgrep 版本号；
- 看到 node 版本号；
- 本地 `tmp-sandbox-test/test.txt` 内容是 `hello`。

---

## 6. 基础设施（PostgreSQL + Redis + MinIO）

### 6.1 docker-compose.dev.yaml

仓库根目录新建 `docker-compose.dev.yaml`：

```yaml
# 本代码仅作学习材料参考
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: webclaude
      POSTGRES_USER: webclaude
      POSTGRES_PASSWORD: webclaude
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
```

### 6.2 启动基础设施

```bash
docker compose -f docker-compose.dev.yaml up -d
```

### 4.3 创建 S3 桶

打开 MinIO 控制台 http://localhost:9001（用户名 `minioadmin`），创建 bucket `web-claude`。

或用 mc：

```bash
docker run --rm --network=host minio/mc \
  alias set local http://localhost:9000 minioadmin minioadmin
docker run --rm --network=host minio/mc \
  mb local/web-claude
```

### 6.4 检查点

```bash
docker compose -f docker-compose.dev.yaml ps
```

**检查点 02-4**：3 个容器（postgres / redis / minio）都是 `running` 状态。

---

## 7. 数据库初始化（Flyway）

### 7.1 创建迁移文件

新建 `src/main/resources/db/migration/V1__init.sql`：

```sql
-- 本代码仅作学习材料参考
CREATE TABLE sessions (
    id              UUID PRIMARY KEY,
    tenant_id       UUID NOT NULL,
    user_id         UUID NOT NULL,
    title           VARCHAR(255),
    mode            VARCHAR(32) NOT NULL DEFAULT 'default',
    status          VARCHAR(32) NOT NULL DEFAULT 'active',
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    parent_session_id UUID,
    worktree_path   VARCHAR(512),
    model           VARCHAR(64),
    usage_tokens    BIGINT NOT NULL DEFAULT 0,
    cost_usd        DECIMAL(10,4) NOT NULL DEFAULT 0
);

CREATE TABLE messages (
    id                  UUID PRIMARY KEY,
    session_id          UUID NOT NULL REFERENCES sessions(id),
    parent_uuid         UUID,
    role                VARCHAR(16) NOT NULL,
    content             JSONB NOT NULL,
    transition          VARCHAR(32),
    tokens_in           INT,
    tokens_out          INT,
    created_at          TIMESTAMP NOT NULL DEFAULT NOW(),
    is_compact_boundary BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX idx_messages_session ON messages(session_id, created_at);

-- v1 简化：单租户（tenant_id 固定为占位 UUID）
INSERT INTO sessions (id, tenant_id, user_id, title) VALUES
  ('00000000-0000-0000-0000-000000000001'::uuid,
   '00000000-0000-0000-0000-000000000001'::uuid,
   '00000000-0000-0000-0000-000000000001'::uuid,
   'seed session') ON CONFLICT DO NOTHING;
```

### 7.2 检查点

启动后端，看日志中 Flyway 输出 `Migrating schema "public" to version "1 - init"`。

**检查点 02-5**：连接 PostgreSQL，看到 `sessions` 和 `messages` 表。

```bash
docker exec -it $(docker ps -qf name=postgres) \
  psql -U webclaude -d webclaude -c "\dt"
```

---

## 8. 验证：联调

### 8.1 全栈启动顺序

```bash
# 1. 基础设施
docker compose -f docker-compose.dev.yaml up -d

# 2. 后端
mvn spring-boot:run

# 3. 前端
cd frontend && pnpm dev
```

### 8.2 端到端检查

浏览器 http://localhost:5173。

**检查点 02-6（章节总验收）**：
- [ ] 前端显示 `Status: connected`；
- [ ] WS ping → pong 通；
- [ ] REST `/api/ping` 返回 `pong-rest`；
- [ ] DB 中 `sessions` 和 `messages` 表存在；
- [ ] 沙箱镜像 `web-claude-sandbox:latest` 可运行；
- [ ] MinIO 中 `web-claude` bucket 存在。

---

## 9. 对照：与"裸 Spring AI demo"的环境差异

| 维度 | 裸 demo（无 02 章） | 02 章就绪后 |
|------|--------------------|------------|
| 后端框架 | Spring MVC 阻塞 | Spring Boot 4 + WebFlux + WS |
| 模型层 | 自己 new OpenAI client | Spring AI 2.0 starter |
| 数据库 | 无 / H2 | PostgreSQL + Flyway 迁移 |
| 缓存 | 无 | Redis |
| 对象存储 | 无 | MinIO（S3 兼容） |
| 沙箱 | 无 | Docker 镜像 + 专用 network |
| 前端 | curl 测试 | React + Vite + WS 客户端 |
| 验证手段 | 看日志 | wscat + 浏览器 UI |

> 02 章不写一行业务代码，**但它是后面所有章节的"地基"**。所有 import 都从这里来。

---

## 10. 避坑：环境准备常踩的雷

### Q1: WS 连接失败（前端 Status 一直是 connecting）

- 检查后端是否启动（端口 8080）；
- 检查 vite 代理是否配了 `/ws`；
- Chrome DevTools → Network → WS 看握手是否成功。

### Q2: Flyway 报 "schema differs"

- JPA 用 `ddl-auto: validate`，确保 Entity 字段与 SQL 完全一致；
- 实在不行临时改成 `ddl-auto: update`（仅开发）。

### Q3: 沙箱构建很慢

- Dockerfile 用了 Playwright 安装 chromium，比较大；
- 第一次构建约 5-10 分钟；
- 后续构建走 docker cache 快很多。

### Q4: 端口冲突

- 8080 / 5173 / 5432 / 6379 / 9000 / 9001 任一被占用都会失败；
- 用 `lsof -i :端口` 检查；
- 修改对应 yaml 端口映射。

---

## 11. 下一步

环境就绪，进入 [03-项目骨架](./03-项目骨架.md)，开始搭建 Agent Engine 的最简骨架，跑通"用户发消息 → 模型回复 → 流式渲染"的完整链路。
