# 35 文档会话级独占改造蓝图（执行依据，非交付物）

## 核查结论
文档全程无 sessionId 概念。第6章 create/trigger/RunStore.create 签名都只有 prompt+idempotencyKey。
第6章 1728 行明确写"一会话可多次 run，互不干扰"——与"同会话同时只一个任务"诉求直接对立。

## 自洽性改动清单（必须成套改，否则矛盾）

### A. 接口层：RunController.create 加 sessionId 参数（第6章）
- @RequestParam String sessionId
- 传给 streamService.trigger(prompt, sessionId, idemKey)
- 第9章 StreamService.trigger 也要同步加 sessionId（否则签名不一致）

### B. 存储层：RunStore 记录 run→session 映射 + 会话级独占锁
- create(idempotencyKey, sessionId)：新建 run 时记 sessionId
- 新增 acquireSession(sessionId, runId)：SETNX session:{id}:running，抢到才允许触发
- 新增 releaseSession(sessionId)：终态时释放
- get/run 资源 JSON 里加 session 字段

### C. 编排层：StreamService.trigger 加 sessionId + 会话闸门
- 顺序：acquireSession(SETNX) → create run → setStatus(RUNNING) → acquireLock(runId) → startGeneration
- 抢不到会话锁 → Mono.error(409 Conflict "当前会话有任务在跑")
- startGeneration 的 doOnComplete/doOnError/cancel 三处都要 releaseSession

### D. 文字论述修正（自洽关键）
- 第6章 1728 行"一会话可多次 run，互不干扰" → 改为"一会话可发起多次 run，但同时只能有一个在跑（会话级独占），到终态才能发起新的"
- 前言/概念速查：补充"会话级独占"概念
- 第6章场景：补"多端/狂点"痛点 → 会话级独占解法
- 第6章新增 6.x 小节专门讲会话级独占（状态机图 + 代码 + 三终态释放 + TTL兜底）

### E. 取消逻辑（第6章 cancel + 第7章跨实例取消）
- cancel 时也要 releaseSession（否则取消后会话永久锁死）
- 第7章跨实例取消监听里，也要释放会话锁

### F. checkpoint/验证 curl 更新
- 所有 POST 示例加 sessionId 参数
- 新增"会话独占验证"：同 sessionId 第二次 POST 应返回 409

## 不改的部分（保持自洽）
- 第0-5章：没有 run/session 概念，不动
- 第7章 SETNX 任务锁、第6章幂等键：保留（会话锁是更外层闸门，三者并存）
- Redis/Kafka/网关：不动
- 第9章 Kafka 版：trigger 加 sessionId 透传，其余不动

## 自洽性校验点（改完必须满足）
1. 全文 trigger/create 签名一致（都带 sessionId）
2. 任何创建 run 的路径，都先过会话闸门
3. 任何 run 终态（DONE/FAILED/CANCELLED）路径，都释放会话锁
4. 不再出现"一会话可多次 run 互不干扰"这种和新逻辑矛盾的表述
