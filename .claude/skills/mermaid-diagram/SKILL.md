---
name: mermaid-diagram
description: 在 Markdown 文档中生成格式正确、可直接在 Typora 中渲染的 Mermaid 图。只要用户需要在文档里画架构图、流程图、时序图、调用链、状态机、UML 类图、ER 图、甘特图、饼图、思维导图、时间线等，就必须使用本 skill。始终用 Mermaid 图代替文本/ASCII 流程图（`-->` 文本箭头、`+---+` 字符画）；文档中已有文本流程图时，识别并改写为 Mermaid。生成时遵循 Typora 兼容的保守语法，保证不报错。当用户提到 mermaid、流程图、架构图、时序图、状态机、UML、甘特、ER 图、思维导图，或要求"画个图"、"用图表示"、"示意图"、"把这段流程画出来"时触发。
---

# Mermaid 图生成（Typora 兼容）

在 Markdown 文档中把"需要一张图"的需求落地为格式正确、能直接渲染的 Mermaid 代码块。用户使用 Typora 阅读，所以一切语法选择以 Typora 能渲染为第一优先级。

## 三个铁律

1. **能用图就不要用文本流程图。** 文档需要表达流程、结构、关系、时序时，一律输出 ```` ```mermaid ```` 代码块。绝不输出 ASCII 字符画（`+---+`、文本 `-->` 箭头、`| v |` 竖排箭头）。除非用户明确说不要图。
2. **Typora 兼容优先。** 只写 Typora 内置 Mermaid 11.x 稳定支持的语法。避免 `-beta` 实验图（`radar-beta`、`treemap-beta`、`block-beta`）和 `flowchart-elk` / `graph-elk` 布局引擎。不确定某特性是否兼容时，查 `references/Typora兼容性.md`。
3. **语法必须完整可渲染。** 生成结果用户复制进 Typora 就能渲染出图，不能有解析错误。写完后必须过一遍「输出前自查清单」。

## 什么时候用哪种图

| 要表达的内容 | 图类型 | 常见说法 |
|---|---|---|
| 流程、分支、架构、组件依赖 | flowchart | 流程图、架构图 |
| 消息交互、调用链、请求响应 | sequenceDiagram | 时序图、调用链 |
| 类、继承、接口、对象关系 | classDiagram | UML、类图 |
| 状态机、生命周期、状态迁移 | stateDiagram-v2 | 状态机、状态流转 |
| 数据表、实体关系、外键 | erDiagram | ER 图、表结构、数据模型 |
| 项目排期、任务依赖、里程碑 | gantt | 甘特、排期、进度 |
| 占比、分布 | pie | 比例、分布 |
| 层级分类、头脑风暴 | mindmap | 思维导图 |
| 时间线、里程碑顺序 | timeline | 时间线、里程碑 |
| git 分支合并、CI 流程 | gitGraph | git 分支 |
| 报文/协议字段布局 | packet | 报文、协议、字节布局 |
| 问题原因分析 | ishikawa | 鱼骨图 |

> 不要为了画图而画图：一两个箭头能说清的关系，用一句话或一个表格更好；分支过多的大流程拆成多张小图，别塞进一张图里。

## 通用语法铁律（所有图都适用）

1. 代码块语言标识必须是 `mermaid`（Typora 靠这个渲染）：
   ```` ```mermaid ````
2. **标签含特殊字符就加双引号。** `( ) [ ] { } : # "` 等都会让解析器报错或显示错乱。规则：只要标签文本里有除中英文、数字、下划线、空格、连字符、点以外的字符，整个标签用 `"..."` 包裹：
   - `A["计算 (含公式)"]` —— 有括号，必须加引号
   - `A["状态: 完成"]` —— 有冒号，必须加引号
3. 中文文本可以直接写；但含特殊字符（尤其 `:` `(` `)`）时同样要加引号。**凡是拿不准的，一律加引号。**
4. **ID 用英文小写字母、数字、下划线**，不能含空格和标点（子图 ID 同规则）。ID 只做引用，显示文本放标签里：
   - `participant client as 客户端`
   - `client->>server: 请求`
5. 同一张图里 ID 全局唯一，不要复用。
6. 注释用 `%%` 开头：`%% 这是注释`。
7. 配置指令 `%%{init: {...}}%%` 放第一行，只写最小配置（如主题）。
8. **一个代码块只放一张图。** Typora 对多图 `&&` 分隔支持不稳定，不要用。
9. 生成完的 mermaid 紧跟在相应正文后，代码块内不要放别的语言。

## 中文节点（你文档的主要场景）

你的文档节点基本都是中文。中文在 Mermaid 里是**合法文本**，直接写即可，记住三条：

1. **纯中文直接写，不需要引号**：
   - `A[用户提交订单]`
   - `待支付 --> 已支付`
   - `client->>server: 发送消息`
2. **ID 保持英文**，中文放标签 / 显示名：`participant client as 客户端`。之后引用永远用 `client`，不是 `客户端`。
3. **中文里混入特殊字符（`:` `(` `)` `"` 等）时，整个标签加双引号**：
   - `A["状态: 完成"]`
   - `A["计算 (含公式)"]`
   - **拿不准就加引号**——加引号永远比不加安全。

**例外（标识符严格的图）**：`classDiagram` 的类名 / 成员名、`erDiagram` 的实体名 / 属性名建议用英文（解析器按标识符解析，中文易出问题）；中文语义放 `note` 注释或关系标签里，如 `Dog --|> Animal : 继承`。

## 生成流程

1. 从需求/文档上下文提炼要表达的结构：有哪些节点、实体、参与者，它们之间的关系是什么。
2. 按上面的表格选图类型。
3. 打开对应 reference 文件取模板：`references/<类型>.md`。
4. 套模板写 mermaid，套用「通用语法铁律」。
5. 若文档中已有文本流程图，用 `references/文本流程图转换.md` 的规则转换，不要留字符画。
6. 输出前过一遍自查清单。

## 输出前自查清单

- [ ] 代码块语言标识是 `mermaid`？
- [ ] 图类型关键字正确（`flowchart` / `sequenceDiagram` / `classDiagram` / `stateDiagram-v2` / `erDiagram` / `gantt` / `pie` / `mindmap` / `timeline` / `gitGraph` / `packet-beta` / `sankey-beta` / `xychart-beta` / `quadrantChart` / `kanban` / `architecture-beta` / `venn` / `ishikawa`）？
- [ ] 含特殊字符的标签都加了双引号？
- [ ] 中文节点含 `:` `(` `)` 等特殊字符时也加了引号？（拿不准的也加了？）
- [ ] 引号成对闭合；`{` `}` `(` `)` `[` `]` 成对？
- [ ] 每个 ID 只定义一次？
- [ ] 依赖缩进的图（mindmap / timeline / kanban / stateDiagram 复合态）缩进层级正确且一致？
- [ ] 每张图独立一个代码块，没有 `&&` 多图分隔？
- [ ] 没有 `-beta` 实验特性和 `flowchart-elk`？

## References

| 文件 | 内容 | 何时读 |
|---|---|---|
| `references/流程图.md` | 流程图：节点形状、箭头、子图、链接 | 画流程/架构图时 |
| `references/时序图.md` | 时序图：参与者、消息、激活、alt/loop/par | 画调用链/交互时 |
| `references/类图.md` | 类图：成员、关系、泛型、装饰 | 画 UML 类图时 |
| `references/状态图.md` | 状态图：迁移、复合状态、标签 | 画状态机时 |
| `references/ER图.md` | ER 图：实体、属性、基数 | 画数据模型时 |
| `references/甘特图.md` | 甘特图：任务、依赖、里程碑、状态标记 | 画排期时 |
| `references/饼图思维导图时间线.md` | 饼图 / 思维导图 / 时间线 / 旅程 | 画占比/层级/时间线时 |
| `references/新图类型.md` | 较新图：gitGraph / packet / sankey / 鱼骨图等 | 用户点名要这些图时 |
| `references/文本流程图转换.md` | ASCII 文本流程图 → Mermaid 的转换规则 | 文档里有字符画流程图时 |
| `references/Typora兼容性.md` | Typora 版本兼容性矩阵与排查 | 确认某特性是否兼容、图渲染失败时 |
